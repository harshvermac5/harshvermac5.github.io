<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://harshvermac5.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://harshvermac5.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-02T12:02:14+00:00</updated><id>https://harshvermac5.github.io/feed.xml</id><title type="html">Harsh Verma</title><subtitle>Portfolio and learning hub of Harsh Verma, a server and network engineer focused on Linux, data center infrastructure, networking, and automation. </subtitle><entry><title type="html">Diagnosing WAN Failures Beyond Physical Link State</title><link href="https://harshvermac5.github.io/learn/2026/diagnosing-wan-failures/" rel="alternate" type="text/html" title="Diagnosing WAN Failures Beyond Physical Link State"/><published>2026-09-02T09:58:12+00:00</published><updated>2026-09-02T09:58:12+00:00</updated><id>https://harshvermac5.github.io/learn/2026/diagnosing-wan-failures</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/diagnosing-wan-failures/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>A WAN interface reporting <strong>link up</strong> does not prove that the Internet path is usable. Ethernet carrier state confirms only that two adjacent physical-layer devices have established a link. It says nothing about upstream forwarding, congestion, recursive DNS behavior, or the reachability of remote services.</p> <p>This article explains how to isolate WAN failures by correlating physical-link telemetry with active ICMP and DNS probes. It also examines how a redundant edge should convert those measurements into stable failover decisions without reacting to a single lost packet or a single unresponsive target.</p> <p>The core principle is simple: <strong>failure detection must test the service level that the routing decision is intended to protect</strong>. Carrier detection protects against a failed local attachment. Routed probes test IP forwarding. DNS probes test an application dependency. These signals are related, but they are not interchangeable.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Multi-WAN path health detection, layered fault isolation, and deterministic failover state machines.</strong></p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>Ethernet link establishment is governed by <strong>IEEE 802.3-2022</strong>. A PHY may report carrier, negotiated speed, and duplex while frames beyond the directly connected device are being discarded. A clean MAC interface therefore rules out some local faults, but it cannot certify the provider network.</p> <p>At the network layer, <strong>ICMP Echo Request and Echo Reply</strong> are defined by <a href="https://www.rfc-editor.org/info/rfc792/">RFC 792, Internet Control Message Protocol, 1981</a>. Echo exchanges provide a convenient active reachability signal, but ICMP is not a reliability protocol. A missing reply may represent path loss, rate limiting, filtering, reverse-path failure, or destination behavior. Health monitors must therefore use multiple samples and, preferably, multiple independent destinations.</p> <p>Packet loss and delay have formal measurement models. <a href="https://www.rfc-editor.org/rfc/rfc2680">RFC 2680, 1999</a> defines loss relative to a selected waiting time, while <a href="https://www.rfc-editor.org/rfc/rfc2679.html">RFC 2679, 1999</a> defines one-way delay. <a href="https://www.rfc-editor.org/info/rfc5357/">RFC 5357, TWAMP, 2008</a> provides a standardized active-measurement architecture.</p> <p>DNS adds an application-semantic test. <a href="https://www.rfc-editor.org/info/rfc1035/">RFC 1035, Domain Names—Implementation and Specification, 1987</a> defines DNS message structure, including the transaction identifier, flags, section counts, questions, and resource records. A response is not healthy merely because a UDP datagram arrives from port 53. The monitor must validate that the response is syntactically well formed, corresponds to the query, and contains an acceptable result.</p> <p>No universal RFC defines a generic multi-WAN failover algorithm. <a href="https://www.rfc-editor.org/rfc/rfc5880">RFC 5880, Bidirectional Forwarding Detection, 2010</a> supplies a useful analogue: periodic control traffic, timers, and explicit state transitions detect forwarding-path failure.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A robust monitor evaluates the path in layers.</p> <ul> <li> <p><strong>Physical state:</strong> Read carrier, negotiated speed, duplex, PHY resets, and link transitions. If carrier drops, the failure is at or before the adjacent device. Repeated down intervals of nearly identical duration can indicate deterministic modem retraining, watchdog recovery, or firmware restart behavior rather than random cable movement.</p> </li> <li> <p><strong>Interface integrity:</strong> Inspect CRC errors, alignment errors, runts, giants, symbol errors, dropped frames, and carrier counters. Rising corruption counters with link flaps implicate the local cable, optics, PHY, or adjacent port. Zero error counters do not prove end-to-end health, but they materially weaken a local Layer 1 hypothesis.</p> </li> <li> <p><strong>Next-hop reachability:</strong> Test the directly connected gateway when it is expected to answer. Success proves the local Layer 2 segment, address resolution, and a limited portion of the upstream control plane. Failure may still reflect an intentionally silent gateway, so this test requires topology knowledge.</p> </li> <li> <p><strong>Remote IP reachability:</strong> Probe multiple stable addresses through the monitored interface. Bind both the source address and egress interface so policy routing cannot accidentally send the probe through a healthy secondary WAN. Measure loss, round-trip time, and response consistency across a sliding window.</p> </li> <li> <p><strong>Application validation:</strong> Send a deterministic DNS query through a selected resolver. Validate the transaction ID, QR bit, opcode, response code, question tuple, section boundaries, and resource-record encoding. A malformed response proves that some packet exchange occurred, but not that the resolver path is usable.</p> </li> </ul> <p>The monitor then converts samples into a state machine such as <strong>UP</strong>, <strong>DEGRADED</strong>, <strong>DOWN</strong>, and <strong>RECOVERING</strong>. A single timeout should not normally produce <strong>DOWN</strong>. Instead, the decision should combine consecutive failures, windowed loss, delay limits, and agreement between independent probe types.</p> <p><strong>Hysteresis</strong> prevents route oscillation. The failure threshold can be relatively short to protect active traffic, while the recovery threshold should require a longer run of successful probes. This asymmetric policy avoids restoring the preferred route during a brief improvement in an unstable circuit.</p> <p>An interface can be administratively enabled and carrier-up yet unusable because its routed probes fail. That combination indicates a fault beyond the local Ethernet attachment; it is not contradictory.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>Begin by checking local link and interface evidence on a Linux-based edge:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ethtool wan0
ip <span class="nt">-s</span> <span class="nb">link </span>show dev wan0
ethtool <span class="nt">-S</span> wan0
</code></pre></div></div> <p>A result such as the following narrows the fault domain:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Speed: 2500Mb/s
Duplex: Full
Link detected: yes
RX errors: 0
TX errors: 0
Carrier changes: 0
</code></pre></div></div> <p>This does not prove Internet reachability. It proves that the sampled local attachment is established and that the host has not observed common MAC-layer faults.</p> <p>Bind active tests to the WAN under investigation:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ping <span class="nt">-I</span> wan0 <span class="nt">-c</span> 20 1.1.1.1
ping <span class="nt">-I</span> wan0 <span class="nt">-c</span> 20 8.8.8.8
dig @1.1.1.1 example.net A +tries<span class="o">=</span>1 +time<span class="o">=</span>2
</code></pre></div></div> <p>Evidence such as <strong>57 percent packet loss</strong>, multi-second or tens-of-seconds round-trip time, and invalid DNS messages while carrier remains up identifies a severe upstream forwarding problem. The conclusion is stronger when independent destinations fail concurrently and a secondary WAN remains healthy.</p> <p>A generic monitor policy might be expressed as follows:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">health_check</span><span class="pi">:</span>
  <span class="na">interval_ms</span><span class="pi">:</span> <span class="m">1000</span>
  <span class="na">timeout_ms</span><span class="pi">:</span> <span class="m">2000</span>
  <span class="na">targets</span><span class="pi">:</span>
    <span class="na">icmp</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">198.51.100.10</span><span class="pi">,</span> <span class="nv">203.0.113.10</span><span class="pi">]</span>
    <span class="na">dns</span><span class="pi">:</span>
      <span class="na">server</span><span class="pi">:</span> <span class="s">192.0.2.53</span>
      <span class="na">question</span><span class="pi">:</span> <span class="s">health.example.net.</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">A</span>
  <span class="na">declare_down</span><span class="pi">:</span>
    <span class="na">consecutive_failed_rounds</span><span class="pi">:</span> <span class="m">5</span>
    <span class="na">require_failed_probe_classes</span><span class="pi">:</span> <span class="m">2</span>
  <span class="na">declare_up</span><span class="pi">:</span>
    <span class="na">consecutive_successful_rounds</span><span class="pi">:</span> <span class="m">20</span>
</code></pre></div></div> <p>The documentation ranges are illustrative. In production, use permitted targets whose routing independence matches the design objective.</p> <p>Correlate probe transitions with carrier events using a monotonic clock and normalized timestamps. If four link losses each recover after exactly 38 seconds, the repeatability is significant. Random copper faults usually vary; fixed recovery intervals more often indicate retraining, reboot, or watchdog behavior. This remains an inference until upstream telemetry confirms it.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <ul> <li> <p><strong>IEEE 802.3-2022</strong>, Ethernet, defines MAC and PHY operation, link establishment, and management parameters.</p> </li> <li> <p><strong>RFC 792, 1981</strong>, defines ICMPv4 Echo and Echo Reply behavior.</p> </li> <li> <p><strong>RFC 1035, 1987</strong>, defines DNS message encoding and response semantics.</p> </li> <li> <p><strong>RFC 2679 and RFC 2680, 1999</strong>, define IP performance metrics for delay and packet loss.</p> </li> <li> <p><strong>RFC 5357, 2008</strong>, defines TWAMP for standardized two-way active measurement.</p> </li> <li> <p><strong>RFC 5880, 2010</strong>, defines BFD timers and state-machine principles for rapid forwarding-failure detection.</p> </li> <li> <p><strong>ITU-T Y.1540</strong>, Internet protocol data communication service performance parameters, provides a carrier-oriented framework for IP transfer performance.</p> </li> </ul> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li> <p><strong>Carrier is adjacency evidence, not service evidence.</strong> A provider path can fail while the customer-facing Ethernet link remains stable.</p> </li> <li> <p><strong>Probe diversity reduces false attribution.</strong> ICMP tests forwarding, while DNS validation tests both transport and application semantics. Multiple destinations reduce dependence on one host’s response policy.</p> </li> <li> <p><strong>Clean counters narrow rather than close the investigation.</strong> They make local corruption unlikely but do not exclude faults inside an upstream modem or provider network.</p> </li> <li> <p><strong>Timing patterns reveal state machines.</strong> Identical outage durations are evidence of deterministic recovery behavior and should be correlated with modem retraining, watchdog, thermal, and firmware logs.</p> </li> <li> <p><strong>Failover is a control-system problem.</strong> Aggressive detection reduces outage time but increases false positives. Conservative recovery and hysteresis protect against flapping.</p> </li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li> <p>Monitor physical, network, and application layers independently and retain the raw measurements behind every failover event.</p> </li> <li> <p>Bind probes to the intended source and interface, and verify the resulting route, so a healthy backup path cannot mask primary-path failure.</p> </li> <li> <p>Use multiple administratively independent probe targets and require consensus across probe classes before withdrawing a route.</p> </li> <li> <p>Record loss, delay distribution, DNS validation failures, carrier transitions, and interface counters with synchronized timestamps.</p> </li> <li> <p>Maintain separate failure and recovery thresholds. Require sustained health before failing back to the preferred circuit.</p> </li> <li> <p>When evidence points upstream, request provider telemetry such as signal-to-noise ratio, correctable and uncorrectable codewords, optical levels where applicable, and timeout or retraining history.</p> </li> </ul> <p>The decisive troubleshooting habit is to ask what each signal actually proves. Link state proves local physical adjacency. Clean counters reduce the probability of local corruption. Successful remote probes prove forwarding at a moment in time. Only their correlation provides a defensible diagnosis and a stable failover design.</p>]]></content><author><name></name></author><category term="Networking"/><category term="wan"/><category term="failover"/><category term="network-monitoring"/><summary type="html"><![CDATA[A layered method for distinguishing Ethernet carrier loss, upstream path degradation, and application-level failure in redundant WAN designs.]]></summary></entry><entry><title type="html">Diagnosing Linux Critical Thermal Shutdowns</title><link href="https://harshvermac5.github.io/learn/2026/linux-critical-thermal-shutdowns/" rel="alternate" type="text/html" title="Diagnosing Linux Critical Thermal Shutdowns"/><published>2026-09-01T23:53:02+00:00</published><updated>2026-09-01T23:53:02+00:00</updated><id>https://harshvermac5.github.io/learn/2026/linux-critical-thermal-shutdowns</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/linux-critical-thermal-shutdowns/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>A Linux host that abruptly disappears may resemble an application crash, watchdog reset, or power failure. When the kernel records <strong><code class="language-plaintext highlighter-rouge">critical temperature reached</code></strong> immediately before shutdown, however, the causal mechanism is different: a registered thermal zone crossed its <strong>critical trip point</strong>, and the kernel deliberately entered a hardware-protection path.</p> <p>The objective is to prove that sequence, distinguish mitigation from emergency action, and determine whether the trigger represents genuine silicon overheating or invalid thermal telemetry. This distinction matters because cooling changes can correct environmental heat soak, while an erratic on-die sensor, sensor interface, calibration value, or board-level thermal path usually requires hardware remediation.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Linux thermal management, embedded hardware monitoring, and evidence-based shutdown diagnostics.</strong></p> <p>The central principle is that temperature is not a single system-wide value. Each sensor observes a particular physical location through its own driver and calibration path. A processor junction sensor can therefore report a materially different temperature from a board-mounted monitor without either reading being inherently impossible.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>The Linux thermal subsystem models thermal control as three related objects: <strong>thermal zones</strong>, <strong>trip points</strong>, and <strong>cooling devices</strong>. A zone represents a monitored region such as a CPU cluster or system-on-chip. Trip points define temperatures at which actions become necessary. Cooling devices expose controllable states such as fan levels, processor frequency limits, or idle injection.</p> <p>Trip types express increasing levels of intervention:</p> <ul> <li><strong>Active</strong> trips request physical cooling, commonly a higher fan state.</li> <li><strong>Passive</strong> trips reduce heat generation through mechanisms such as frequency or power throttling.</li> <li><strong>Hot</strong> trips notify a platform driver so firmware-specific handling can occur.</li> <li><strong>Critical</strong> trips protect hardware by initiating shutdown or, where explicitly configured, reboot.</li> </ul> <p>Thermal governors such as <strong><code class="language-plaintext highlighter-rouge">step_wise</code></strong> and <strong><code class="language-plaintext highlighter-rouge">power_allocator</code></strong> select cooling-device states around controllable trips. The critical shutdown path is not merely another governor decision. Once the critical threshold is crossed, the thermal core invokes the platform’s emergency action. Linux first attempts an orderly shutdown; supported configurations may force power-off after a delay and ultimately invoke an emergency restart if orderly handling fails.</p> <p>On Device Tree platforms, firmware description links a sensor to a thermal zone, specifies polling intervals or interrupt-driven monitoring, defines trip temperature and hysteresis, and maps non-critical trips to cooling devices. <strong>Hysteresis</strong> prevents repeated state changes near a threshold by retaining mitigation until temperature falls below the trip point minus the hysteresis value.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>The failure sequence begins when a sensor driver registers a temperature source with the thermal core. The platform associates that source with a zone such as <strong><code class="language-plaintext highlighter-rouge">soc-thermal</code></strong> and exposes it below <strong><code class="language-plaintext highlighter-rouge">/sys/class/thermal/thermal_zoneN</code></strong>. Temperatures and trip points are normally represented in millidegrees Celsius.</p> <p>The sensor either raises an interrupt when a programmed window is crossed or the thermal framework polls it. Polling intervals must be shorter than the time required for the hardware’s maximum temperature slope to cross multiple trip boundaries. An overly long interval can allow the system to move from a manageable state to a critical state between samples.</p> <p>At an active or passive trip, the selected governor evaluates the zone and requests a cooling state. A frequency-cooling device might progress from unrestricted operation to successively lower operating points. If cooling is effective, temperature falls through the hysteresis boundary and mitigation relaxes.</p> <p>If temperature continues upward and reaches a critical trip, the thermal core emits a kernel event and invokes the configured critical action. Services may then log connection refusals, reporting failures, or incomplete writes because shutdown has already started. Those later errors are consequences, not competing root causes.</p> <p>After reboot, real-time sensor output may look normal because the device cooled while powered off or operated at reduced load. A single healthy sample therefore cannot invalidate a preceding critical event. The investigation must correlate monotonic event order, persisted logs, sensor identities, workload, ambient conditions, and recurrence across rotated logs.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>This mechanism is governed primarily by Linux platform interfaces rather than an IETF RFC or IEEE networking standard. The authoritative implementation references are the <a href="https://docs.kernel.org/7.1/driver-api/thermal/sysfs-api.html">Linux Kernel 7.1 generic thermal sysfs documentation</a>, derived from the platform-independent thermal interface introduced in 2008, and the <a href="https://www.kernel.org/doc/Documentation/devicetree/bindings/thermal/thermal-zones.yaml">Devicetree thermal-zones schema</a>, standardized as a YAML binding in 2020.</p> <p>The <a href="https://docs.kernel.org/hwmon/sysfs-interface.html">Linux hardware-monitoring sysfs interface</a> defines attributes such as <strong><code class="language-plaintext highlighter-rouge">tempN_input</code></strong>, <strong><code class="language-plaintext highlighter-rouge">tempN_max</code></strong>, <strong><code class="language-plaintext highlighter-rouge">tempN_crit</code></strong>, and <strong><code class="language-plaintext highlighter-rouge">fanN_input</code></strong>. These values must be interpreted according to the sensor’s physical placement and label; similarly named channels are not automatically measurements of the same thermal mass.</p> <p>For facility context, <strong>ASHRAE TC 9.9, Thermal Guidelines for Data Processing Environments, Fifth Edition, 2021</strong>, distinguishes recommended inlet conditions from wider allowable operating limits. Its environmental classes are useful for room and inlet design, but ambient compliance does not prove that a device has adequate local airflow or a healthy internal thermal path.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>Start by inventorying every zone, its policy, and its trips rather than reading only <strong><code class="language-plaintext highlighter-rouge">thermal_zone0</code></strong>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for </span>zone <span class="k">in</span> /sys/class/thermal/thermal_zone<span class="k">*</span><span class="p">;</span> <span class="k">do
  </span><span class="nb">printf</span> <span class="s1">'\n%s type=%s temp=%s policy=%s\n'</span> <span class="se">\</span>
    <span class="s2">"</span><span class="nv">$zone</span><span class="s2">"</span> <span class="se">\</span>
    <span class="s2">"</span><span class="si">$(</span><span class="nb">cat</span> <span class="s2">"</span><span class="nv">$zone</span><span class="s2">/type"</span><span class="si">)</span><span class="s2">"</span> <span class="se">\</span>
    <span class="s2">"</span><span class="si">$(</span><span class="nb">cat</span> <span class="s2">"</span><span class="nv">$zone</span><span class="s2">/temp"</span><span class="si">)</span><span class="s2">"</span> <span class="se">\</span>
    <span class="s2">"</span><span class="si">$(</span><span class="nb">cat</span> <span class="s2">"</span><span class="nv">$zone</span><span class="s2">/policy"</span> 2&gt;/dev/null <span class="o">||</span> <span class="nb">printf </span>unknown<span class="si">)</span><span class="s2">"</span>
  <span class="nb">grep</span> <span class="nt">-H</span> <span class="nb">.</span> <span class="s2">"</span><span class="nv">$zone</span><span class="s2">"</span>/trip_point_<span class="k">*</span> 2&gt;/dev/null
<span class="k">done</span>
</code></pre></div></div> <p>A standards-aligned embedded platform may describe mitigation and protection like this:</p> <pre><code class="language-dts">cpu_thermal: cpu-thermal {
    polling-delay-passive = &lt;250&gt;;
    polling-delay = &lt;1000&gt;;
    thermal-sensors = &lt;&amp;tsens 0&gt;;

    trips {
        cpu_passive: cpu-passive {
            temperature = &lt;90000&gt;;
            hysteresis = &lt;2000&gt;;
            type = "passive";
        };

        cpu_critical: cpu-critical {
            temperature = &lt;105000&gt;;
            hysteresis = &lt;1000&gt;;
            type = "critical";
        };
    };
};
</code></pre> <p>The following event is direct evidence of the protection path, not merely a high-temperature observation:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kernel: thermal thermal_zone0: critical temperature reached (106 C), shutting down
</code></pre></div></div> <p>Correlate it against boot boundaries and related services:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>journalctl <span class="nt">-k</span> <span class="nt">--list-boots</span>
journalctl <span class="nt">-k</span> <span class="nt">-b</span> <span class="nt">-1</span> | <span class="nb">grep</span> <span class="nt">-Ei</span> <span class="s1">'thermal|critical|shutdown|watchdog'</span>
journalctl <span class="nt">--since</span> <span class="s1">'2026-08-31 07:40:30'</span> <span class="nt">--until</span> <span class="s1">'2026-08-31 07:42:00'</span>
sensors <span class="nt">-u</span>
</code></pre></div></div> <p>Suppose an on-die zone reaches <strong>106 °C</strong> while a board monitor reports <strong>64 °C</strong>. The 42 °C delta supports two hypotheses: localized junction heating across a real thermal resistance, or an erroneous sensor path. It does not select between them. A smooth rise tied to workload and improved by airflow supports genuine heating. Instantaneous spikes, impossible slew rates, repeated identical ceiling values, or failures at low load support telemetry or hardware instability.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Causal ordering outranks log volume.</strong> The first decisive kernel event explains the service failures that follow during shutdown.</li> <li><strong>Sensor scope matters.</strong> Junction, package, board, inlet, and external-monitor temperatures are not interchangeable.</li> <li><strong>A normal post-boot reading is weak counterevidence.</strong> Power-off changes both thermal load and elapsed cooling time.</li> <li><strong>Repeated historical critical trips establish recurrence.</strong> Rotated logs help separate a persistent mechanism from a one-time environmental excursion.</li> <li><strong>Low fan RPM is contextual, not self-explanatory.</strong> Zero RPM can indicate fan-stop policy, passive cooling, missing fan hardware, or failure; validate the platform design and requested cooling state.</li> <li><strong>Reboot is a risky critical action.</strong> Repeated rebooting can re-energize thermally stressed hardware before it cools, which is why shutdown is the preferred Device Tree default.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Place equipment where intake and exhaust paths remain unobstructed; avoid enclosed cabinets, stacked heat sources, and recirculating exhaust.</li> <li>Monitor every relevant zone and retain kernel logs across boots. Alert below the critical threshold so operators observe throttling before protection activates.</li> <li>Record ambient or inlet temperature alongside junction telemetry. Cross-sensor trends are more useful than isolated absolute values.</li> <li>Validate polling intervals against the platform’s worst-case temperature rise and use sensor interrupts where supported.</li> <li>After an airflow change, reproduce comparable workload and verify that no new <strong><code class="language-plaintext highlighter-rouge">critical temperature reached</code></strong> event appears.</li> <li>Escalate for hardware repair when critical trips persist under verified ambient conditions, readings show implausible discontinuities, or sensor disagreement cannot be explained by load and thermal design.</li> </ul> <p>The governing diagnostic rule is simple: trust the kernel event as proof of why the system shut down, but do not confuse proof of the shutdown mechanism with proof of the underlying thermal cause. That second conclusion requires correlated telemetry, physical inspection, controlled environmental testing, and recurrence analysis.</p>]]></content><author><name></name></author><category term="Infrastructure"/><category term="linux"/><category term="thermal-management"/><category term="hardware-diagnostics"/><summary type="html"><![CDATA[A practical framework for distinguishing genuine silicon overheating from faulty thermal telemetry when Linux initiates a protective shutdown.]]></summary></entry><entry><title type="html">WireGuard Site-to-Site Routing with Selective Split Tunneling</title><link href="https://harshvermac5.github.io/learn/2026/wireguard-site-to-site-split-tunneling/" rel="alternate" type="text/html" title="WireGuard Site-to-Site Routing with Selective Split Tunneling"/><published>2026-09-01T23:24:00+00:00</published><updated>2026-09-01T23:24:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/wireguard-site-to-site-split-tunneling</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/wireguard-site-to-site-split-tunneling/"><![CDATA[<p>A selective WireGuard site-to-site VPN is best understood as a <strong>Layer-3 encrypted overlay controlled by normal routing policy</strong>. WireGuard encrypts and authenticates packets between peers, but it does not independently decide which application traffic should enter the tunnel.</p> <h2 id="separate-the-underlay-from-the-overlay">Separate the Underlay from the Overlay</h2> <p>Every WireGuard deployment has two logical paths.</p> <p>The <strong>underlay</strong> carries encrypted UDP packets between the public or translated addresses of the WireGuard peers.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Router A public IP
        |
     Internet
        |
Router B public/NAT address
</code></pre></div></div> <p>The <strong>overlay</strong> carries the original Layer-3 traffic after encryption and decapsulation.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LAN A ---- Router A === WireGuard === Router B ---- LAN B
</code></pre></div></div> <p>The remote WireGuard endpoint must remain reachable through the underlay. Routing the peer’s public endpoint back into the tunnel creates recursive routing: the tunnel would need to exist before the endpoint required to establish it could be reached.</p> <h2 id="route-selection-happens-before-wireguard-peer-selection">Route Selection Happens Before WireGuard Peer Selection</h2> <p>Assume only two systems need to communicate:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Site A backup server: 10.10.10.20
Site B storage server: 10.20.20.30
</code></pre></div></div> <p>A destination-specific route can steer all traffic for the remote server into WireGuard:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>10.20.20.30/32 -&gt; wg0
</code></pre></div></div> <p>If only one local source should use the tunnel, <strong>Policy-Based Routing</strong> can make the decision more specific:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if src == 10.10.10.20
and dst == 10.20.20.30:
    use wg0
else:
    use normal routing
</code></pre></div></div> <p>The forwarding plane therefore decides whether a packet reaches the WireGuard interface. WireGuard then decides which peer is allowed to carry that packet.</p> <h2 id="understand-what-allowedips-actually-does">Understand What AllowedIPs Actually Does</h2> <p>WireGuard associates IP prefixes with peer public keys through <strong><code class="language-plaintext highlighter-rouge">AllowedIPs</code></strong>.</p> <p>For outbound traffic:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Destination IP
    -&gt; AllowedIPs lookup
    -&gt; Peer public key
    -&gt; Encryption
</code></pre></div></div> <p>For inbound traffic:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Authenticated peer
    -&gt; Decrypt packet
    -&gt; Inspect source IP
    -&gt; Verify source is inside that peer's AllowedIPs
</code></pre></div></div> <p>For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Peer B
AllowedIPs = 10.20.20.0/24
</code></pre></div></div> <p>A decrypted packet from Peer B with source <code class="language-plaintext highlighter-rouge">10.20.20.30</code> is valid. A packet from the same peer claiming source <code class="language-plaintext highlighter-rouge">10.50.50.100</code> is rejected unless that prefix is also assigned to Peer B.</p> <p>This means <strong><code class="language-plaintext highlighter-rouge">AllowedIPs</code> is both a peer-selection mechanism and a source-address authorization mechanism</strong>.</p> <p>Do not treat it as identical to the operating system routing table. Some management tools automatically create routes from <code class="language-plaintext highlighter-rouge">AllowedIPs</code>, but the logical functions remain separate:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Routing/PBR
    -&gt; select wg0
    -&gt; AllowedIPs
    -&gt; select cryptographic peer
</code></pre></div></div> <h2 id="keep-split-tunnel-scope-narrow">Keep Split-Tunnel Scope Narrow</h2> <p>The route scope determines whether the design is selective or broad.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>10.20.20.30/32 -&gt; tunnel
</code></pre></div></div> <p>This sends only one destination through WireGuard.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>10.20.20.0/24 -&gt; tunnel
</code></pre></div></div> <p>This sends the entire remote subnet through the tunnel.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0.0.0.0/0 -&gt; tunnel
</code></pre></div></div> <p>This can turn the peer into a default IPv4 egress path.</p> <p>For tightly controlled application connectivity, a <code class="language-plaintext highlighter-rouge">/32</code> route provides the smallest forwarding scope and reduces accidental VPN transit.</p> <h2 id="return-path-routing-is-mandatory">Return-Path Routing Is Mandatory</h2> <p>A successful handshake proves that the WireGuard peers can authenticate and exchange tunnel packets. It does not prove that hosts behind those peers can communicate.</p> <p>If Site A routes <code class="language-plaintext highlighter-rouge">10.20.20.30</code> through WireGuard but Site B has no route back to <code class="language-plaintext highlighter-rouge">10.10.10.20</code>, the request may arrive while the response leaves through another interface.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Forward path: Site A -&gt; WireGuard -&gt; Site B
Return path:  Site B -&gt; default route -&gt; WAN
</code></pre></div></div> <p>That asymmetry commonly breaks connectivity because of stateful firewalls, NAT, anti-spoofing checks, or private RFC1918 addressing.</p> <p>Verify both directions:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Site A: 10.20.20.30/32 -&gt; WireGuard
Site B: 10.10.10.20/32 -&gt; WireGuard
</code></pre></div></div> <h2 id="treat-routing-and-firewall-policy-as-separate-controls">Treat Routing and Firewall Policy as Separate Controls</h2> <p>Routing answers <strong>where the packet should go</strong>. Firewall policy answers <strong>whether the packet is allowed to go there</strong>.</p> <p>A secure selective design should align three scopes:</p> <ul> <li><strong>Cryptographic scope:</strong> which addresses belong to the remote peer.</li> <li><strong>Routing scope:</strong> which traffic is sent into the tunnel.</li> <li><strong>Security scope:</strong> which sources, destinations, and applications are permitted.</li> </ul> <p>For a backup workflow, avoid allowing the complete LAN-to-LAN path when only two systems need access.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>source      = 10.10.10.20
destination = 10.20.20.30
protocol    = required backup services
action      = allow
</code></pre></div></div> <h2 id="account-for-nat-and-idle-peers">Account for NAT and Idle Peers</h2> <p>WireGuard runs over UDP. A peer behind NAT may lose its translation or firewall state during long idle periods.</p> <p><strong><code class="language-plaintext highlighter-rouge">PersistentKeepalive</code></strong> can maintain that state:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PersistentKeepalive = 25
</code></pre></div></div> <p>This is primarily a NAT traversal mechanism, not a general tunnel-health monitor. WireGuard is intentionally quiet when no traffic needs to be sent.</p> <h2 id="check-mtu-when-large-transfers-fail">Check MTU When Large Transfers Fail</h2> <p>WireGuard adds roughly 60 bytes of overhead over IPv4 transport and 80 bytes over IPv6.</p> <p>If the tunnel MTU is too large, symptoms can include:</p> <ul> <li>TCP sessions that establish but stall during larger transfers.</li> <li>Fragmentation or ICMP dependency.</li> <li>Broken Path MTU Discovery.</li> <li>Backup jobs that fail only under sustained throughput.</li> </ul> <p>If small pings work but application transfers fail, MTU and TCP MSS behavior should be part of the investigation.</p> <h2 id="troubleshooting-order">Troubleshooting Order</h2> <p>When a selective WireGuard site-to-site path fails, check the forwarding chain in order:</p> <ul> <li>Confirm the remote public endpoint is reachable through the WAN underlay.</li> <li>Confirm a recent WireGuard handshake exists when traffic is generated.</li> <li>Verify the local route or PBR rule sends the intended flow to the WireGuard interface.</li> <li>Verify the destination matches the correct peer’s <code class="language-plaintext highlighter-rouge">AllowedIPs</code>.</li> <li>Verify the remote side accepts the decrypted source address under its own <code class="language-plaintext highlighter-rouge">AllowedIPs</code> configuration.</li> <li>Confirm firewall policy allows the application traffic in both directions.</li> <li>Confirm the return route points back through WireGuard.</li> <li>Check for overlapping LAN prefixes.</li> <li>Investigate MTU if only large packets or sustained transfers fail.</li> </ul> <p>The key design principle is simple: <strong>WireGuard encryption, route selection, and firewall policy should describe the same intended traffic scope</strong>. When those three layers agree, selective site-to-site connectivity becomes predictable and much easier to troubleshoot.</p>]]></content><author><name></name></author><category term="Networking"/><category term="wireguard"/><category term="vpn"/><category term="routing"/><category term="pbr"/><category term="networking"/><summary type="html"><![CDATA[How WireGuard cryptokey routing, policy-based forwarding, firewall rules, NAT, and return paths combine in a selective site-to-site VPN design.]]></summary></entry><entry><title type="html">Building and Transferring Docker Images to Air-Gapped Debian Systems</title><link href="https://harshvermac5.github.io/learn/2026/docker-images-containers-air-gapped-debian/" rel="alternate" type="text/html" title="Building and Transferring Docker Images to Air-Gapped Debian Systems"/><published>2026-09-01T21:55:00+00:00</published><updated>2026-09-01T21:55:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/docker-images-containers-air-gapped-debian</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/docker-images-containers-air-gapped-debian/"><![CDATA[<p>Docker works well in isolated environments because applications and their dependencies can be packaged on an internet-connected system, transferred as files, and loaded on a completely offline host.</p> <p>The important part is understanding what should be transferred. In most cases, <strong>transfer the Docker image, not the container</strong>.</p> <p>A Docker image is a static, read-only template containing the application filesystem, configuration, metadata, and image layers. A Docker container is a runtime instance created from that image.</p> <h2 id="image-vs-container">Image vs Container</h2> <p>Think of an <strong>image</strong> as an application package and a <strong>container</strong> as a running copy of that package.</p> <p>An image:</p> <ul> <li>Is read-only.</li> <li>Can be versioned and tagged.</li> <li>Can create multiple containers.</li> <li>Preserves image layers and Docker metadata.</li> <li>Is normally built from a Dockerfile.</li> <li>Can be transferred with <code class="language-plaintext highlighter-rouge">docker save</code> and restored with <code class="language-plaintext highlighter-rouge">docker load</code>.</li> </ul> <p>A container:</p> <ul> <li>Is created from an image.</li> <li>Has a writable filesystem layer.</li> <li>Represents runtime state.</li> <li>Can be started, stopped, modified, or deleted.</li> <li>Can be exported as a flattened filesystem using <code class="language-plaintext highlighter-rouge">docker export</code>.</li> </ul> <p>For air-gapped deployments, <strong><code class="language-plaintext highlighter-rouge">docker save</code> and <code class="language-plaintext highlighter-rouge">docker load</code> should normally be used</strong>.</p> <p>Use <code class="language-plaintext highlighter-rouge">docker export</code> and <code class="language-plaintext highlighter-rouge">docker import</code> when you specifically need the filesystem state of an existing container and do not care about preserving its original image layers and history.</p> <h2 id="choosing-the-right-method">Choosing the Right Method</h2> <p>Use <strong>Dockerfile → build → save → load</strong> for production deployments, repeatable lab environments, and normal air-gapped application distribution.</p> <p>Use <strong>docker commit → save → load</strong> when you need to capture useful container changes quickly but cannot immediately reproduce them in a Dockerfile.</p> <p>Use <strong>export → import</strong> when only the resulting container filesystem matters and losing layers, history, and some Docker metadata is acceptable.</p> <p>For long-term infrastructure management, prefer Dockerfiles. They document exactly how an image was created and make rebuilding, auditing, patching, and version control significantly easier.</p> <h2 id="building-an-image-on-an-internet-connected-debian-system">Building an Image on an Internet-Connected Debian System</h2> <p>First, install Docker Engine on the connected host. Follow the official Docker installation guide for Debian: https://docs.docker.com/engine/install/debian/</p> <p>Verify installation:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl status docker
<span class="nb">sudo </span>docker run hello-world
</code></pre></div></div> <p>A Dockerfile is the preferred way to create a repeatable image. For example:</p> <div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> debian:13-slim</span>

<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> <span class="se">\
</span>    apt-get <span class="nb">install</span> <span class="nt">-y</span> curl iputils-ping <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>

<span class="k">CMD</span><span class="s"> ["/bin/bash"]</span>
</code></pre></div></div> <p>Build it:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> offline-tools:1.0 <span class="nb">.</span>
</code></pre></div></div> <p>Verify the image:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker images
docker run <span class="nt">--rm</span> <span class="nt">-it</span> offline-tools:1.0
</code></pre></div></div> <p>Everything the container will require from package repositories should be installed <strong>during the image build while internet access is available</strong>. Do not design an offline container around running <code class="language-plaintext highlighter-rouge">apt update</code> after deployment.</p> <h2 id="saving-the-docker-image">Saving the Docker Image</h2> <p>Export the image into a portable tar archive:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker save <span class="nt">-o</span> offline-tools-1.0.tar offline-tools:1.0
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">docker save</code> preserves the image’s layers, tags, configuration, and parent-layer information that Docker needs.</p> <p>If you made changes after running it interactively, commit the image first:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker commit &lt;container_id&gt; offline-tools:2.0
docker save <span class="nt">-o</span> offline-tools-2.0.tar offline-tools:2.0
</code></pre></div></div> <p>For multiple images:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker save <span class="nt">-o</span> application-stack.tar <span class="se">\</span>
  frontend:1.0 <span class="se">\</span>
  backend:1.0 <span class="se">\</span>
  postgres:17
</code></pre></div></div> <p>Generate a checksum before transferring:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sha256sum </span>offline-tools-1.0.tar <span class="o">&gt;</span> offline-tools-1.0.tar.sha256
</code></pre></div></div> <p>Transfer both files using approved removable media or your organization’s air-gap transfer process.</p> <h2 id="installing-docker-on-the-air-gapped-debian-system">Installing Docker on the Air-Gapped Debian System</h2> <p>The offline host needs Docker Engine before it can load the transferred image. Download Docker <code class="language-plaintext highlighter-rouge">.deb</code> packages on an internet-connected system and transfer them to the air-gapped host.</p> <p>Follow the official Docker documentation for the <strong>manual package installation method</strong>: https://docs.docker.com/engine/install/debian/#install-from-a-package</p> <p>In summary:</p> <ul> <li>Download the required <code class="language-plaintext highlighter-rouge">.deb</code> files from <code class="language-plaintext highlighter-rouge">https://download.docker.com/linux/debian/dists/</code> for your Debian version and architecture.</li> <li>Transfer the packages offline.</li> <li>Install them using <code class="language-plaintext highlighter-rouge">sudo dpkg -i</code>.</li> <li>Verify installation with <code class="language-plaintext highlighter-rouge">sudo systemctl status docker</code>.</li> </ul> <p>Because the machine is offline, do <strong>not</strong> use <code class="language-plaintext highlighter-rouge">docker run hello-world</code> unless the <code class="language-plaintext highlighter-rouge">hello-world</code> image has also been transferred.</p> <h2 id="loading-the-image-offline">Loading the Image Offline</h2> <p>First, verify that the archive was not corrupted during transfer:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sha256sum</span> <span class="nt">-c</span> offline-tools-1.0.tar.sha256
</code></pre></div></div> <p>A successful result should report:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>offline-tools-1.0.tar: OK
</code></pre></div></div> <p>Load the image:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker load <span class="nt">-i</span> offline-tools-1.0.tar
</code></pre></div></div> <p>Verify it:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker images
</code></pre></div></div> <p>Run the container:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker run <span class="nt">--rm</span> <span class="nt">-it</span> offline-tools:1.0
</code></pre></div></div> <p>At this point, neither an internet connection nor Docker Hub is required.</p> <h2 id="exporting-a-container-instead">Exporting a Container Instead</h2> <p>Sometimes the important state exists inside a modified container rather than its original image.</p> <p>Find the container:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker ps <span class="nt">-a</span>
</code></pre></div></div> <p>Export its filesystem:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker <span class="nb">export</span> &lt;container_id&gt; <span class="nt">-o</span> container.tar
</code></pre></div></div> <p>Transfer the archive and import it on the offline system:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker import container.tar imported-container:1.0
</code></pre></div></div> <p>Verify it:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker images
</code></pre></div></div> <p>This is different from <code class="language-plaintext highlighter-rouge">docker save</code>.</p> <p><strong><code class="language-plaintext highlighter-rouge">docker save</code> + <code class="language-plaintext highlighter-rouge">docker load</code></strong> should be used for normal image distribution.</p> <p><strong><code class="language-plaintext highlighter-rouge">docker export</code> + <code class="language-plaintext highlighter-rouge">docker import</code></strong> creates a flattened filesystem snapshot. Image layer history is lost, and data stored in Docker volumes is not included.</p> <p>If you modified a container interactively but want to preserve it as an image before transferring it, another option is:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker commit &lt;container_id&gt; custom-image:1.0
docker save <span class="nt">-o</span> custom-image-1.0.tar custom-image:1.0
</code></pre></div></div> <p>This works, but building the changes into a Dockerfile is preferable because the resulting environment is reproducible.</p> <h2 id="key-takeaways">Key Takeaways</h2> <p>For most air-gapped deployments, the workflow should be:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Internet-connected Debian host
        |
        | docker build
        v
Docker image
        |
        | docker save / commit and save
        v
image.tar + SHA256 checksum
        |
        | approved offline transfer
        v
Air-gapped Debian host
        |
        | docker load
        v
Local Docker image
        |
        | docker run
        v
Running container
</code></pre></div></div> <p>Build everything possible while connected to the internet, transfer immutable images instead of relying on runtime downloads, verify transferred files with checksums, and keep the Dockerfile as the authoritative definition of the environment.</p> <p>That approach makes an air-gapped Docker deployment reproducible instead of turning the offline container into a manually maintained system.</p>]]></content><author><name></name></author><category term="learning"/><category term="docker"/><category term="debian"/><category term="containers"/><category term="airgap"/><category term="linux"/><summary type="html"><![CDATA[A practical Debian-focused guide to Docker images, containers, offline Docker installation, and transferring workloads into air-gapped environments.]]></summary></entry><entry><title type="html">Configuring Git and GitHub SSH Access Safely</title><link href="https://harshvermac5.github.io/learn/2026/configuring-git-github-ssh-access-safely/" rel="alternate" type="text/html" title="Configuring Git and GitHub SSH Access Safely"/><published>2026-09-01T20:30:00+00:00</published><updated>2026-09-01T20:30:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/configuring-git-github-ssh-access-safely</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/configuring-git-github-ssh-access-safely/"><![CDATA[<p>Git is straightforward once the relationship between your local repository, SSH authentication, and GitHub remotes is clear. Most setup problems come from mixing these three areas together or using commands without understanding their impact.</p> <p>This guide covers a common GitHub workflow while masking usernames, email addresses, and repository names that should not be copied directly between environments.</p> <h2 id="configure-your-git-identity">Configure Your Git Identity</h2> <p>Git records an author name and email address inside each commit. Configure these globally if you normally use the same identity across repositories.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> user.name <span class="s2">"YOUR_NAME"</span>
git config <span class="nt">--global</span> user.email <span class="s2">"YOUR_EMAIL@example.com"</span>
</code></pre></div></div> <p>Verify the configuration:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> user.name
git config <span class="nt">--global</span> user.email
</code></pre></div></div> <p>These values are commit metadata. They are separate from GitHub SSH authentication.</p> <p>If you use different identities for different repositories, configure them locally instead:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config user.name <span class="s2">"YOUR_NAME"</span>
git config user.email <span class="s2">"YOUR_EMAIL@example.com"</span>
</code></pre></div></div> <p>Local configuration overrides the global value for that repository.</p> <h2 id="generate-an-ssh-key">Generate an SSH Key</h2> <p>SSH authentication allows Git to communicate with GitHub without repeatedly entering credentials.</p> <p>Generate an Ed25519 key:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-t</span> ed25519 <span class="nt">-C</span> <span class="s2">"YOUR_EMAIL@example.com"</span>
</code></pre></div></div> <p>Unless you already have a key-management strategy, the default location is normally sufficient:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/.ssh/id_ed25519
</code></pre></div></div> <p>Two files are created:</p> <ul> <li><code class="language-plaintext highlighter-rouge">id_ed25519</code> is the private key.</li> <li><code class="language-plaintext highlighter-rouge">id_ed25519.pub</code> is the public key.</li> </ul> <p><strong>Never share the private key.</strong></p> <p>Display only the public key:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> ~/.ssh/id_ed25519.pub
</code></pre></div></div> <p>Copy that value into the <a href="https://github.com/settings/ssh/new">SSH key section of your GitHub account</a>.</p> <h2 id="test-github-ssh-authentication">Test GitHub SSH Authentication</h2> <p>After adding the public key to GitHub, test the connection:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-T</span> git@github.com
</code></pre></div></div> <p>On the first connection, SSH may ask whether you trust GitHub’s host key.</p> <p>A successful authentication confirms that:</p> <ul> <li>SSH can locate your private key.</li> <li>GitHub recognizes the associated public key.</li> <li>Your local machine can authenticate to GitHub.</li> </ul> <p>This does not automatically mean you have access to every repository. Repository permissions are still evaluated separately.</p> <h2 id="understand-git-remotes">Understand Git Remotes</h2> <p>A Git remote maps a local repository to another repository, typically on GitHub.</p> <p>Check the current configuration:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote <span class="nt">-v</span>
</code></pre></div></div> <p>A typical SSH remote looks like this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>origin  git@github.com:YOUR_USERNAME/YOUR_REPOSITORY.git
</code></pre></div></div> <p>If <code class="language-plaintext highlighter-rouge">origin</code> already exists and you want to change it, use:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote set-url origin git@github.com:YOUR_USERNAME/YOUR_REPOSITORY.git
</code></pre></div></div> <p>If the repository does not have an <code class="language-plaintext highlighter-rouge">origin</code> remote yet, use:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPOSITORY.git
</code></pre></div></div> <p>Do not run both commands blindly.</p> <p><strong><code class="language-plaintext highlighter-rouge">git remote add</code> creates a new remote. <code class="language-plaintext highlighter-rouge">git remote set-url</code> modifies an existing one.</strong></p> <p>Verify the result:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote <span class="nt">-v</span>
</code></pre></div></div> <p>This simple check prevents accidentally pushing code to the wrong repository.</p> <h2 id="commit-and-push-changes">Commit and Push Changes</h2> <p>After modifying files, stage the specific files you want included:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add _config.yml .github/workflows/deploy.yml
</code></pre></div></div> <p>Review what Git will commit:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div> <p>Create the commit:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">-m</span> <span class="s2">"Fix GitHub Pages deployment"</span>
</code></pre></div></div> <p>Push it:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push origin main
</code></pre></div></div> <p>For a new remote branch, it is useful to configure the upstream relationship:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push <span class="nt">-u</span> origin main
</code></pre></div></div> <p>After that, future pushes can usually be performed with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push
</code></pre></div></div> <h2 id="be-careful-with-force-pushes">Be Careful With Force Pushes</h2> <p>A command such as:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push origin main <span class="nt">--force</span>
</code></pre></div></div> <p>should not be part of a normal deployment workflow.</p> <p>A force push tells GitHub to replace the remote branch history with the local version, even when the histories conflict.</p> <p>This can overwrite commits created by another workstation or contributor.</p> <p>If rewriting history is intentional, a safer option is:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push origin main <span class="nt">--force-with-lease</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">--force-with-lease</code> refuses the push when the remote branch has changed unexpectedly since your last fetch.</p> <p>Before any force push, inspect the repository state:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git log <span class="nt">--oneline</span> <span class="nt">--decorate</span> <span class="nt">-10</span>
git remote <span class="nt">-v</span>
</code></pre></div></div> <p>For shared repositories, fetching first is also useful:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
</code></pre></div></div> <h2 id="switching-between-github-repositories">Switching Between GitHub Repositories</h2> <p>If you reuse the same local working directory for another GitHub repository, changing <code class="language-plaintext highlighter-rouge">origin</code> changes where future pushes are sent.</p> <p>For example:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote set-url origin git@github.com:YOUR_USERNAME/ANOTHER_REPOSITORY.git
</code></pre></div></div> <p>Verify immediately:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote <span class="nt">-v</span>
</code></pre></div></div> <p>In most cases, however, maintaining separate local directories for separate repositories is safer and easier to reason about.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/projects/
├── website/
└── application/
</code></pre></div></div> <p>Each directory can keep its own <code class="language-plaintext highlighter-rouge">.git</code> metadata and <code class="language-plaintext highlighter-rouge">origin</code> remote.</p> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li>Configure Git identity separately from SSH authentication.</li> <li>Use Ed25519 SSH keys for GitHub authentication.</li> <li>Share only the <code class="language-plaintext highlighter-rouge">.pub</code> public key, never the private key.</li> <li>Check <code class="language-plaintext highlighter-rouge">git remote -v</code> before pushing.</li> <li>Use <code class="language-plaintext highlighter-rouge">git remote add</code> only when the remote does not exist.</li> <li>Use <code class="language-plaintext highlighter-rouge">git remote set-url</code> when changing an existing remote.</li> <li>Prefer normal pushes over force pushes.</li> <li>Use <code class="language-plaintext highlighter-rouge">--force-with-lease</code> instead of <code class="language-plaintext highlighter-rouge">--force</code> when history rewriting is genuinely required.</li> <li>Replace usernames, email addresses, and repository names with environment-specific values instead of embedding personal information in reusable documentation.</li> </ul>]]></content><author><name></name></author><category term="learning"/><category term="git"/><category term="github"/><category term="ssh"/><summary type="html"><![CDATA[A practical guide to configuring Git identity, setting up SSH authentication with GitHub, managing remotes, and pushing changes safely.]]></summary></entry><entry><title type="html">Welcome to the Learning Hub</title><link href="https://harshvermac5.github.io/learn/2026/welcome-to-the-learning-hub/" rel="alternate" type="text/html" title="Welcome to the Learning Hub"/><published>2026-08-30T03:30:00+00:00</published><updated>2026-08-30T03:30:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/welcome-to-the-learning-hub</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/welcome-to-the-learning-hub/"><![CDATA[<p>This Learning Hub is where I will document useful lessons from hands-on infrastructure work: diagnosing physical servers, understanding network behavior, hardening Linux systems, and automating repetitive operational tasks.</p> <h2 id="what-you-can-expect">What you can expect</h2> <ul> <li>Clear troubleshooting notes built around symptoms, evidence, and repeatable checks.</li> <li>Configuration walkthroughs for Linux networking, firewalls, VPNs, and monitoring.</li> <li>Practical automation patterns in Python, Bash, PowerShell, Ansible, and Terraform.</li> <li>Post-incident summaries that focus on what changed and how to prevent recurrence.</li> </ul> <p>The goal is simple: turn each useful lesson into a reference that is easier to find and apply the next time a similar problem appears.</p>]]></content><author><name></name></author><category term="learning"/><category term="linux"/><category term="networking"/><category term="servers"/><category term="automation"/><summary type="html"><![CDATA[A home for practical notes on infrastructure, networking, Linux, and automation.]]></summary></entry></feed>