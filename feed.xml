<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://harshvermac5.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://harshvermac5.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-06T21:36:16+00:00</updated><id>https://harshvermac5.github.io/feed.xml</id><title type="html">Harsh Verma</title><subtitle>Portfolio and learning hub of Harsh Verma, a server and network engineer focused on Linux, data center infrastructure, networking, and automation. </subtitle><entry><title type="html">Diagnosing MAC Authentication Bypass Failures on Embedded Ethernet Devices</title><link href="https://harshvermac5.github.io/learn/2026/mab-embedded-devices/" rel="alternate" type="text/html" title="Diagnosing MAC Authentication Bypass Failures on Embedded Ethernet Devices"/><published>2026-09-06T21:22:00+00:00</published><updated>2026-09-06T21:22:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/mab-embedded-devices</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/mab-embedded-devices/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>Embedded controllers, printers, industrial devices, cameras, and other fixed-function endpoints often lack an <strong>IEEE 802.1X supplicant</strong>. In networks that require authenticated access, these devices are commonly admitted through <strong>MAC Authentication Bypass (MAB)</strong>, where the access switch presents the endpoint MAC address to a backend AAA service.</p> <p>The difficult failure mode is not a simple RADIUS rejection. An endpoint can remain absent from the switch client table, never transition to an authorized state, and produce no obvious authentication event. The key diagnostic question is therefore: did the endpoint fail authorization at the RADIUS server, or did the authenticator never trigger MAB?</p> <p>This article explains how to separate those conditions by reasoning from Layer 2 learning, 802.1X port state, RADIUS signaling, and packet evidence.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Network Access Control: IEEE 802.1X authenticator behavior, MAC Authentication Bypass, RADIUS authorization, and Ethernet source-MAC learning.</strong></p> <p>The central principle is that <strong>MAB is not an IEEE 802.1X authentication method</strong>. IEEE 802.1X defines the controlled-port architecture and EAP-based relationship among supplicant, authenticator, and authentication server. MAB is an implementation technique for endpoints that do not participate in EAPOL. Trigger conditions, credential representation, retry behavior, and fallback timers can therefore vary between switching platforms.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>IEEE <strong>802.1X-2020</strong>, <em>Port-Based Network Access Control</em>, defines the architecture used to restrict access to a LAN port until authentication and authorization conditions are satisfied. The standard describes controlled and uncontrolled access paths associated with a Port Access Entity. The uncontrolled path permits authentication-control exchanges, while ordinary data traffic is governed by the controlled-port state.</p> <p>The IEEE summarizes the objective as: <a href="https://standards.ieee.org/ieee/802.1X/7345/">“Port-based network access control allows a network administrator to restrict the use of IEEE 802 LAN service access points.”</a></p> <p>In a normal wired 802.1X exchange, a supplicant communicates using <strong>EAP over LAN (EAPOL)</strong>. The authenticator relays authentication material toward a backend service, commonly RADIUS. RFC 3580 summarizes the model: <a href="https://www.rfc-editor.org/rfc/rfc3580.html">“IEEE 802.1X enables authenticated access to IEEE 802 media.”</a></p> <p>RADIUS is defined by <strong>RFC 2865, June 2000</strong>. The switch acts as the <strong>Network Access Server (NAS)</strong> and sends an <code class="language-plaintext highlighter-rouge">Access-Request</code>. The server returns an <code class="language-plaintext highlighter-rouge">Access-Accept</code>, <code class="language-plaintext highlighter-rouge">Access-Reject</code>, or, in EAP workflows, potentially an <code class="language-plaintext highlighter-rouge">Access-Challenge</code>.</p> <p>RFC 3579, published in <strong>September 2003</strong>, defines how EAP is carried inside RADIUS using <code class="language-plaintext highlighter-rouge">EAP-Message</code>. That path applies to real 802.1X supplicants. A MAB-only endpoint has no EAP conversation; the authenticator constructs an AAA request based on the Layer 2 identity it observes.</p> <p>This distinction matters because an embedded device may never send <code class="language-plaintext highlighter-rouge">EAPOL-Start</code>, never answer <code class="language-plaintext highlighter-rouge">EAP-Request/Identity</code>, and may transmit only a brief burst of ARP, DHCP, discovery, or proprietary Ethernet frames after link-up.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>When an Ethernet link comes up on an access-controlled port, the authenticator initializes the authorization state. If 802.1X is active, normal user data is not yet permitted through the controlled path.</p> <p>A standards-based supplicant can initiate EAPOL or respond to an identity request. A MAB-only endpoint is unaware that network access control exists, so the switch typically has to infer identity from ordinary Ethernet traffic.</p> <p>A common MAB flow is:</p> <ul> <li>Link transitions to <strong>up</strong> while the port remains unauthorized.</li> <li>The authenticator attempts 802.1X or waits through a supplicant-response interval.</li> <li>A frame arrives from an endpoint source MAC address.</li> <li>The switch associates that MAC with the physical port.</li> <li>The MAB process converts the MAC into an authentication identity.</li> <li>The switch sends a RADIUS <code class="language-plaintext highlighter-rouge">Access-Request</code> with NAS, port, and station attributes.</li> <li><code class="language-plaintext highlighter-rouge">Access-Accept</code> authorizes the session, optionally applying VLAN or policy attributes.</li> <li><code class="language-plaintext highlighter-rouge">Access-Reject</code> leaves the endpoint unauthorized.</li> </ul> <p>The crucial boundary is between <strong>frame arrival</strong> and <strong>Access-Request generation</strong>. If the switch never exposes the station identity to its access-control process, the RADIUS server has nothing to accept or reject.</p> <p>MAC representation creates another interoperability risk. RFC 3580 recommends that <code class="language-plaintext highlighter-rouge">Calling-Station-Id</code> contain the endpoint MAC in uppercase ASCII with hyphens, for example <code class="language-plaintext highlighter-rouge">00-10-A4-23-19-C0</code>. However, MAB <code class="language-plaintext highlighter-rouge">User-Name</code> and <code class="language-plaintext highlighter-rouge">User-Password</code> conventions are not standardized by IEEE 802.1X. Implementations may use <code class="language-plaintext highlighter-rouge">0010A42319C0</code>, <code class="language-plaintext highlighter-rouge">00-10-A4-23-19-C0</code>, or <code class="language-plaintext highlighter-rouge">00:10:A4:23:19:C0</code>. AAA policy must match the identity format actually sent by the NAS.</p> <p>Dynamic VLAN assignment adds a later decision point. RFC 3580 describes a common <code class="language-plaintext highlighter-rouge">Access-Accept</code> pattern:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Tunnel-Type = VLAN
Tunnel-Medium-Type = 802
Tunnel-Private-Group-ID = "2002"
</code></pre></div></div> <p>Authentication can therefore succeed while post-authentication policy still fails. That is distinct from a MAB transaction that never reaches RADIUS.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The core references are:</p> <ul> <li><strong>IEEE 802.1X-2020</strong>, published 2020: port-based network access control, Port Access Entities, controlled access, authentication, and authorization.</li> <li><strong>RFC 2865</strong>, published June 2000: base RADIUS authentication and authorization protocol.</li> <li><strong>RFC 3579</strong>, published September 2003: EAP carriage within RADIUS using <code class="language-plaintext highlighter-rouge">EAP-Message</code> and <code class="language-plaintext highlighter-rouge">Message-Authenticator</code>.</li> <li><strong>RFC 3580</strong>, published September 2003: RADIUS usage guidelines for IEEE 802.1X, including station identity and VLAN authorization.</li> <li><strong>RFC 7268</strong>, published July 2014: additional RADIUS attributes for IEEE 802 networks and an update to RFC 3580.</li> <li><strong>IEEE 802.1Q-2022</strong>: bridging and VLAN behavior relevant after authorization.</li> </ul> <p>RFC 3580 provides a useful identity convention: <a href="https://www.rfc-editor.org/rfc/rfc3580.html">“this attribute is used to store the Supplicant MAC address in ASCII format”</a>. That guidance applies to <code class="language-plaintext highlighter-rouge">Calling-Station-Id</code>; it should not be confused with implementation-specific MAB username formatting.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>The most decisive troubleshooting method is to observe the transaction at the AAA server while reproducing the endpoint connection.</p> <p>On a Linux RADIUS server:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>tcpdump <span class="nt">-ni</span> any <span class="s1">'udp port 1812'</span>
</code></pre></div></div> <p>To search a capture for a specific station identity:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tshark <span class="nt">-r</span> radius-test.pcapng <span class="se">\</span>
  <span class="nt">-Y</span> <span class="s1">'radius.Calling_Station_Id contains "00-0E-C6" || radius.User_Name contains "000EC6"'</span>
</code></pre></div></div> <p>The evidence should be classified by where the chain stops.</p> <p>If an <code class="language-plaintext highlighter-rouge">Access-Request</code> arrives and the server returns <code class="language-plaintext highlighter-rouge">Access-Reject</code>, the switch successfully triggered MAB. Investigation should move to <strong>AAA policy</strong>, including MAC normalization, NAS-IP restrictions, NAS groups, credential matching, and authorization rules.</p> <p>If an <code class="language-plaintext highlighter-rouge">Access-Request</code> receives <code class="language-plaintext highlighter-rouge">Access-Accept</code> but the endpoint remains blocked, the fault is downstream of authentication. Inspect controlled-port state, returned VLAN or ACL attributes, and session installation.</p> <p>If <strong>no Access-Request reaches RADIUS</strong>, the problem is earlier. Capture directly on or near the access link and determine whether the endpoint transmitted frames after link-up. A port mirror is valuable because traffic dropped at the ingress access-control boundary may never be visible elsewhere.</p> <p>For a MAB-only endpoint, useful trigger traffic includes:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ARP who-has 192.0.2.1 tell 192.0.2.50
DHCP Discover from 00:0e:c6:94:08:a4
IPv6 Neighbor Solicitation
Vendor discovery Ethernet frame
</code></pre></div></div> <p>If those frames are visible on the wire but the switch never creates a MAB session or RADIUS request, the evidence points toward the authenticator’s <strong>station-learning or MAB trigger path</strong>, not AAA policy.</p> <p>Link-state history also matters. A device that emits only one startup burst can be sensitive to link renegotiation, forwarding initialization, power-saving behavior, or repeated physical flaps. Those conditions do not prove a MAB defect, but they can explain why one authenticator sees the endpoint while another does not.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>No RADIUS packet is itself evidence.</strong> It moves the failure boundary from AAA policy toward the authenticator, Layer 2 learning, or endpoint transmit behavior.</li> <li><strong>A healthy RADIUS server does not prove every MAB identity will work.</strong> NAS-specific rules and MAC formatting can produce selective failures.</li> <li><strong>Successful MAB clients on the same switch are powerful controls.</strong> They reduce the likelihood of a global reachability, shared-secret, or VLAN fault.</li> <li><strong>Client-table absence can be meaningful.</strong> For a MAB implementation driven by source-MAC observation, no learned station may explain why authentication never starts.</li> <li><strong>MAB interoperability is weaker than 802.1X interoperability.</strong> The access-control architecture is standardized; the bypass trigger and credential encoding are not.</li> <li><strong>Comparative captures are most useful when aligned by event.</strong> Compare link-up, first endpoint frame, first RADIUS request, authorization result, and first permitted data frame.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <p>Use MAB as an exception path for devices that cannot support 802.1X, not as an equivalent security mechanism. A MAC address is observable and spoofable, so authorization should be narrow and combined with segmentation, least-privilege ACLs, and physically controlled access ports.</p> <p>Normalize MAC identities in AAA policy so that separator and case differences do not silently break authorization. Policy should still validate NAS identity and expected access location.</p> <p>During troubleshooting, instrument both sides of the decision boundary. Capture endpoint ingress traffic and RADIUS traffic simultaneously. This separates <strong>endpoint silence</strong>, <strong>authenticator trigger failure</strong>, <strong>RADIUS rejection</strong>, and <strong>post-authentication policy failure</strong> without relying on UI state alone.</p> <p>Treat MAB as a chain of dependent mechanisms: Ethernet link establishment, source-MAC observation, access-control state, RADIUS request generation, AAA policy evaluation, and post-authorization forwarding. Root-cause analysis becomes much faster once you identify the first stage for which expected evidence is missing.</p>]]></content><author><name></name></author><category term="Networking"/><category term="802.1X"/><category term="RADIUS"/><category term="MAB"/><category term="Ethernet"/><category term="NAC"/><summary type="html"><![CDATA[A protocol-level guide to understanding and troubleshooting MAB failures when embedded Ethernet devices lack an 802.1X supplicant.]]></summary></entry><entry><title type="html">Diagnosing IPsec IKE Failures with Packet-Flow Evidence</title><link href="https://harshvermac5.github.io/learn/2026/diagnosing-ipsec-ike-failures/" rel="alternate" type="text/html" title="Diagnosing IPsec IKE Failures with Packet-Flow Evidence"/><published>2026-09-06T20:38:00+00:00</published><updated>2026-09-06T20:38:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/diagnosing-ipsec-ike-failures</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/diagnosing-ipsec-ike-failures/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>An IPsec tunnel that never completes its initial key exchange is often diagnosed by repeatedly changing encryption, integrity, Diffie-Hellman, or Perfect Forward Secrecy settings. That approach is backwards when the initiator is transmitting IKE packets but receiving no response.</p> <p>The core principle is <strong>failure-domain ordering</strong>: prove bidirectional IKE transport before troubleshooting cryptographic agreement. If an initiator sends UDP packets to the peer and the peer never answers, there is not yet enough protocol evidence to blame an AES, SHA, authentication, or Diffie-Hellman mismatch. The fault domain is still reachability, filtering, NAT, peer availability, or responder configuration.</p> <p>A proposal mismatch requires the remote endpoint to receive and parse a request sufficiently to reject or negotiate it. Silence is a different failure class.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>IPsec control-plane troubleshooting, IKE state establishment, NAT traversal, and packet-path validation.</strong></p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>The modern architectural basis is <strong>RFC 4301, Security Architecture for the Internet Protocol</strong>, with IKEv2 defined by <strong>RFC 7296, Internet Key Exchange Protocol Version 2, October 2014</strong>. ESP is defined by <strong>RFC 4303</strong>. NAT traversal for older IKE deployments is described by <strong>RFC 3947</strong> and UDP encapsulation of ESP by <strong>RFC 3948</strong>, both published in January 2005.</p> <p>IKE establishes shared cryptographic state rather than directly carrying application traffic. An IKE Security Association protects subsequent control exchanges, while one or more <strong>Child SAs</strong> protect data with ESP or AH. This sequencing is central to diagnosis: data-plane parameters cannot be meaningfully debugged until the control-plane exchange progresses far enough to create the required state.</p> <p>RFC 7296 explicitly states that <a href="https://www.rfc-editor.org/rfc/rfc7296.html#section-3.1">“IKE messages use UDP ports 500 and/or 4500”</a>. UDP 500 is the conventional IKE listener, while UDP 4500 is used for NAT traversal and may also be used by IKEv2 from the beginning.</p> <p>IKEv1 was defined by <strong>RFC 2409 in November 1998</strong>, but <strong>RFC 9395, published in 2023</strong>, moved IKEv1 to Historic status. The current guidance is unambiguous: <a href="https://www.rfc-editor.org/rfc/rfc9395.html#section-1">“IKEv1 has been deprecated”</a>. New deployments should therefore prefer IKEv2 unless interoperability with legacy systems requires otherwise.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>IKEv2 begins with an <strong>IKE_SA_INIT</strong> exchange. The initiator proposes cryptographic transforms, sends a nonce, and provides a Diffie-Hellman key exchange value. The responder selects an acceptable proposal and returns its own nonce and key exchange material. Only after this request-response pair succeeds does <strong>IKE_AUTH</strong> authenticate the peers and establish the first Child SA in the common case.</p> <p>The diagnostic hierarchy is:</p> <ul> <li>The initiator must have a route to the responder’s public address.</li> <li>The request must leave the local interface with the expected source address and UDP destination port.</li> <li>Intermediate firewalls, NAT devices, ACLs, and ISPs must forward the datagram.</li> <li>The remote host must be listening for IKE and must associate the packet with the intended VPN configuration.</li> <li>The responder must send a response that can return through the reverse path.</li> <li>Only then can proposal selection, authentication, identity validation, and Child SA negotiation become observable failure domains.</li> </ul> <p>IKE runs over UDP, so reliability is implemented by IKE itself. RFC 7296 defines exchanges as request-response pairs and requires the requester to retransmit when a response is not received. Implementations normally use increasing retransmission intervals to avoid amplifying congestion. Repeated identical outbound requests with no inbound response therefore strongly indicate that the state machine has not advanced beyond waiting for the peer.</p> <p>A cryptographic mismatch looks different. Once the responder participates, packet captures or daemon logs may expose errors such as <strong>NO_PROPOSAL_CHOSEN</strong>, <strong>AUTHENTICATION_FAILED</strong>, or <strong>INVALID_KE_PAYLOAD</strong>. Those signals prove the remote endpoint processed the request. Silence does not.</p> <p>NAT traversal introduces another branch. For IKEv2, NAT detection occurs through NAT detection payloads. When NAT-T is used, IKE and UDP-encapsulated ESP normally operate on UDP 4500. RFC 3948 defines UDP encapsulation so ESP can traverse translators that cannot directly handle native ESP reliably. When diagnosing a tunnel, inspect both UDP 500 and UDP 4500 rather than assuming all negotiation remains on a single port.</p> <p><strong>Perfect Forward Secrecy</strong> must also be placed at the correct stage. IKE itself derives keying material from ephemeral Diffie-Hellman. In IKEv2, a later <strong>CREATE_CHILD_SA</strong> exchange may include an additional KE payload to give the generated Child SA stronger forward-secrecy guarantees. This means toggling a PFS option associated with Child SA creation cannot repair a condition where the first IKE request receives no response at all.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The relevant standards form a layered troubleshooting model:</p> <ul> <li><strong>RFC 4301, 2005</strong> defines the IPsec security architecture, Security Policy Database, Security Association Database, and processing model.</li> <li><strong>RFC 4303, 2005</strong> defines ESP, which provides confidentiality and optional integrity for protected IP traffic.</li> <li><strong>RFC 7296, 2014</strong> defines IKEv2 exchanges, retransmission behavior, NAT detection, authentication, Child SA creation, and rekeying.</li> <li><strong>RFC 3947, 2005</strong> defines negotiation of NAT traversal for IKE.</li> <li><strong>RFC 3948, 2005</strong> defines UDP encapsulation of ESP, including operation on UDP 4500.</li> <li><strong>RFC 8247, 2017</strong>, updated by RFC 9395, provides IKEv2 algorithm implementation guidance.</li> <li><strong>RFC 8221, 2017</strong>, also updated by RFC 9395, provides algorithm guidance for ESP and AH.</li> <li><strong>RFC 9395, 2023</strong> deprecates IKEv1 and several obsolete cryptographic algorithms.</li> </ul> <p>Compatibility settings should not be weakened blindly. Modern deployments should prefer IKEv2 and contemporary cryptographic suites.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>A Linux packet capture can establish whether the local endpoint is sending IKE and whether any response returns:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>tcpdump <span class="nt">-ni</span> any <span class="s1">'host 203.0.113.20 and (udp port 500 or udp port 4500)'</span>
</code></pre></div></div> <p>A healthy initial exchange should show traffic in both directions. Repeated traffic in only one direction is more significant than the exact transform proposal at that moment.</p> <p>Linux connection tracking can provide another clue:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>conntrack <span class="nt">-L</span> <span class="nt">-p</span> udp <span class="nt">--dport</span> 500
</code></pre></div></div> <p>Representative output might resemble:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>udp 17 25 src=198.51.100.10 dst=203.0.113.20 sport=500 dport=500 \
    [UNREPLIED] src=203.0.113.20 dst=198.51.100.10 sport=500 dport=500
</code></pre></div></div> <p><strong><code class="language-plaintext highlighter-rouge">UNREPLIED</code> is a Linux conntrack implementation state, not an IETF protocol state.</strong> It means the tracking subsystem has observed packets in the original direction but none matching the reverse tuple. It does not identify where the packet was lost.</p> <p>The decisive next test is a capture at or immediately in front of the responder:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>tcpdump <span class="nt">-ni</span> any <span class="s1">'host 198.51.100.10 and (udp port 500 or udp port 4500)'</span>
</code></pre></div></div> <p>Interpret the result causally:</p> <ul> <li>If the responder capture sees nothing, investigate routing, upstream ACLs, NAT, ISP filtering, wrong peer addressing, or cloud security policy.</li> <li>If the request arrives but the responder sends nothing, investigate IKE service state, peer matching, local firewall policy, malformed input, or responder configuration.</li> <li>If both request and response leave the responder but the initiator sees only outbound traffic, investigate asymmetric routing, NAT state, return-path filtering, or intermediate packet loss.</li> <li>If bidirectional IKE is visible, move upward to proposal, authentication, identity, certificate, traffic-selector, and Child SA diagnostics.</li> </ul> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Transport evidence precedes cryptographic evidence.</strong> No response means the responder has not been proven to participate in negotiation.</li> <li><strong>Proposal mismatches require protocol interaction.</strong> Look for IKE notifications or responder logs before concluding that AES, SHA, or Diffie-Hellman settings disagree.</li> <li><strong>UDP retransmissions are diagnostic evidence.</strong> Repeated requests with no reverse traffic show that IKE reliability logic is waiting on the peer.</li> <li><strong>NAT-T changes the observable path.</strong> Capture both UDP 500 and 4500 and account for address or port translation between endpoints.</li> <li><strong>PFS is not a reachability switch.</strong> Additional Child SA Diffie-Hellman exchange affects key derivation and forward secrecy, not whether the first IKE packet receives a response.</li> <li><strong>Working parallel tunnels are useful controls.</strong> If the same local IPsec stack maintains other SAs successfully, a global IKE subsystem failure becomes less likely, although peer-specific routing and policy must still be verified.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <p>Build IPsec troubleshooting around protocol progression rather than configuration guessing.</p> <ul> <li>Verify peer addressing and routing before modifying cryptographic proposals.</li> <li>Capture IKE traffic on both endpoints whenever possible.</li> <li>Treat one-way UDP 500 or 4500 traffic as a reachability or responder problem until evidence proves otherwise.</li> <li>Confirm NAT-T behavior explicitly when NAT exists anywhere in the path.</li> <li>Move to proposal and authentication debugging only after bidirectional IKE traffic is established.</li> <li>Prefer IKEv2 for new deployments and avoid weakening cryptography merely to test reachability.</li> <li>Record a known-good baseline containing peer addresses, IKE version, transforms, authentication method, lifetimes, Child SA selectors, and NAT assumptions.</li> </ul> <p>The most important operational habit is simple: <strong>follow the packet and the state machine in order</strong>. When IKE never receives a response, the next useful question is not which cipher to try. It is where the response path stops existing.</p>]]></content><author><name></name></author><category term="Networking"/><category term="ipsec"/><category term="ikev2"/><category term="troubleshooting"/><summary type="html"><![CDATA[A standards-based method for separating IKE reachability failures from cryptographic negotiation problems using UDP flow state, packet captures, and protocol sequencing.]]></summary></entry><entry><title type="html">Designing Secure VLAN Segmentation with Zone-Based Firewall Policy</title><link href="https://harshvermac5.github.io/learn/2026/vlan-zone-segmentation/" rel="alternate" type="text/html" title="Designing Secure VLAN Segmentation with Zone-Based Firewall Policy"/><published>2026-09-06T20:13:00+00:00</published><updated>2026-09-06T20:13:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/vlan-zone-segmentation</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/vlan-zone-segmentation/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>Network segmentation is often described too loosely: create several VLANs, assign different SSIDs, and the network is “isolated.” That model is incomplete. A <strong>VLAN</strong> creates a Layer-2 forwarding and broadcast boundary; it does not define which routed flows are permitted between those boundaries. Security isolation appears only when Layer-3 forwarding is combined with an explicit policy-enforcement mechanism such as a <strong>stateful firewall</strong>.</p> <p>The core design principle is therefore simple: use VLANs to create distinct network attachment domains, then use firewall policy to control traffic crossing those domains. Grouping several VLANs into a common <strong>security zone</strong> can simplify policy, but the zone itself is an implementation abstraction rather than an IEEE or IETF protocol element.</p> <p>A compromised IoT endpoint with Internet access is not automatically able to reach a workstation VLAN. If inter-VLAN forwarding is denied, the blast radius remains bounded.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Layer-2 VLAN Segmentation and Stateful Inter-Zone Firewalling</strong></p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>IEEE <strong>802.1Q-2022, Bridges and Bridged Networks</strong> defines VLAN-aware bridging. The standard describes bridges that interconnect LANs to <a href="https://1.ieee802.org/maintenance/p802-1q-rev/">“provide Bridged Networks and VLANs”</a>. An 802.1Q-tagged Ethernet frame carries a four-byte tag containing the Tag Protocol Identifier and Tag Control Information. The Tag Control Information includes <strong>PCP</strong>, <strong>DEI</strong>, and a 12-bit <strong>VLAN Identifier</strong>.</p> <p>A VLAN-aware bridge makes forwarding decisions within a VLAN context. A broadcast, unknown unicast, or multicast frame in VLAN 20 is flooded only to eligible ports participating in VLAN 20, not to ports that belong exclusively to VLAN 30. This is why VLANs create separate Layer-2 broadcast domains.</p> <p>Traffic between VLANs requires a Layer-3 device. For IPv4, <strong>RFC 1812, Requirements for IP Version 4 Routers, June 1995</strong>, defines the architectural behavior expected of routers. Once a host sends a packet to a destination outside its local subnet, the frame is addressed to a router interface, the Layer-2 header is removed, the IP route is evaluated, and a new Layer-2 frame is constructed for the selected egress network.</p> <p>Without filtering, two VLANs attached to the same router may communicate freely; the VLAN boundary alone does not imply a deny policy.</p> <p>NIST <strong>SP 800-41 Rev. 1, Guidelines on Firewalls and Firewall Policy, September 2009</strong>, defines firewalls as mechanisms that <a href="https://csrc.nist.gov/pubs/sp/800/41/r1/final">“control the flow of network traffic between networks or hosts employing differing security postures”</a>. This is the conceptual basis for zone-based policy: networks with similar trust characteristics are grouped, and traffic between groups is explicitly permitted or denied.</p> <p>NIST <strong>SP 800-207, Zero Trust Architecture, August 2020</strong>, adds an important constraint: <a href="https://csrc.nist.gov/pubs/sp/800/207/final">“there is no implicit trust granted to assets or user accounts based solely on their physical or network location”</a>. VLAN membership is therefore a useful enforcement signal, not proof that an endpoint is trustworthy.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>At ingress, an Ethernet switch classifies each frame into a VLAN. A tagged trunk frame normally carries its VLAN identifier explicitly. An untagged access frame is associated with a configured <strong>Port VLAN Identifier</strong>, often called the access or native VLAN depending on context.</p> <p>The switch then learns the source MAC address in a forwarding database scoped to the VLAN and looks up the destination MAC. Known unicast traffic is forwarded to the matching port within the same VLAN. Broadcast and unknown-destination traffic is flooded only across ports authorized for that VLAN.</p> <p>When a host needs to reach a different IP subnet, it sends the packet toward its default gateway. The gateway receives the Ethernet frame on the source VLAN interface, strips Layer-2 encapsulation, decrements the IP TTL, evaluates the routing table, and determines the destination interface or next hop.</p> <p>Before forwarding, a stateful firewall can evaluate attributes such as:</p> <ul> <li>Source and destination IP prefixes</li> <li>Source and destination security zones</li> <li>IP protocol and transport ports</li> <li>Interface direction</li> <li>Connection-tracking state</li> <li>Application identity or higher-layer metadata where supported</li> </ul> <p>A typical stateful rule set permits packets belonging to <strong>established</strong> or <strong>related</strong> flows, evaluates new connection attempts against policy, and drops traffic that has no matching authorization. This enables asymmetric trust relationships such as trusted clients initiating management sessions toward an IoT device while preventing the IoT network from initiating sessions back toward trusted endpoints.</p> <p>A security zone can contain several VLANs if they share the same trust policy. For example, separate camera and building-automation VLANs may remain distinct Layer-2 domains while both belong to an untrusted-device zone. Conversely, two VLANs should not be placed in the same permissive zone merely because their endpoints are physically nearby.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The relevant standards and guidance form a layered model rather than a single end-to-end specification:</p> <ul> <li><strong>IEEE Std 802.1Q-2022</strong> defines VLAN-aware bridges, VLAN identification, bridged forwarding, and associated control behavior. IEEE is developing a maintenance revision that supersedes the 2022 edition, but 802.1Q-2022 remains the published baseline referenced here.</li> <li><strong>RFC 1812, 1995</strong> specifies IPv4 router requirements and the Layer-3 forwarding behavior that moves packets between IP networks.</li> <li><strong>NIST SP 800-41 Rev. 1, 2009</strong> provides firewall architecture and policy guidance, including the principle that filtering boundaries separate systems with differing security postures.</li> <li><strong>NIST SP 800-207, 2020</strong> establishes zero-trust principles and explicitly rejects network location as sufficient evidence of trust.</li> </ul> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>A Linux host can demonstrate the 802.1Q boundary with a standards-compliant VLAN subinterface:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip <span class="nb">link </span>add <span class="nb">link </span>eth0 name eth0.20 <span class="nb">type </span>vlan <span class="nb">id </span>20
ip address add 192.0.2.1/24 dev eth0.20
ip <span class="nb">link set </span>eth0.20 up
</code></pre></div></div> <p>A capture on the parent interface exposes the VLAN tag:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-eni</span> eth0 vlan 20
</code></pre></div></div> <p>Representative output shows the 802.1Q EtherType and VLAN identifier:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ethertype 802.1Q (0x8100), vlan 20, ethertype IPv4,
192.0.2.10.51032 &gt; 198.51.100.20.443: Flags [S]
</code></pre></div></div> <p>The VLAN tag proves Layer-2 classification, not security policy. The following <strong>nftables</strong> example illustrates a default-deny inter-zone model:</p> <pre><code class="language-nft"> table inet filter {
     chain forward {
         type filter hook forward priority 0; policy drop;

         ct state established,related accept

         iifname "trusted" oifname "iot" tcp dport 443 accept
         iifname "trusted" oifname "wan" accept
         iifname "iot" oifname "wan" accept
     }
 }
</code></pre> <p>Both trusted and IoT networks may initiate Internet traffic, while the IoT segment cannot initiate toward the trusted segment.</p> <p>Wireless networks follow the same model. An SSID may map clients into a VLAN, but one SSID per VLAN is not required. Excessive SSID counts can increase management-frame airtime overhead.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <p><strong>VLAN count is not the same as VLAN overhead.</strong> The 802.1Q tag adds four bytes. Operational cost usually comes from additional subnets, DHCP scopes, routing interfaces, firewall rules, and troubleshooting complexity rather than tag processing.</p> <p><strong>Segmentation is a policy problem, not a naming problem.</strong> VLAN names are administrative metadata. Security comes from membership, forwarding boundaries, authentication, routing, and firewall enforcement.</p> <p><strong>Broadcast reduction is a side effect, not the primary security objective.</strong> Smaller Layer-2 domains reduce broadcast scope, but the principal security value is controlling lateral movement across routed boundaries.</p> <p><strong>Zones reduce policy duplication.</strong> VLANs with equivalent trust requirements can share one inter-zone rule set, but over-grouping can create unintended reachability.</p> <p><strong>Stateful directionality matters.</strong> Allowing trusted-to-untrusted initiation while denying untrusted-to-trusted initiation does not mean return packets are blocked. Connection tracking permits response traffic for an authorized session without creating a general reverse-direction allowance.</p> <p><strong>Management networks deserve distinct treatment.</strong> Infrastructure management interfaces should not be exposed on endpoint-facing ports or broadly reachable from untrusted zones. A dedicated management VLAN is useful only when routing and firewall policy protect it.</p> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Define trust classes before creating VLANs. Separate endpoints when their communication requirements, administrative ownership, or compromise impact differ materially.</li> <li>Use VLANs to create Layer-2 boundaries and use a default-deny firewall posture for traffic crossing those boundaries.</li> <li>Permit only required inter-zone flows, preferably by destination service and direction rather than broad subnet-to-subnet allowances.</li> <li>Keep management interfaces in a restricted network and avoid carrying the management VLAN across ports that do not require it.</li> <li>Treat IoT, guest, camera, lab, and externally exposed systems as separate trust categories when their risk profiles justify it.</li> <li>Do not equate local segmentation with endpoint protection. Patch management, MFA, host firewalls, endpoint detection, credential hygiene, and application security remain necessary layers.</li> <li>Validate segmentation with packet captures, connection-state inspection, and explicit positive and negative tests. A successful Internet connection proves routing; a failed cross-zone connection proves isolation only when the firewall log or packet path confirms the intended policy caused the drop.</li> </ul> <p>A robust design therefore uses <strong>802.1Q for attachment boundaries, routing for controlled Layer-3 transit, stateful firewalls for enforcement, and identity-aware security controls for defense in depth</strong>. Each layer solves a different problem, and the architecture is strongest when those roles remain explicit.</p>]]></content><author><name></name></author><category term="Networking"/><category term="vlan"/><category term="firewall"/><category term="segmentation"/><summary type="html"><![CDATA[A standards-based guide to combining IEEE 802.1Q VLAN boundaries, Layer-3 routing, and stateful firewall zones for practical network segmentation.]]></summary></entry><entry><title type="html">DHCPv6 Prefix Delegation: From WAN Lease to LAN Prefix Activation</title><link href="https://harshvermac5.github.io/learn/2026/dhcpv6-prefix-delegation/" rel="alternate" type="text/html" title="DHCPv6 Prefix Delegation: From WAN Lease to LAN Prefix Activation"/><published>2026-09-06T19:25:00+00:00</published><updated>2026-09-06T19:25:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/dhcpv6-prefix-delegation</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/dhcpv6-prefix-delegation/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>DHCPv6 Prefix Delegation separates <strong>obtaining IPv6 address space from an upstream provider</strong> from <strong>installing and advertising that address space on downstream networks</strong>. This distinction matters because a router can successfully receive a delegated prefix in DHCPv6 while still failing to configure LAN interfaces, generate downstream routes, or advertise usable prefixes to hosts.</p> <p>The central troubleshooting principle is therefore to validate Prefix Delegation as a pipeline rather than a single event. Engineers should independently prove that the requesting router asked for a prefix, that the delegating router returned one, that the requesting router installed appropriate downstream subnets, and that hosts learned those subnets through Router Advertisements or DHCPv6.</p> <p>A successful WAN-side DHCPv6 exchange proves only the first half of that chain. It does not prove that downstream IPv6 service is operational.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>IPv6 DHCPv6 Prefix Delegation and downstream subnet lifecycle management</strong></p> <p>The mechanism spans DHCPv6 client state, delegated-prefix lifetimes, routing-table installation, /64 subnet derivation, Neighbor Discovery, Router Advertisements, SLAAC, and optional stateful DHCPv6 address assignment.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p><strong>RFC 8415, Dynamic Host Configuration Protocol for IPv6, November 2018</strong>, defines modern DHCPv6 behavior and incorporates Prefix Delegation previously specified separately by RFC 3633. It defines <strong>IA_PD</strong> as <a href="https://datatracker.ietf.org/doc/html/rfc8415">“an IA that carries delegated prefixes”</a>, while <strong>IA_NA</strong> carries non-temporary IPv6 addresses assigned to the requesting node itself.</p> <p>These objects solve different problems. A WAN interface might receive an address through IA_NA such as <code class="language-plaintext highlighter-rouge">2001:db8:601:30::f:a1df/128</code>, while the same DHCPv6 transaction delegates <code class="language-plaintext highlighter-rouge">2001:db8:42a1:5c00::/56</code> through IA_PD. The <code class="language-plaintext highlighter-rouge">/128</code> identifies the router as an IPv6 endpoint on the WAN. The <code class="language-plaintext highlighter-rouge">/56</code> is routing authority that the router can divide into downstream subnets.</p> <p>A <code class="language-plaintext highlighter-rouge">/56</code> contains 256 distinct <code class="language-plaintext highlighter-rouge">/64</code> networks. The delegated <code class="language-plaintext highlighter-rouge">/56</code> is therefore not normally assigned wholesale to one LAN. Instead, the requesting router derives one or more <code class="language-plaintext highlighter-rouge">/64</code> prefixes from it and binds those prefixes to downstream interfaces.</p> <p><strong>RFC 7084, Basic Requirements for IPv6 Customer Edge Routers, November 2013</strong>, states that an IPv6 edge router <a href="https://datatracker.ietf.org/doc/html/rfc7084">“MUST assign a separate /64”</a> from its delegated space to each LAN interface. RFC 9818, published in July 2025, updates RFC 7084 with additional guidance for DHCPv6 Prefix Delegation on LANs.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A standard DHCPv6 Prefix Delegation transaction commonly begins with <strong>Solicit</strong>. The requesting router includes an IA_PD option and may include an IA Prefix option as a <strong>prefix-length hint</strong>, for example <code class="language-plaintext highlighter-rouge">::/56</code>. The hint expresses the desired size; it does not force the server to return that exact prefix length.</p> <p>The delegating router responds with <strong>Advertise</strong>, including an IA_PD if it can offer a prefix. The requesting router then sends <strong>Request</strong>, identifying the selected server and the requested IA_PD. The server finalizes the lease with <strong>Reply</strong>. Rapid Commit can shorten this exchange when both sides support it.</p> <p>The IA_PD contains an <strong>IAID</strong>, <strong>T1</strong>, <strong>T2</strong>, and one or more delegated prefixes. Each delegated prefix has a <strong>preferred lifetime</strong> and <strong>valid lifetime</strong>.</p> <ul> <li><strong>T1</strong> controls when the client should contact the original server to renew the binding.</li> <li><strong>T2</strong> controls when the client should attempt rebinding with any available server.</li> <li><strong>Preferred lifetime</strong> determines how long addresses formed from the prefix remain preferred for new connections.</li> <li><strong>Valid lifetime</strong> determines how long those addresses remain valid at all.</li> </ul> <p>RFC 8415 recommends T1 and T2 values near 50% and 80% of the shortest preferred lifetime when the server explicitly controls renewal timing. For a 21,600-second preferred lifetime, values of <code class="language-plaintext highlighter-rouge">T1=10800</code> and <code class="language-plaintext highlighter-rouge">T2=17280</code> map exactly to those proportions.</p> <p>After receiving the Reply, the requesting router must move from <strong>lease acquisition</strong> to <strong>prefix consumption</strong>. It selects <code class="language-plaintext highlighter-rouge">/64</code> subnets from the delegated block, configures downstream interface state, installs connected and delegated-prefix routes, and begins advertising appropriate Prefix Information Options through ICMPv6 Router Advertisements.</p> <p>Hosts using SLAAC then construct addresses from the advertised <code class="language-plaintext highlighter-rouge">/64</code>. RFC 4862 describes SLAAC as requiring <a href="https://datatracker.ietf.org/doc/html/rfc4862">“no manual configuration of hosts”</a> in the normal case. DHCPv6 may still provide stateful addresses or ancillary information such as DNS, depending on Router Advertisement flags and local design.</p> <p>Neighbor Discovery is separate from DHCPv6. <strong>RFC 4861, September 2007</strong>, defines Neighbor Solicitation, Neighbor Advertisement, Router Solicitation, and Router Advertisement behavior. Seeing Neighbor Solicitation in a capture proves NDP activity, not Prefix Delegation success.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <ul> <li><strong>RFC 8200 — Internet Protocol, Version 6 (IPv6) Specification, July 2017:</strong> the base IPv6 Internet Standard and packet-format architecture.</li> <li><strong>RFC 8415 — Dynamic Host Configuration Protocol for IPv6, November 2018:</strong> authoritative DHCPv6 behavior, IA_NA, IA_PD, IA Prefix options, timers, Renew, Rebind, and status codes.</li> <li><strong>RFC 4861 — Neighbor Discovery for IP version 6, September 2007:</strong> NDP, Router Advertisements, Neighbor Solicitation, reachability, and router discovery.</li> <li><strong>RFC 4862 — IPv6 Stateless Address Autoconfiguration, September 2007:</strong> SLAAC address formation, lifetimes, and Duplicate Address Detection.</li> <li><strong>RFC 6177 — IPv6 Address Assignment to End Sites, March 2011:</strong> operational guidance for end-site allocation sizes and the architectural need for multiple subnets.</li> <li><strong>RFC 7084 — Basic Requirements for IPv6 Customer Edge Routers, November 2013:</strong> edge-router requirements for requesting delegated prefixes and assigning downstream <code class="language-plaintext highlighter-rouge">/64</code>s.</li> <li><strong>RFC 9818 — DHCPv6 Prefix Delegation on IPv6 Customer Edge Routers in LANs, July 2025:</strong> updates RFC 7084 for downstream Prefix Delegation scenarios.</li> </ul> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>The following sanitized trace demonstrates a healthy acquisition phase using documentation-only IPv6 prefixes:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DHCPv6 Solicit
  IA_NA IAID:1
  IA_PD IAID:1
    IA_PREFIX ::/56 preferred=0 valid=0

DHCPv6 Advertise
  IA_NA IAID:1 T1=10800 T2=17280
    IA_ADDR 2001:db8:601:30::f:a1df
  IA_PD IAID:1 T1=10800 T2=17280
    IA_PREFIX 2001:db8:42a1:5c00::/56 preferred=21600 valid=21600

DHCPv6 Request
  IA_NA IAID:1
  IA_PD IAID:1
    IA_PREFIX 2001:db8:42a1:5c00::/56

DHCPv6 Reply
  IA_NA IAID:1 T1=10800 T2=17280
    IA_ADDR 2001:db8:601:30::f:a1df
  IA_PD IAID:1 T1=10800 T2=17280
    IA_PREFIX 2001:db8:42a1:5c00::/56 preferred=21600 valid=21600
</code></pre></div></div> <p>This capture proves that the server accepted the Prefix Delegation request and returned a valid <code class="language-plaintext highlighter-rouge">/56</code>. If the router subsequently has no delegated <code class="language-plaintext highlighter-rouge">/64</code> on its LAN, the failure is downstream of DHCPv6 acquisition.</p> <p>A correct implementation could derive networks such as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Delegated block: 2001:db8:42a1:5c00::/56
LAN-A:           2001:db8:42a1:5c00::/64
LAN-B:           2001:db8:42a1:5c01::/64
LAN-C:           2001:db8:42a1:5c02::/64
</code></pre></div></div> <p>Engineers should then verify the control plane directly:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip <span class="nt">-6</span> addr show
ip <span class="nt">-6</span> route show
rdisc6 eth0
<span class="nb">sudo </span>tcpdump <span class="nt">-ni</span> eth0 <span class="s1">'icmp6 or (udp port 546 or udp port 547)'</span>
</code></pre></div></div> <p>A DHCPv6 Reply containing <strong>NoPrefixAvail</strong> inside IA_PD has a very different meaning: the delegating server could not provide a prefix for that IA. RFC 8415 explicitly defines this status behavior. Absence of that status, combined with a returned IA Prefix, rules out upstream refusal for that transaction.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <p>The most important diagnostic separation is <strong>IA_NA versus IA_PD</strong>. Receiving a WAN IPv6 address does not prove that the router obtained routable space for downstream networks. Conversely, successful IA_PD does not require the delegated prefix to appear as the WAN interface address.</p> <p>A second separation is <strong>delegation versus advertisement</strong>. DHCPv6 operates between the requesting and delegating routers. SLAAC and NDP operate on the downstream link. A working DHCPv6 exchange can coexist with missing Router Advertisements, absent <code class="language-plaintext highlighter-rouge">/64</code> interface configuration, stale routes, or failed internal prefix-state propagation.</p> <p>Prefix changes introduce another state problem. A router must not simply replace an old prefix without lifecycle handling. Downstream hosts may retain addresses until advertised lifetimes expire. RFC 7084 requires routers to deprecate an old prefix when a delegated prefix changes so hosts stop selecting stale addresses for new connections.</p> <p>Finally, intermittent failures should be analyzed around renewal boundaries. If initial acquisition works but service disappears around T1, T2, or valid-lifetime expiry, investigate Renew and Rebind exchanges, DUID and IAID stability, lease persistence, routing updates, and Router Advertisement regeneration.</p> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Validate DHCPv6 Prefix Delegation in stages: <strong>request, server assignment, local installation, route creation, Router Advertisement, and host address formation</strong>.</li> <li>Capture both successful and failed periods and compare IAID, DUID, delegated prefix, T1, T2, preferred lifetime, valid lifetime, and DHCPv6 status codes.</li> <li>Treat a returned IA_PD prefix as proof of upstream delegation, not proof of downstream activation.</li> <li>Allocate one <code class="language-plaintext highlighter-rouge">/64</code> per LAN or VLAN from the delegated block rather than assigning the entire delegated aggregate to a single broadcast domain.</li> <li>Verify that prefix changes trigger downstream deprecation and new Router Advertisements instead of leaving stale addresses active.</li> <li>Correlate failures with T1/T2 renewal and rebind events when instability appears periodic.</li> <li>Keep NDP evidence separate from DHCPv6 evidence. Neighbor Solicitation shows link-local neighbor discovery; IA_PD in DHCPv6 is what proves Prefix Delegation.</li> </ul> <p>The reusable troubleshooting model is simple: <strong>prove each state transition independently</strong>. When the DHCPv6 Reply contains a valid IA_PD but downstream <code class="language-plaintext highlighter-rouge">/64</code>s never appear, the investigation should move away from the provider and toward the router’s internal prefix-consumption, routing, and advertisement logic.</p>]]></content><author><name></name></author><category term="Networking"/><category term="ipv6"/><category term="dhcpv6"/><category term="prefix-delegation"/><summary type="html"><![CDATA[A deep technical guide to DHCPv6 Prefix Delegation, including IA_PD state, renewal timers, downstream /64 assignment, Router Advertisements, and failure isolation.]]></summary></entry><entry><title type="html">Path MTU Discovery and TCP MSS Clamping Across WireGuard Tunnels</title><link href="https://harshvermac5.github.io/learn/2026/wireguard-mtu-mss-clamping/" rel="alternate" type="text/html" title="Path MTU Discovery and TCP MSS Clamping Across WireGuard Tunnels"/><published>2026-09-06T18:28:00+00:00</published><updated>2026-09-06T18:28:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/wireguard-mtu-mss-clamping</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/wireguard-mtu-mss-clamping/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>Encrypted tunnels reduce the usable packet size available to inner traffic because every original IP packet is wrapped in additional protocol headers and cryptographic metadata. When endpoints continue transmitting packets sized for a 1500-byte path while the encapsulated path can carry less, the result may be fragmentation, packet loss, or a <strong>Path MTU Discovery black hole</strong>.</p> <p>The important engineering problem is not simply “set a smaller MTU.” It is understanding how <strong>Path MTU Discovery (PMTUD)</strong>, <strong>TCP Maximum Segment Size (MSS)</strong>, UDP encapsulation, and ICMP feedback interact. The most confusing failures are selective: DNS works, TCP handshakes succeed, and small requests complete, yet TLS sessions, large HTTP responses, uploads, or application APIs stall.</p> <p><a href="https://www.rfc-editor.org/rfc/rfc8201.html">RFC 8201 defines the Path MTU as “the minimum link MTU of all the links in a path.”</a> Once a tunnel is introduced, its encapsulation overhead becomes part of the packet-size budget that must fit within that minimum.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Tunnel PMTU, TCP MSS advertisement, ICMP-based PMTUD, and packetization-layer MTU discovery for encrypted UDP overlays.</strong></p> <p>WireGuard is a useful concrete example because it carries encrypted IP packets over UDP and therefore exposes the interaction between inner MTU, outer PMTU, and TCP MSS clearly.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>Classical IPv4 PMTUD is defined by <strong>RFC 1191, November 1990</strong>. The sender transmits IPv4 packets with the <strong>Don’t Fragment (DF)</strong> bit set. If a router cannot forward a packet because the next-hop link MTU is smaller, it returns an ICMP Destination Unreachable, Fragmentation Needed message. The sender then lowers its estimate of the PMTU.</p> <p>IPv6 uses a related model defined by <strong>RFC 8201, July 2017</strong>. Routers do not fragment forwarded IPv6 packets. When a packet is too large, the router generates an <strong>ICMPv6 Packet Too Big</strong> message containing the next-hop MTU, as specified by <strong>RFC 4443, March 2006</strong>.</p> <p>The weakness is dependence on control-plane feedback. <a href="https://datatracker.ietf.org/doc/html/rfc2923">RFC 2923 notes that “Firewalls are often misconfigured to suppress all ICMP messages.”</a> If Fragmentation Needed or Packet Too Big messages disappear, a sender may continue transmitting oversized packets. Small packets pass, while larger packets repeatedly vanish. That is the classic PMTU black hole.</p> <p>TCP adds another mechanism: the <strong>MSS option</strong> exchanged in SYN and SYN/ACK packets. MSS limits TCP payload size, not total IP packet size. <a href="https://www.rfc-editor.org/rfc/rfc6691.html">RFC 6691 states that “The MSS counts only data octets in the segment.”</a> For ordinary headers, an IPv4 TCP MSS can be approximated as <code class="language-plaintext highlighter-rouge">PMTU - 20 - 20</code>; for IPv6, <code class="language-plaintext highlighter-rouge">PMTU - 40 - 20</code>. TCP or IP options must be handled separately by the sender.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A tunnel changes the packet path by adding an outer transport envelope around an existing inner IP packet.</p> <p>For a WireGuard-style UDP tunnel, transport data contains a fixed tunnel header, encrypted payload, and authentication tag, then UDP and an outer IP header. The protocol specification shows <strong>16 bytes of transport header fields</strong> before the encrypted packet, while AEAD adds a <strong>16-byte authentication tag</strong>. All transport packets are carried over UDP. The resulting fixed overhead is therefore at least:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Outer IPv4: 20-byte IP + 8-byte UDP + 16-byte tunnel header + 16-byte AEAD tag = 60 bytes
Outer IPv6: 40-byte IP + 8-byte UDP + 16-byte tunnel header + 16-byte AEAD tag = 80 bytes
</code></pre></div></div> <p>The protocol may also pad encrypted inner packets, and additional technologies such as PPPoE, provider tunnels, or nested VPNs can reduce effective PMTU further. The correct inner MTU is therefore a property of the complete path, not merely the local Ethernet interface.</p> <p>The failure sequence typically looks like this:</p> <ul> <li>An endpoint sends a TCP SYN through the tunnel. The packet is small and succeeds.</li> <li>The remote endpoint returns SYN/ACK. The three-way handshake completes normally.</li> <li>One endpoint then emits a near-MSS-sized TCP segment.</li> <li>Tunnel encapsulation expands the packet beyond the outer PMTU.</li> <li>A router drops the packet and should return an ICMP size error.</li> <li>If that ICMP message is filtered, lost, or not associated correctly with the tunnel flow, the sender never learns the smaller PMTU.</li> <li>TCP retransmits the same oversized data, producing an apparent application stall rather than a total connectivity failure.</li> </ul> <p><a href="https://www.rfc-editor.org/rfc/rfc8201.html">RFC 8201 describes this exact symptom: a connection can complete the TCP handshake and then hang when data is transferred.</a></p> <p><strong>MSS clamping</strong> changes the TCP MSS advertised during connection establishment so endpoints never create TCP payloads that would exceed the known tunnel PMTU. Because MSS is directional, SYN and SYN/ACK traffic should both be considered.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The relevant standards form a progression from classical PMTUD to more robust packetization-layer discovery:</p> <ul> <li><strong>RFC 1191 — Path MTU Discovery, 1990:</strong> defines IPv4 PMTUD using DF and ICMP Fragmentation Needed feedback.</li> <li><strong>RFC 2923 — TCP Problems with Path MTU Discovery, 2000:</strong> documents PMTU black holes and TCP-specific failure modes.</li> <li><strong>RFC 4443 — ICMPv6, 2006:</strong> defines ICMPv6 Packet Too Big behavior.</li> <li><strong>RFC 4821 — Packetization Layer Path MTU Discovery, 2007:</strong> defines probe-based discovery that does not depend entirely on ICMP.</li> <li><strong>RFC 6691 — TCP Options and Maximum Segment Size, 2012:</strong> clarifies correct MSS calculation and interaction with header options.</li> <li><strong>RFC 8085 / BCP 145 — UDP Usage Guidelines, 2017:</strong> provides message-size guidance for UDP applications and tunnels.</li> <li><strong>RFC 8201 — IPv6 Path MTU Discovery, 2017:</strong> updates IPv6 PMTUD behavior.</li> <li><strong>RFC 8899 — Datagram PLPMTUD, 2020:</strong> extends robust packetization-layer discovery to datagram transports.</li> </ul> <p><a href="https://datatracker.ietf.org/doc/html/rfc4821">RFC 4821 explains that PLPMTUD “does not depend on the delivery of ICMP messages.”</a> RFC 8899 extends that design to datagram packetization layers, making it especially relevant to modern UDP-based tunnels.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>On Linux, first inspect the configured tunnel MTU and routing decision:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip <span class="nb">link </span>show wg0
ip route get 198.51.100.10
</code></pre></div></div> <p>Probe candidate IPv4 packet sizes with DF set. An ICMP payload of 1372 bytes plus 20 bytes of IPv4 header and 8 bytes of ICMP header produces a 1400-byte IPv4 packet:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ping <span class="nt">-M</span> <span class="k">do</span> <span class="nt">-s</span> 1372 198.51.100.10
</code></pre></div></div> <p>For broader path testing, <code class="language-plaintext highlighter-rouge">tracepath</code> can expose discovered PMTU changes:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tracepath 198.51.100.10
</code></pre></div></div> <p>Packet capture is often more conclusive than application logs:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-ni</span> any <span class="s1">'icmp or icmp6 or tcp'</span>
</code></pre></div></div> <p>Evidence consistent with a PMTU black hole includes a successful SYN/SYN-ACK/ACK exchange followed by repeated retransmission of the same large TCP sequence range, with no corresponding ICMP Fragmentation Needed or ICMPv6 Packet Too Big message reaching the sender.</p> <p>A standards-aligned Linux firewall can clamp TCP MSS to route-derived PMTU using nftables:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nft add rule inet mangle forward tcp flags syn tcp option maxseg size <span class="nb">set </span>rt mtu
</code></pre></div></div> <p>The nftables documentation states that <code class="language-plaintext highlighter-rouge">rt mtu</code> uses route information learned through PMTUD. A fixed MSS can also be applied when the effective tunnel PMTU is known and deterministic:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nft add rule inet mangle forward tcp flags syn tcp option maxseg size <span class="nb">set </span>1360
</code></pre></div></div> <p>A static value should be derived from measured PMTU and header requirements, not copied from another environment.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Selective failure is diagnostic evidence.</strong> If small flows work but large TLS or HTTP transfers stall, investigate PMTU before blaming DNS, routing, or application-layer policy.</li> <li><strong>MSS is not MTU.</strong> MSS constrains TCP payload only. UDP, ICMP, QUIC, and non-TCP traffic do not benefit from TCP MSS clamping.</li> <li><strong>Clamping hides symptoms but does not repair PMTUD.</strong> Restoring valid ICMP size feedback is architecturally preferable where possible.</li> <li><strong>Tunnel overhead is cumulative.</strong> Outer IPv6, PPPoE, nested tunnels, carrier overlays, and additional encapsulation all consume packet budget.</li> <li><strong>Asymmetric paths complicate diagnosis.</strong> Each direction can have a different PMTU and therefore a different safe MSS.</li> <li><strong>A very low MSS is safe but inefficient.</strong> Excessively small segments increase packet rate, per-packet processing, interrupt load, and protocol overhead.</li> <li><strong>UDP overlays need their own sizing strategy.</strong> RFC 8899 exists because TCP-specific techniques cannot solve datagram PMTU problems generically.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Measure the effective PMTU across the real production path rather than assuming 1500 bytes end to end.</li> <li>Permit required ICMP Fragmentation Needed and ICMPv6 Packet Too Big messages through security policy; do not treat all ICMP as expendable.</li> <li>Set tunnel interface MTU with enough headroom for outer IP, UDP, tunnel metadata, cryptographic authentication, padding, and any additional encapsulation.</li> <li>Use MSS clamping for TCP when the tunnel PMTU is lower than endpoints can reliably discover, but derive the value from the path.</li> <li>Validate both directions because PMTU and MSS can be asymmetric.</li> <li>Capture SYN packets and large retransmissions to prove whether MSS advertisement and PMTU behavior match the design.</li> <li>Prefer PLPMTUD/DPLPMTUD-capable transports where available because they recover more robustly when ICMP feedback is unreliable.</li> </ul> <p>The central principle is simple: <strong>an encrypted tunnel does not create more MTU; it consumes it</strong>. Reliable tunnel performance depends on ensuring that packetization at the inner layer respects the smallest usable size of the encapsulated path.</p>]]></content><author><name></name></author><category term="Networking"/><category term="wireguard"/><category term="mtu"/><category term="tcp"/><summary type="html"><![CDATA[A deep technical guide to PMTU black holes, TCP MSS behavior, and reliable packet sizing across encrypted UDP tunnels.]]></summary></entry><entry><title type="html">Engineering Reliable SNMP Monitoring for Network Switches</title><link href="https://harshvermac5.github.io/learn/2026/snmp-switch-monitoring/" rel="alternate" type="text/html" title="Engineering Reliable SNMP Monitoring for Network Switches"/><published>2026-09-06T17:51:00+00:00</published><updated>2026-09-06T17:51:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/snmp-switch-monitoring</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/snmp-switch-monitoring/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>Reliable switch monitoring requires more than enabling an SNMP agent and allowing UDP port 161. The engineering problem is understanding how a manager retrieves objects, authenticates requests, handles counter discontinuities, and distinguishes a failed poll from a failed device.</p> <p>The core principle is <strong>manager-driven telemetry over a structured management information model</strong>. SNMP normally operates as a request-response protocol in which a manager queries an agent for objects identified by hierarchical <strong>Object Identifiers</strong>, or OIDs. Those objects are defined in <strong>Management Information Base</strong> modules using the <strong>Structure of Management Information Version 2</strong>, or SMIv2.</p> <p>For switching infrastructure, this model exposes interface state, counters, errors, discards, speed, and uptime without coupling monitoring to a proprietary API.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Network Management Protocols and Switch Observability</strong></p> <p>The relevant domains are SNMP architecture, SMIv2 data modeling, interface MIB instrumentation, polling design, transport reachability, counter semantics, and authenticated management-plane security.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>The modern SNMP framework is described by <strong>RFC 3411, An Architecture for Describing Simple Network Management Protocol Management Frameworks, December 2002</strong>. It separates an SNMP entity into an engine and applications, with message processing, security, access control, and command-response functions treated as modular subsystems.</p> <p>RFC 3411 explicitly models managers and agents, describing <a href="https://www.rfc-editor.org/rfc/rfc3411.html">“several potentially many nodes, each with an SNMP entity”</a> and a management entity that generates commands or receives notifications. The manager sends protocol requests to an SNMP engine, which validates security and access control, resolves OIDs, and returns a response.</p> <p><strong>RFC 3416, Version 2 of the Protocol Operations for SNMP, December 2002</strong> defines the principal protocol data units. These include <strong>GetRequest</strong>, <strong>GetNextRequest</strong>, <strong>GetBulkRequest</strong>, <strong>Response</strong>, <strong>SetRequest</strong>, <strong>InformRequest</strong>, and <strong>SNMPv2-Trap</strong>. Monitoring systems primarily use the first four for polling.</p> <p>The data model is governed by <strong>RFC 2578, Structure of Management Information Version 2, April 1999</strong>. SMIv2 defines the OID hierarchy, base data types, textual conventions, module identities, and object semantics used by MIB modules. An OID is therefore not merely a numeric sensor address; it is an administratively assigned identifier with a defined syntax, access mode, status, and semantic meaning.</p> <p>For interfaces, <strong>RFC 2863, The Interfaces Group MIB, June 2000</strong> defines standard objects such as <code class="language-plaintext highlighter-rouge">ifAdminStatus</code>, <code class="language-plaintext highlighter-rouge">ifOperStatus</code>, <code class="language-plaintext highlighter-rouge">ifLastChange</code>, <code class="language-plaintext highlighter-rouge">ifInErrors</code>, <code class="language-plaintext highlighter-rouge">ifOutErrors</code>, <code class="language-plaintext highlighter-rouge">ifInDiscards</code>, <code class="language-plaintext highlighter-rouge">ifOutDiscards</code>, and high-capacity 64-bit octet counters.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A typical polling cycle begins when the monitoring system selects an OID or a subtree to query. A single scalar can be retrieved with <strong>GetRequest</strong>. Sequential discovery can use <strong>GetNextRequest</strong>, while <strong>GetBulkRequest</strong> is preferred for efficiently walking tables under SNMPv2c and SNMPv3.</p> <p>The manager creates a request containing a request identifier and one or more variable bindings. The packet is sent to the managed node, conventionally using <strong>UDP port 161</strong>. IANA registers <code class="language-plaintext highlighter-rouge">snmp</code> on port 161 for both UDP and TCP, although UDP remains the dominant transport for conventional SNMP polling.</p> <p>The agent processes the request through its message-processing and security subsystems. If authentication, privacy, context, and access-control checks succeed, the command responder resolves the requested OIDs against its instrumentation. It then returns a <strong>Response-PDU</strong> carrying values or an error indication.</p> <p>Polling reliability depends on manager-side timeout and retry policy. With UDP, the manager waits for its configured timeout and may retransmit. A timeout proves only that a valid response was not received in time; filtering, routing failure, CPU pressure, authentication mismatch, packet loss, or agent overload can produce the same symptom.</p> <p>For SNMPv3, the security path is more involved. <strong>RFC 3414, User-based Security Model for SNMPv3, December 2002</strong> defines the <strong>User-based Security Model</strong>, or USM. Each message is associated with an authoritative SNMP engine, an engine identifier, security parameters, and a security level. The common security levels are <strong>noAuthNoPriv</strong>, <strong>authNoPriv</strong>, and <strong>authPriv</strong>.</p> <p>USM also provides timeliness protection using values associated with the authoritative engine, including engine boots and engine time. This helps reject replayed messages outside the accepted time window. Before authenticated communication is fully established, a manager may need to discover the remote engine identifier and synchronize the necessary timeliness state.</p> <p>The original USM specification defined HMAC-MD5-96 and HMAC-SHA-96 authentication. Modern deployments should prefer stronger supported authentication algorithms. <strong>RFC 7860, April 2016</strong> adds HMAC-SHA-2 authentication protocols for SNMPv3 USM. For privacy, <strong>RFC 3826, June 2004</strong> defines AES in CFB mode with a 128-bit key and states that the protocol provides <a href="https://www.rfc-editor.org/rfc/rfc3826.html">“support for data confidentiality”</a>.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The primary standards for a standards-compliant monitoring design are <strong>RFC 3411</strong> for architecture, <strong>RFC 3414</strong> for SNMPv3 USM, <strong>RFC 3416</strong> for protocol operations, <strong>RFC 2578</strong> for SMIv2, <strong>RFC 2863</strong> for interface instrumentation, <strong>RFC 3826</strong> for AES privacy, and <strong>RFC 7860</strong> for HMAC-SHA-2 authentication.</p> <p>IANA’s Service Name and Transport Protocol Port Number Registry assigns <strong>161</strong> to <code class="language-plaintext highlighter-rouge">snmp</code> and <strong>162</strong> to <code class="language-plaintext highlighter-rouge">snmptrap</code>. This distinction is architectural: polling requests target the agent’s SNMP service, while trap or inform receivers conventionally listen on port 162. A device that implements polling but not notifications is an implementation limitation, not a limitation of the SNMP standards themselves.</p> <p>SMIv2 permits enterprise-specific MIB modules beneath <code class="language-plaintext highlighter-rouge">1.3.6.1.4.1</code>. Prefer standard MIBs when they expose the required signal because they improve monitoring portability.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>A simple SNMPv2c query using Net-SNMP can verify basic reachability and object access:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>snmpget <span class="nt">-v2c</span> <span class="nt">-c</span> monitor 192.0.2.10 1.3.6.1.2.1.1.3.0
</code></pre></div></div> <p>The requested object is <code class="language-plaintext highlighter-rouge">sysUpTime.0</code>. Successful output demonstrates IP reachability, UDP/161 reachability, community acceptance, OID availability, and a functioning request-response path. It does not prove that every MIB subtree is authorized.</p> <p>Walking the standard interface table is more useful for switch monitoring:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>snmpbulkwalk <span class="nt">-v2c</span> <span class="nt">-c</span> monitor 192.0.2.10 1.3.6.1.2.1.2.2
</code></pre></div></div> <p>A secure SNMPv3 query can use authenticated and encrypted transport semantics at the SNMP message layer:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>snmpget <span class="nt">-v3</span> <span class="nt">-l</span> authPriv <span class="nt">-u</span> monitor <span class="se">\</span>
  <span class="nt">-a</span> SHA-256 <span class="nt">-A</span> <span class="s1">'authentication-secret'</span> <span class="se">\</span>
  <span class="nt">-x</span> AES <span class="nt">-X</span> <span class="s1">'privacy-secret'</span> <span class="se">\</span>
  192.0.2.10 1.3.6.1.2.1.1.3.0
</code></pre></div></div> <p>Exact command-line algorithm names depend on the installed Net-SNMP build, so supported algorithms should be verified locally.</p> <p>Packet capture is useful when a poll times out:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-ni</span> any <span class="s1">'udp port 161 or udp port 162'</span>
</code></pre></div></div> <p>If requests leave the manager but no responses return, investigate path filtering, agent reachability, and device-side processing. If responses return but the monitoring application still reports failure, investigate credentials, security parameters, OID parsing, request identifiers, or application-level timeout handling.</p> <p>For high-speed links, prefer <code class="language-plaintext highlighter-rouge">ifHCInOctets</code> and <code class="language-plaintext highlighter-rouge">ifHCOutOctets</code> over 32-bit counters. RFC 2863 defines these as 64-bit versions of the basic octet counters. A 32-bit octet counter can wrap rapidly on multi-gigabit interfaces, producing misleading rate calculations if the poller does not correctly handle rollover.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>SNMP reachability is a management-plane dependency.</strong> Successful ICMP or production traffic does not prove that UDP/161 is permitted end to end.</li> <li><strong>Polling failures are ambiguous until packet flow is observed.</strong> A timeout can indicate network loss, filtering, authentication failure, agent overload, or device failure.</li> <li><strong>MIB semantics matter more than raw OID discovery.</strong> Monitoring systems should understand object type, counter width, discontinuity behavior, and indexing rather than treating every integer as a generic sensor.</li> <li><strong>Counter discontinuities must be handled explicitly.</strong> Reboots, management-process restarts, and interface reinitialization can invalidate naive delta calculations.</li> <li><strong>64-bit interface counters are essential at modern speeds.</strong> Counter32 objects can wrap too quickly for reliable bandwidth computation on high-capacity links.</li> <li><strong>SNMPv3 security is message-oriented.</strong> Authentication and privacy are provided by the SNMP security model rather than by TCP session security.</li> <li><strong>Notification support is optional in practice.</strong> Polling can provide complete periodic observability even when a device does not originate traps, but polling intervals determine detection latency.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <p>Design SNMP monitoring as a controlled management-plane service rather than a convenience feature. Place management interfaces in dedicated networks, permit queries only from authorized monitoring systems, and restrict SNMP views to the minimum required OID subtrees.</p> <p>Prefer <strong>SNMPv3 authPriv</strong> where supported. Use strong authentication and privacy algorithms available on both manager and agent, and avoid SNMPv1 or SNMPv2c across untrusted networks because community strings do not provide modern confidentiality or message authentication.</p> <p>Use standard interface MIB objects for portable health checks, especially operational state, error counters, discard counters, last-change timestamps, and 64-bit octet counters. Supplement them with enterprise MIB objects only when the required telemetry is not standardized.</p> <p>Finally, treat the monitoring pipeline itself as an observable system. Capture SNMP traffic during failures, track poll duration and timeout rates, distinguish transport loss from authorization errors, and correlate counter resets with uptime or interface-state transitions. A reliable SNMP deployment is not defined by whether an OID can be queried once; it is defined by whether the resulting telemetry remains interpretable under packet loss, device restarts, counter rollover, security changes, and management-plane faults.</p>]]></content><author><name></name></author><category term="Networking"/><category term="snmp"/><category term="network-monitoring"/><category term="observability"/><summary type="html"><![CDATA[A standards-based guide to SNMP polling, MIB/OID structure, SNMPv3 security, interface counters, and reliable switch observability.]]></summary></entry><entry><title type="html">SIP ALG, Conntrack State, and Persistent NAT Rewriting</title><link href="https://harshvermac5.github.io/learn/2026/sip-alg-conntrack-state/" rel="alternate" type="text/html" title="SIP ALG, Conntrack State, and Persistent NAT Rewriting"/><published>2026-09-05T21:02:00+00:00</published><updated>2026-09-05T21:02:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/sip-alg-conntrack-state</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/sip-alg-conntrack-state/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>SIP over UDP is unusually sensitive to the boundary between <strong>Layer 3/4 NAT state</strong> and <strong>Layer 7 signaling state</strong>. A firewall can successfully pass packets in both directions while SIP registration still fails because an application-layer gateway, or ALG, rewrites addresses or ports embedded inside SIP headers or SDP bodies.</p> <p>The central mechanism is <strong>state persistence</strong>. Stateful firewalls classify traffic into connection-tracking entries, and protocol helpers may become metadata on those entries. If helper policy changes after a flow already exists, the existing entry can continue to use the old helper association until that entry expires or is explicitly removed. Continuous SIP retransmission or keep-alive traffic can therefore preserve stale processing longer than an operator expects.</p> <p>The practical lesson is that <strong>configuration state and active flow state are separate</strong>. Changing helper policy does not necessarily mutate an existing connection.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>SIP NAT traversal and stateful connection tracking</strong>: specifically, the interaction among SIP response routing, UDP NAT mappings, Netfilter-style conntrack state, and SIP ALG payload rewriting.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>SIP signaling is defined by <strong>RFC 3261, Session Initiation Protocol, 2002</strong>. SIP messages carry routing information inside the application payload, especially <strong>Via</strong> and <strong>Contact</strong> headers. RFC 3261 explains that the Via header <a href="https://www.rfc-editor.org/rfc/rfc3261.html#section-8.1.1.7"><code class="language-plaintext highlighter-rouge">"identifies the location where the response is to be sent"</code></a>. This creates tension with NAT because the endpoint may place a private address or pre-NAT port in the SIP message while the IP/UDP packet is translated to a different public tuple.</p> <p><strong>RFC 3581, An Extension to SIP for Symmetric Response Routing, 2003</strong>, introduced the <strong><code class="language-plaintext highlighter-rouge">rport</code></strong> parameter to address this mismatch. It allows a client to request that the server return a response to the packet source tuple actually observed on the wire. The RFC states that <code class="language-plaintext highlighter-rouge">rport</code> lets the server send the response <a href="https://www.rfc-editor.org/rfc/rfc3581.html#section-1"><code class="language-plaintext highlighter-rouge">"back to the source IP address and port where the request came from"</code></a>.</p> <p>NAT behavior for UDP is standardized operationally by <strong>RFC 4787, NAT Behavioral Requirements for Unicast UDP, 2007</strong>. It requires endpoint-independent mapping for compliant NATs and states that a UDP mapping timer generally <strong>must not expire in less than two minutes</strong>, with defined exceptions, while recommending a default of five minutes or more. This matters because periodic SIP traffic can refresh the same mapping indefinitely.</p> <p><strong>RFC 5626, Managing Client-Initiated Connections in SIP, 2009</strong>, defines SIP Outbound and keep-alives, treating the client-created flow as persistent routing state that must survive NAT and firewall boundaries.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A SIP user agent sending a UDP <code class="language-plaintext highlighter-rouge">REGISTER</code> normally creates a five-tuple such as source IP, source port, destination IP, destination port, and protocol. A stateful gateway creates a <strong>conntrack entry</strong> representing both the original and reply directions. NAT metadata is stored alongside that state so that return packets can be translated back to the internal endpoint.</p> <p>A protocol helper adds another dimension. Because SIP carries IP addresses and ports in <strong>Via</strong>, <strong>Contact</strong>, and SDP, an ALG may inspect or alter payload fields and create expectations for related media flows. Linux Netfilter implements this with components such as <code class="language-plaintext highlighter-rouge">nf_conntrack_sip</code> and <code class="language-plaintext highlighter-rouge">nf_nat_sip</code>; the kernel source describes the latter as a <a href="https://github.com/torvalds/linux/blob/master/net/netfilter/nf_nat_sip.c"><code class="language-plaintext highlighter-rouge">"SIP extension for NAT alteration"</code></a>. In nftables, helper assignment becomes connection metadata once attached to a tracked flow.</p> <p>The sequence is therefore:</p> <ul> <li>The first SIP datagram creates a UDP conntrack entry.</li> <li>NAT allocates or reuses an external address/port mapping.</li> <li>A SIP helper rule may attach the <strong><code class="language-plaintext highlighter-rouge">sip</code></strong> helper to the new connection.</li> <li>Later packets matching the same tuple refresh the entry rather than creating a replacement.</li> <li>Reply traffic sets reply-seen state; sustained bidirectional UDP traffic may transition the entry to <strong><code class="language-plaintext highlighter-rouge">ASSURED</code></strong>.</li> <li>SIP retransmissions, re-registration, NAT keep-alives, or other periodic messages continue refreshing the timer.</li> <li>If helper policy is disabled after creation, the existing flow can remain alive with its original helper association until the entry is destroyed.</li> </ul> <p>This explains an important diagnostic paradox: the running configuration can show <strong>SIP helper disabled</strong> while the connection table still shows <strong><code class="language-plaintext highlighter-rouge">helper=sip</code></strong> on an established UDP flow.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The mechanism spans several standards and implementation layers:</p> <ul> <li><strong>RFC 3261 (2002)</strong> defines SIP transactions, registration, Via processing, Contact bindings, retransmission behavior, and response routing.</li> <li><strong>RFC 3581 (2003)</strong> adds <strong><code class="language-plaintext highlighter-rouge">rport</code></strong> and symmetric response routing so UDP responses can follow the observed source address and port through NAT.</li> <li><strong>RFC 4787 (2007)</strong> defines required NAT mapping and filtering behavior for UDP, including mapping timers and refresh behavior.</li> <li><strong>RFC 5389 (2008)</strong> defines STUN, used by later SIP NAT-traversal mechanisms to discover and maintain translated paths.</li> <li><strong>RFC 5626 (2009)</strong> defines SIP Outbound, flow tokens, flow recovery, and keep-alive procedures for clients behind NATs and firewalls.</li> <li><strong>RFC 6223 (2011)</strong> extends SIP keep-alive negotiation and complements the flow-management model introduced by RFC 5626.</li> <li><strong>Linux Netfilter conntrack/nftables</strong> is an implementation reference, not an IETF standard. Its helper model demonstrates how application-aware state can be attached to tracked flows and retained independently of later policy edits.</li> </ul> <p>SIP ALG is not required by RFC 3261. It is an implementation technique for NAT-unaware endpoints; modern deployments generally favor <code class="language-plaintext highlighter-rouge">rport</code>, STUN, persistent outbound flows, or TLS over transparent payload rewriting.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>Consider a SIP endpoint behind NAT using UDP/5060. A simplified request may contain:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>REGISTER sip:voice.example.net SIP/2.0
Via: SIP/2.0/UDP 10.20.30.40:5060;rport;branch=z9hG4bK-1
Contact: &lt;sip:user@10.20.30.40:5060&gt;
</code></pre></div></div> <p>The packet may leave the NAT as <code class="language-plaintext highlighter-rouge">198.51.100.25:62014 -&gt; 203.0.113.50:5060</code>. With <code class="language-plaintext highlighter-rouge">rport</code>, a compliant server can reply to <code class="language-plaintext highlighter-rouge">198.51.100.25:62014</code> instead of trusting the private address embedded by the client.</p> <p>On a Linux stateful gateway, an illustrative conntrack entry might look like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>udp 17 118 src=10.20.30.40 dst=203.0.113.50 sport=5060 dport=5060 \
    packets=840 bytes=512400 \
    src=203.0.113.50 dst=198.51.100.25 sport=5060 dport=62014 \
    packets=120 bytes=74400 [ASSURED] helper=sip
</code></pre></div></div> <p>The asymmetry does not prove packet loss by itself. Reply counters prove that <strong>some reverse traffic reached conntrack</strong>. An <code class="language-plaintext highlighter-rouge">[ASSURED]</code> UDP entry likewise reflects connection-tracking state, not successful SIP registration.</p> <p>Helper assignment can be inspected with nftables metadata, and active flows can be listed using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conntrack <span class="nt">-L</span> <span class="nt">-p</span> udp <span class="nt">--dport</span> 5060 <span class="nt">-o</span> extended
</code></pre></div></div> <p>If policy has been changed and a stale entry must be invalidated for a controlled test, the relevant flow can be removed:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conntrack <span class="nt">-D</span> <span class="nt">-p</span> udp <span class="nt">--dport</span> 5060
</code></pre></div></div> <p>The Netfilter conntrack-tools manual explicitly supports listing, updating, and deleting in-kernel flow entries. Removing the entry forces the next SIP packet to create a new connection that is evaluated against the <strong>current</strong> helper-assignment policy.</p> <p>Paired captures across the NAT boundary are equally useful. A response visible on WAN but absent on LAN points toward translation or helper processing; a response visible on both sides shifts analysis toward SIP transaction and endpoint behavior.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Packet filtering and SIP correctness are different questions.</strong> An established-state firewall rule can accept a return packet while Layer 7 rewriting still makes the SIP transaction unusable.</li> <li><strong>Conntrack state outlives configuration edits.</strong> Policy changes generally govern new flow creation; persistent UDP traffic can preserve an old connection long enough to hide the effect of a new configuration.</li> <li><strong><code class="language-plaintext highlighter-rouge">ASSURED</code> is not application success.</strong> It indicates durable bidirectional conntrack activity, not a <code class="language-plaintext highlighter-rouge">200 OK</code>, successful authentication, or a valid registrar binding.</li> <li><strong>SIP embeds topology information in payloads.</strong> Any mismatch among packet tuples, Via, Contact, SDP, and NAT mappings creates opportunities for asymmetric signaling or media failure.</li> <li><strong>Retransmission can preserve the failure mechanism.</strong> Continuous <code class="language-plaintext highlighter-rouge">REGISTER</code> retries may keep both the NAT mapping and stale helper state alive, preventing natural re-evaluation.</li> <li><strong>Response counters are high-value evidence.</strong> If conntrack records reply packets, a simple WAN firewall-drop hypothesis becomes weaker and analysis should move inward toward translation and application semantics.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <p>Prefer standards-based NAT traversal when supported: enable <strong><code class="language-plaintext highlighter-rouge">rport</code></strong>, use SIP Outbound or suitable keep-alives, and have registrars route responses using observed transport information.</p> <p>Treat connection tracking as operational state. After changing helper or NAT policy, verify both the ruleset and live conntrack table; expire relevant entries during controlled tests when immediate re-evaluation is required.</p> <p>Avoid unnecessary ALGs. Moving a gateway from packet forwarding into protocol parsing and payload modification increases failure modes, complicates encryption, and reduces troubleshooting determinism.</p> <p>Finally, diagnose SIP failures layer by layer: confirm DNS resolution, packet egress, return traffic, NAT translation, conntrack helper state, SIP header correctness, transaction matching, and registrar response. The most important distinction is simple: <strong>a flow can be permitted by the firewall and still be broken by stateful application-layer processing.</strong></p>]]></content><author><name></name></author><category term="Networking"/><category term="sip"/><category term="conntrack"/><category term="nat"/><summary type="html"><![CDATA[How SIP application-layer gateways interact with UDP connection tracking, NAT mappings, response routing, and stale helper state.]]></summary></entry><entry><title type="html">Why Domain-Based Policy Routing Fails When DNS Bypasses the Gateway</title><link href="https://harshvermac5.github.io/learn/2026/domain-policy-routing-dns-observation/" rel="alternate" type="text/html" title="Why Domain-Based Policy Routing Fails When DNS Bypasses the Gateway"/><published>2026-09-05T20:45:00+00:00</published><updated>2026-09-05T20:45:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/domain-policy-routing-dns-observation</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/domain-policy-routing-dns-observation/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>Domain-based policy routing appears simple: define a hostname such as <code class="language-plaintext highlighter-rouge">service.example</code>, associate it with a secondary uplink, and expect every connection to that service to leave through that uplink. The hidden difficulty is that routers forward IP packets, not domain names. Once an application resolves a name, the packet carries only source and destination IP addresses; the original DNS name is absent from the IPv4 or IPv6 header.</p> <p>Many implementations bridge that semantic gap by observing DNS responses and translating domain names into a temporary set of destination addresses. The forwarding rule then matches the resulting IP set. This creates a dependency that is easy to miss: <strong>the router must observe the DNS transaction before it can classify the later IP flow</strong>.</p> <p>The objective is to understand this coupling and why external or encrypted DNS, caches, and CDN address rotation can leave an apparently valid policy with nothing to match.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>DNS-Assisted Policy-Based Routing and Multi-WAN Traffic Classification</strong></p> <p>This mechanism combines DNS resolution, packet classification, policy routing, connection tracking, and NAT. It is an implementation architecture built on standard DNS and IP forwarding behavior, not a standalone IETF routing protocol.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>DNS is defined primarily by <strong>RFC 1034</strong> and <strong>RFC 1035</strong>, published in 1987 as Internet Standard <strong>STD 13</strong>. DNS separates names from network-layer addresses. RFC 1035 states that <a href="https://www.rfc-editor.org/rfc/rfc1035.html#section-2.1">“The goal of domain names is to provide a mechanism for naming resources”</a>. A resolver returns A or AAAA records, and applications then communicate using those addresses.</p> <p>An ordinary IP packet does not carry the DNS name that selected its destination. <strong>RFC 1812</strong> defines IPv4 router requirements around IP forwarding, so domain-aware routing requires an additional classifier outside normal destination lookup.</p> <p>A gateway DNS proxy is also a recognized architecture. <strong>RFC 5625, BCP 152, 2009</strong> notes that <a href="https://www.rfc-editor.org/rfc/rfc5625.html#section-1">“The proxy serves as a convenient default DNS resolver for clients on the LAN”</a>. A policy-routing implementation can reuse that observation point to associate DNS answers with configured domains. This domain-to-address classifier is <strong>not standardized by DNS RFCs</strong>.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A DNS-assisted policy-routing pipeline typically operates as follows.</p> <ul> <li>The client sends an A or AAAA query to the gateway DNS proxy or forwarder.</li> <li>The gateway observes the response and extracts relevant A/AAAA records, including applicable CNAME-derived addresses.</li> <li>Those addresses enter a <strong>dynamic destination set</strong>, usually with lifetimes related to DNS TTLs.</li> <li>Later packets are matched against that set.</li> <li>A match applies a policy selector such as a mark or alternate routing table.</li> <li>Policy lookup chooses the alternate WAN next hop, while NAT and connection tracking retain flow-specific egress state.</li> </ul> <p>Conceptually, the DNS-learning stage resembles:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>on_dns_response(qname, answers):
    if qname matches configured_domain:
        for address_record in answers:
            destination_set.insert(
                address_record.address,
                timeout=address_record.ttl
            )
</code></pre></div></div> <p>If a client sends DNS directly to an external resolver, the local proxy never observes the response in the context required by the classifier. The destination set remains empty, so a valid rule can still match <strong>zero packets</strong>.</p> <p>A fail-closed or “kill switch” control does not fix this. It acts <strong>after a packet matches a policy</strong>; a classification miss falls through to normal forwarding before that logic matters.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The standards define the building blocks rather than domain routing itself.</p> <ul> <li><strong>RFC 1034 and RFC 1035, STD 13, 1987</strong> define DNS concepts, resolver behavior, resource records, caching, and query/response processing.</li> <li><strong>RFC 5625, BCP 152, 2009</strong> provides DNS proxy implementation guidance for gateway devices.</li> <li><strong>RFC 1812, Proposed Standard, 1995</strong> defines core IPv4 router forwarding requirements.</li> <li><strong>RFC 7858, Proposed Standard, 2016</strong> defines DNS over TLS. Encryption prevents an ordinary intermediate observer from reading the DNS transaction.</li> <li><strong>RFC 8310, Proposed Standard, 2018</strong> updates operational privacy profiles for DNS over TLS and DTLS.</li> <li><strong>RFC 8484, Proposed Standard, 2018</strong> defines DNS over HTTPS. Its architecture explicitly states that <a href="https://www.rfc-editor.org/rfc/rfc8484.html#section-1">“Each DNS query-response pair is mapped into an HTTP exchange”</a>, which can remove DNS visibility from a gateway classifier.</li> <li><strong>RFC 1918, BCP 5, 1996</strong> defines private IPv4 address space commonly seen on internal or upstream-NAT interfaces.</li> <li><strong>RFC 3022, Informational, 2001</strong> describes traditional NAT and NAPT behavior relevant to multi-WAN egress translation.</li> <li><strong>RFC 5737, Informational, 2010</strong> reserves IPv4 prefixes used below for documentation examples.</li> </ul> <p>No IEEE switching standard directly governs this mechanism because the decision is primarily a Layer-3 forwarding and DNS-classification function rather than an Ethernet control-plane behavior.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>A Linux reference implementation illustrates the forwarding portion. Assume WAN1 uses <code class="language-plaintext highlighter-rouge">192.0.2.1</code>, WAN2 uses <code class="language-plaintext highlighter-rouge">198.51.100.1</code>, and the DNS observer learned <code class="language-plaintext highlighter-rouge">203.0.113.40</code>.</p> <p>A dynamic nftables set can represent learned destinations:</p> <pre><code class="language-nft">set domain_wan2_v4 {
    type ipv4_addr
    flags timeout
}
</code></pre> <p>The observer inserts the address using a lifetime derived from DNS state:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nft add element inet pbr domain_wan2_v4 <span class="s1">'{ 203.0.113.40 timeout 300s }'</span>
</code></pre></div></div> <p>Packet classification can then mark matching traffic:</p> <pre><code class="language-nft">ip daddr @domain_wan2_v4 meta mark set 0x2
</code></pre> <p>Linux policy routing can map that mark to a secondary table:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip rule add fwmark 0x2 lookup 200
ip route add default via 198.51.100.1 dev wan2 table 200
</code></pre></div></div> <p>If the destination set is empty, no mark is applied and the main table remains authoritative. Diagnostic evidence therefore should test the entire chain rather than only the configured rule:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DNS query observed by gateway:     yes/no
A/AAAA address learned:             yes/no
Destination present in dynamic set: yes/no
Packet counter on classifier:       increasing/static
Policy mark applied:                yes/no
Alternate FIB selected:             yes/no
Egress interface:                   wan1/wan2
</code></pre></div></div> <p>Path testing can expose the resulting forwarding choice. A simplified pre-fix trace might show WAN1:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1  10.0.0.1
2  192.0.2.1
</code></pre></div></div> <p>After DNS visibility and policy matching are restored, a fresh connection might show WAN2:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1  10.0.0.1
2  198.51.100.1
</code></pre></div></div> <p>Existing sessions may remain on the original path because connection tracking, NAT state, HTTP reuse, and QUIC can outlive a browser tab. Test with fresh DNS resolution and a new transport flow.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Domain routing is really address routing with a DNS-fed classifier.</strong> The configured hostname is metadata used to maintain an IP set; packets are still forwarded by IP.</li> <li><strong>Resolver placement becomes a routing dependency.</strong> Direct external DNS can bypass the observation point even though ordinary Internet connectivity remains healthy.</li> <li><strong>Encrypted DNS changes observability by design.</strong> DoH and DoT improve confidentiality but can conflict with middlebox features that depend on inspecting DNS answers.</li> <li><strong>Caching creates temporal state.</strong> The OS, application, recursive resolver, gateway, classifier, and connection tracker can all hold state with different lifetimes.</li> <li><strong>CDNs make domain-to-IP mapping non-deterministic.</strong> A hostname may return multiple addresses, change by geography, use short TTLs, or share addresses with unrelated hostnames.</li> <li><strong>Double NAT does not inherently break outbound policy routing.</strong> A secondary WAN may itself use private addressing behind an upstream NAT and still provide a valid outbound next hop. The larger impact is on inbound reachability and port mapping.</li> <li><strong>Static IP policies are more deterministic but less semantic.</strong> They avoid DNS-observation failure but are fragile with rotating CDN or anycast addresses.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <p>Treat the DNS path as part of the forwarding architecture.</p> <ul> <li>Ensure clients intended to use DNS-derived routing send resolution through the observation point that maintains the policy’s destination set.</li> <li>Account for manually configured resolvers, VPN-provided DNS, browser DoH, operating-system encrypted DNS, and privacy relay services.</li> <li>Monitor dynamic-set population and rule counters, not merely configuration presence.</li> <li>Align learned-address lifetimes with DNS TTL behavior and remove stale entries predictably.</li> <li>Test with fresh DNS state and new connections so old cache, NAT, or connection-tracking entries do not mask the result.</li> <li>Prefer IP or prefix policy when the destination set is stable and strict deterministic routing matters more than hostname semantics.</li> <li>For large CDN-backed applications, treat domain-based routing as a best-effort classifier unless the architecture provides an authoritative application-aware control point.</li> <li>Document the privacy trade-off explicitly. Requiring gateway-visible DNS can conflict with encrypted-DNS objectives, so the network design should choose intentionally between observability-driven policy and end-to-end resolver privacy.</li> </ul> <p>The central principle is simple but consequential: <strong>a domain-based routing rule can only classify addresses it has learned</strong>. When DNS resolution bypasses the learner, the routing policy does not fail at the forwarding table; it fails one stage earlier, at classification.</p>]]></content><author><name></name></author><category term="Networking"/><category term="dns"/><category term="policy-routing"/><category term="multi-wan"/><summary type="html"><![CDATA[A deep technical analysis of DNS-observed policy routing, dynamic destination sets, encrypted DNS bypass, and deterministic multi-WAN forwarding design.]]></summary></entry><entry><title type="html">Site-to-Site WireGuard Through NAT: Cryptokey Routing, Policy Routes, and Firewall State</title><link href="https://harshvermac5.github.io/learn/2026/wireguard-site-to-site-nat-routing/" rel="alternate" type="text/html" title="Site-to-Site WireGuard Through NAT: Cryptokey Routing, Policy Routes, and Firewall State"/><published>2026-09-05T20:42:00+00:00</published><updated>2026-09-05T20:42:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/wireguard-site-to-site-nat-routing</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/wireguard-site-to-site-nat-routing/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>A gateway-to-gateway WireGuard VPN can be fully established while inter-site traffic still fails. The handshake proves peer authentication and UDP reachability, but it does not prove that either gateway has the correct routes, that the peer authorizes the routed prefixes, or that the forwarding firewall permits decrypted traffic to cross from the tunnel into the LAN.</p> <p>The core principle is that <strong>WireGuard separates peer identity, cryptokey routing, operating-system routing, NAT traversal, and firewall policy</strong>. These mechanisms cooperate, but they are not interchangeable. The goal is to build a routed tunnel between private LANs while normal Internet traffic continues to use each site’s local WAN.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Route-based VPN design, WireGuard cryptokey routing, UDP NAT traversal, policy routing, and stateful firewall forwarding.</strong></p> <p>This architecture is useful for branch connectivity, backup networks, and management overlays where one or both VPN gateways sit behind upstream NAT.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>WireGuard itself is not defined by an IETF standards-track RFC. Its authoritative behavior is documented by the WireGuard project and technical whitepaper. The project summarizes the transport model simply: <a href="https://www.wireguard.com/">“WireGuard securely encapsulates IP packets over UDP.”</a></p> <p>Its central abstraction is <strong>Cryptokey Routing</strong>. Each peer public key is associated with IP prefixes. For outbound traffic, the packet destination selects the peer whose allowed prefix matches. For inbound traffic, the decrypted packet’s source must be valid for the authenticated peer. Peer identity and permitted layer-3 addresses are therefore tightly coupled.</p> <p>The underlying primitives are standardized separately: <strong>X25519</strong> in <a href="https://www.rfc-editor.org/rfc/rfc7748.html">RFC 7748 (2016)</a>, <strong>ChaCha20-Poly1305</strong> in <a href="https://www.rfc-editor.org/rfc/rfc8439.html">RFC 8439 (2018)</a>, <strong>BLAKE2</strong> in <a href="https://www.rfc-editor.org/rfc/rfc7693.html">RFC 7693 (2015)</a>, and <strong>HKDF</strong> in <a href="https://www.rfc-editor.org/rfc/rfc5869.html">RFC 5869 (2010)</a>. WireGuard uses UDP, standardized by <a href="https://www.rfc-editor.org/rfc/rfc768.html">RFC 768 (1980)</a>. Private LAN prefixes typically come from <a href="https://www.rfc-editor.org/rfc/rfc1918.html">RFC 1918 (1996)</a>, while <a href="https://www.rfc-editor.org/rfc/rfc4787.html">RFC 4787 (2007)</a> defines relevant UDP NAT behavior.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>Assume Site A uses <code class="language-plaintext highlighter-rouge">192.168.10.0/24</code>, Site B uses <code class="language-plaintext highlighter-rouge">192.168.20.0/24</code>, and the WireGuard transit network is <code class="language-plaintext highlighter-rouge">192.168.50.0/24</code>. Site A is reachable through an upstream UDP port forward. Site B initiates from behind NAT.</p> <p>Site A must associate Site B’s public key with both the tunnel address and Site B LAN:</p> <div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[Interface]</span><span class="w">
</span><span class="py">Address</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">192.168.50.1/24</span>
<span class="py">ListenPort</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">51820</span>
<span class="py">PrivateKey</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">&lt;site-a-private-key&gt;</span>
<span class="w">
</span><span class="nn">[Peer]</span><span class="w">
</span><span class="py">PublicKey</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">&lt;site-b-public-key&gt;</span>
<span class="py">AllowedIPs</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">192.168.50.8/32, 192.168.20.0/24</span>
</code></pre></div></div> <p>Site B must associate Site A’s peer with the destinations reachable through Site A:</p> <div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[Interface]</span><span class="w">
</span><span class="py">Address</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">192.168.50.8/32</span>
<span class="py">PrivateKey</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">&lt;site-b-private-key&gt;</span>
<span class="w">
</span><span class="nn">[Peer]</span><span class="w">
</span><span class="py">PublicKey</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">&lt;site-a-public-key&gt;</span>
<span class="py">Endpoint</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">vpn.example.net:51820</span>
<span class="py">AllowedIPs</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">192.168.50.1/32, 192.168.10.0/24</span>
<span class="py">PersistentKeepalive</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">25</span>
</code></pre></div></div> <p>A key design point follows: <strong>changing Site A’s <code class="language-plaintext highlighter-rouge">AllowedIPs</code> for Site B does not inherently change Site B’s private key, tunnel address, endpoint, or Site A public key</strong>. Adding <code class="language-plaintext highlighter-rouge">192.168.20.0/24</code> to Site B’s peer definition on Site A is local Site A state. Generic WireGuard therefore does not require regenerating Site B’s configuration merely because Site A expands the prefixes associated with that peer.</p> <p><code class="language-plaintext highlighter-rouge">AllowedIPs</code>, however, is not a routing protocol. The operating system still needs a route toward the WireGuard interface. Tools such as <code class="language-plaintext highlighter-rouge">wg-quick</code> may create routes automatically, but that is implementation behavior rather than a WireGuard control-plane exchange.</p> <p>A simple route-based design installs only the remote LAN:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip route add 192.168.20.0/24 dev wg0
</code></pre></div></div> <p>The default route remains on the ordinary WAN, preserving local Internet breakout. If only one source host should use the VPN, policy routing can narrow the forwarding decision:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip rule add from 192.168.10.50/32 to 192.168.20.0/24 table 51820 priority 1000
ip route add 192.168.20.0/24 dev wg0 table 51820
</code></pre></div></div> <p>After the kernel selects <code class="language-plaintext highlighter-rouge">wg0</code>, WireGuard still requires the destination to match a peer’s <code class="language-plaintext highlighter-rouge">AllowedIPs</code>.</p> <h2 id="nat-traversal-and-session-state">NAT Traversal and Session State</h2> <p>If Site A is behind an upstream router, that router must translate the public UDP socket to Site A’s private WAN address:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Public_IP:51820/UDP -&gt; Site_A_Private_WAN:51820/UDP
</code></pre></div></div> <p>Site B normally needs no inbound port forward because it initiates the exchange. Once authenticated packets arrive, WireGuard can learn Site B’s current external endpoint.</p> <p>The more subtle problem is idle timeout. NAT mappings expire, while WireGuard intentionally sends nothing when no traffic exists. The WireGuard quick start says that <a href="https://www.wireguard.com/quickstart/">“a sensible interval that works with a wide variety of firewalls is 25 seconds.”</a> For a NATed peer that must remain reachable after long idle periods, <code class="language-plaintext highlighter-rouge">PersistentKeepalive = 25</code> is commonly appropriate.</p> <p>This matters for infrequent jobs such as daily backups. Without keepalives, the responder may remember the peer’s previous endpoint while the NAT device has already deleted the mapping.</p> <p>WireGuard separately rotates cryptographic state. Its whitepaper defines <code class="language-plaintext highlighter-rouge">REKEY-AFTER-TIME</code> as 120 seconds, <code class="language-plaintext highlighter-rouge">REJECT-AFTER-TIME</code> as 180 seconds, <code class="language-plaintext highlighter-rouge">REKEY-TIMEOUT</code> as 5 seconds, and <code class="language-plaintext highlighter-rouge">KEEPALIVE-TIMEOUT</code> as 10 seconds. These protocol timers should not be confused with persistent keepalives used specifically to preserve NAT/firewall state.</p> <h2 id="firewall-and-forwarding-semantics">Firewall and Forwarding Semantics</h2> <p>A successful handshake does not authorize decrypted traffic to enter a LAN. Once decrypted, packets enter the host network stack through the WireGuard interface and must pass ordinary forwarding policy.</p> <p>A stateful firewall can permit Site A to reach Site B while allowing only return traffic in the opposite direction:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nft add rule inet filter forward iifname <span class="s2">"wg0"</span> oifname <span class="s2">"br-lan"</span> <span class="se">\</span>
  ip saddr 192.168.10.0/24 ip daddr 192.168.20.0/24 <span class="se">\</span>
  ct state new,established,related accept

nft add rule inet filter forward iifname <span class="s2">"br-lan"</span> oifname <span class="s2">"wg0"</span> <span class="se">\</span>
  ip saddr 192.168.20.0/24 ip daddr 192.168.10.0/24 <span class="se">\</span>
  ct state established,related accept
</code></pre></div></div> <p>A broad temporary rule is useful for proving the forwarding path, but production policy should be reduced to required source hosts, destinations, protocols, and application ports. Avoid NAT between the private prefixes unless overlapping addressing or deliberate translation requires it; routed VPNs are easier to troubleshoot when original addresses remain visible end to end.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>Troubleshooting should follow the packet path rather than treating “VPN up” as a complete test.</p> <p>Check cryptographic state first:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wg show wg0
</code></pre></div></div> <p>A recent handshake and increasing transfer counters prove encrypted UDP exchange. Then confirm the kernel’s forwarding decision:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip route get 192.168.20.10
ip rule show
</code></pre></div></div> <p>Capture at each layer:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-nni</span> wan0 udp port 51820
tcpdump <span class="nt">-nni</span> wg0 host 192.168.20.10
tcpdump <span class="nt">-nni</span> br-lan host 192.168.20.10
</code></pre></div></div> <p>If UDP is visible on the WAN but no handshake forms, investigate endpoint reachability, forwarding, keys, or peer definitions. If the handshake is current and decrypted packets appear on <code class="language-plaintext highlighter-rouge">wg0</code> but never on the LAN interface, routing or firewall forwarding is the likely failure domain. If requests reach the destination LAN but replies do not return through <code class="language-plaintext highlighter-rouge">wg0</code>, inspect the return route, host gateway, stateful policy, and asymmetric routing.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <ul> <li><strong>RFC 768, User Datagram Protocol, 1980</strong> — transport for WireGuard packets.</li> <li><strong>RFC 1918, Address Allocation for Private Internets, 1996</strong> — private IPv4 addressing.</li> <li><strong>RFC 4787, NAT Behavioral Requirements for Unicast UDP, 2007</strong> — UDP NAT mapping and filtering behavior.</li> <li><strong>RFC 5869, HKDF, 2010</strong> — key derivation primitive.</li> <li><strong>RFC 7693, BLAKE2, 2015</strong> — cryptographic hash primitive.</li> <li><strong>RFC 7748, Elliptic Curves for Security, 2016</strong> — X25519 key agreement.</li> <li><strong>RFC 8439, ChaCha20 and Poly1305 for IETF Protocols, 2018</strong> — authenticated encryption.</li> <li><strong>WireGuard Technical Whitepaper and Protocol Documentation</strong> — cryptokey routing, handshake behavior, and timers.</li> </ul> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Handshake success proves transport and peer authentication, not routed LAN connectivity.</strong></li> <li><strong><code class="language-plaintext highlighter-rouge">AllowedIPs</code> provides outbound peer selection and inbound source-prefix validation.</strong></li> <li><strong>Routes and peer authorization must agree.</strong> A route to <code class="language-plaintext highlighter-rouge">wg0</code> cannot compensate for a missing peer prefix, and an allowed prefix is useless if the kernel never selects <code class="language-plaintext highlighter-rouge">wg0</code>.</li> <li><strong>Server-side prefix association is local state.</strong> Expanding a peer’s remote LAN prefixes does not inherently require regenerating the opposite peer’s configuration.</li> <li><strong>NAT traversal is a liveness problem.</strong> Port forwarding makes a responder reachable; persistent keepalives preserve mappings for idle NATed peers.</li> <li><strong>Selective routing is usually simpler than full-tunnel design.</strong> Install only remote private prefixes and leave <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code> on the local WAN.</li> <li><strong>Firewall classification matters after decryption.</strong> Reason from ingress interface, source, destination, state, and forwarding direction rather than trusting a high-level zone label.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <p>Design from the routing requirements outward. Use non-overlapping prefixes, define which peer owns each remote network, and verify that the operating-system FIB points those networks toward WireGuard. Keep the default Internet route outside the tunnel unless full-tunnel behavior is explicitly required.</p> <p>For NATed peers, document which side owns the stable public endpoint, where UDP forwarding occurs, and whether the opposite peer needs persistent keepalives. Treat a recent handshake as only the first checkpoint. Validate <code class="language-plaintext highlighter-rouge">AllowedIPs</code>, route or policy-rule selection, forwarding rules, return-path symmetry, and captures on both tunnel and LAN interfaces.</p> <p>The strongest site-to-site design is not merely one that establishes a tunnel. It is one where cryptographic identity, route ownership, NAT state, and least-privilege firewall policy all describe the same intended traffic flow.</p>]]></content><author><name></name></author><category term="Networking"/><category term="wireguard"/><category term="vpn"/><category term="nat"/><category term="routing"/><category term="firewall"/><summary type="html"><![CDATA[A technical guide to building selective site-to-site WireGuard connectivity through NAT while preserving local Internet breakout and predictable firewall behavior.]]></summary></entry><entry><title type="html">Inter-VLAN IoT Discovery and Stateful Firewall Isolation</title><link href="https://harshvermac5.github.io/learn/2026/inter-vlan-iot-discovery-firewalling/" rel="alternate" type="text/html" title="Inter-VLAN IoT Discovery and Stateful Firewall Isolation"/><published>2026-09-05T20:05:00+00:00</published><updated>2026-09-05T20:05:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/inter-vlan-iot-discovery-firewalling</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/inter-vlan-iot-discovery-firewalling/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>Segmenting IoT devices into a separate VLAN is a sound security control, but segmentation changes more than the IP subnet. It creates a new Layer 2 broadcast domain and usually introduces a Layer 3 security boundary. Applications that previously discovered and contacted devices on the same LAN may therefore fail even when both networks have working Internet access.</p> <p>The core principle is that <strong>service discovery and application communication are separate traffic phases</strong>. Discovery may depend on broadcast or link-local multicast, while the actual session may use ordinary unicast TCP or UDP. A design can successfully relay discovery traffic yet still block the subsequent session with a firewall policy. Conversely, a permissive inter-VLAN firewall does not make link-local broadcast discovery routable.</p> <p>The engineering objective is to identify each traffic phase, determine its scope and direction, and permit only the flows required by the application.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>VLAN segmentation, multicast service discovery, and stateful inter-VLAN firewall policy</strong>.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>IEEE <strong>802.1Q-2022, Bridges and Bridged Networks</strong> defines VLAN-aware bridging and the logical separation of bridged LANs. A VLAN is effectively an independent broadcast domain: Ethernet frames belonging to one VLAN are not transparently bridged into another VLAN unless an explicit higher-layer function intervenes.</p> <p>IPv4 routing reinforces this boundary. RFC 1812, <em>Requirements for IP Version 4 Routers</em> from 1995, specifies that limited broadcasts remain local to the connected network. Its description is explicit: <a href="https://www.rfc-editor.org/rfc/rfc1812.html">“will not be forwarded outside that network”</a>. This is why a discovery protocol based on IPv4 broadcast cannot simply traverse an inter-VLAN router.</p> <p>Multicast has different semantics. RFC 1112, <em>Host Extensions for IP Multicasting</em> from 1989, defines IPv4 multicast group behavior. Some multicast ranges are intentionally local in scope, and forwarding depends on multicast routing rules rather than ordinary unicast routing.</p> <p>Multicast DNS is even more deliberately constrained. RFC 6762, <em>Multicast DNS</em> from 2013, defines mDNS as DNS-like name resolution on the local link using IPv4 <strong>224.0.0.251</strong> or IPv6 <strong>FF02::FB</strong>, normally over UDP <strong>5353</strong>. The standard describes mDNS as operating <a href="https://www.rfc-editor.org/rfc/rfc6762.html">“on the local link”</a>. RFC 6763, <em>DNS-Based Service Discovery</em> from 2013, builds DNS-SD on DNS resource records such as PTR, SRV, and TXT records and can operate over mDNS for zero-configuration discovery.</p> <p>Firewall behavior is logically separate from VLAN and discovery behavior. RFC 2979, <em>Behavior of and Requirements for Internet Firewalls</em> from 2000, treats a firewall as an enforcement point that screens traffic according to policy. It also emphasizes that firewalling and NAT are distinct functions. Internet reachability therefore proves little about whether two internal security zones can communicate directly.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>Consider a trusted client VLAN and an isolated IoT VLAN connected through a Layer 3 gateway. A typical application workflow can involve several independent exchanges.</p> <p>The first phase is <strong>discovery</strong>. A client may send an Ethernet broadcast, an IPv4 broadcast, an mDNS query to <strong>224.0.0.251:5353</strong>, or some proprietary multicast packet. The switch floods appropriate Layer 2 broadcast or multicast traffic only inside the VLAN. The gateway receives traffic addressed to itself or traffic eligible for routing, but it does not convert ordinary local broadcasts into broadcasts on another VLAN.</p> <p>For mDNS, the client emits a DNS query to the link-local multicast group. Devices on the same link may answer with PTR, SRV, TXT, A, or AAAA records. DNS-SD commonly uses PTR records to enumerate service instances, SRV records to advertise target host and port, and TXT records for service metadata. The discovered endpoint is then contacted using normal unicast traffic.</p> <p>This leads to the second phase: <strong>session establishment</strong>. Suppose discovery returns an IoT device at <code class="language-plaintext highlighter-rouge">192.0.2.50</code> with an application service on TCP port <code class="language-plaintext highlighter-rouge">9000</code>. A client at <code class="language-plaintext highlighter-rouge">198.51.100.25</code> sends a SYN toward <code class="language-plaintext highlighter-rouge">192.0.2.50:9000</code>. The gateway performs a route lookup, then evaluates the packet against inter-zone policy. If the policy permits trusted-to-IoT initiation, the SYN crosses the boundary and a stateful firewall normally records the connection tuple.</p> <p>Return traffic is then matched to the existing state. Common implementations model TCP flows as states such as <strong>NEW</strong> and <strong>ESTABLISHED</strong>, although those names are implementation terminology rather than IETF protocol states. For UDP, a firewall cannot observe a handshake, so it tracks bidirectional packet tuples and expires the pseudo-state after an idle timer.</p> <p>Isolation features may install policy that is stricter than ordinary routing. Some implementations enforce a default deny between isolated and trusted networks even when routes exist. This is why placing interfaces in reachable IP subnets does not imply permitted communication.</p> <p>If discovery succeeds but the session fails, the likely fault domain is the inter-zone firewall or host policy. If discovery fails but direct access by IP succeeds, the likely fault domain is broadcast or multicast scope. If both fail, the engineer must test them independently rather than assuming a single cause.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <ul> <li><strong>IEEE 802.1Q-2022</strong>, <em>IEEE Standard for Local and Metropolitan Area Networks—Bridges and Bridged Networks</em>. Defines VLAN-aware bridges, VLAN identification, forwarding behavior, and bridged-network architecture: <a href="https://standards.ieee.org/ieee/802.1Q/10323/">IEEE 802.1Q</a>.</li> <li><strong>RFC 1812, 1995</strong>, <em>Requirements for IP Version 4 Routers</em>. Defines IPv4 router behavior including broadcast forwarding constraints: <a href="https://www.rfc-editor.org/rfc/rfc1812.html">RFC 1812</a>.</li> <li><strong>RFC 1112, 1989</strong>, <em>Host Extensions for IP Multicasting</em>. Defines IPv4 multicast host-group behavior: <a href="https://www.rfc-editor.org/rfc/rfc1112.html">RFC 1112</a>.</li> <li><strong>RFC 4541, 2006</strong>, <em>Considerations for IGMP and MLD Snooping Switches</em>. Documents multicast forwarding considerations within switched LANs: <a href="https://www.rfc-editor.org/rfc/rfc4541.html">RFC 4541</a>.</li> <li><strong>RFC 6762, 2013</strong>, <em>Multicast DNS</em>. Defines link-local DNS resolution using multicast and UDP 5353: <a href="https://www.rfc-editor.org/rfc/rfc6762.html">RFC 6762</a>.</li> <li><strong>RFC 6763, 2013</strong>, <em>DNS-Based Service Discovery</em>. Defines PTR, SRV, and TXT-based service discovery: <a href="https://www.rfc-editor.org/rfc/rfc6763.html">RFC 6763</a>.</li> <li><strong>RFC 2979, 2000</strong>, <em>Behavior of and Requirements for Internet Firewalls</em>. Describes firewall interoperability and traffic-screening principles: <a href="https://www.rfc-editor.org/rfc/rfc2979.html">RFC 2979</a>.</li> </ul> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>Packet capture should distinguish discovery from application traffic. On a Linux router or observation host, mDNS can be isolated with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-ni</span> any <span class="s1">'udp port 5353 and host 224.0.0.251'</span>
</code></pre></div></div> <p>Ordinary broadcast-heavy discovery can be inspected with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-ni</span> any <span class="s1">'broadcast or multicast'</span>
</code></pre></div></div> <p>After a device is discovered, capture traffic between the client and IoT endpoint:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-ni</span> any <span class="s1">'host 198.51.100.25 and host 192.0.2.50'</span>
</code></pre></div></div> <p>The evidence should answer whether the client sends a SYN or UDP request, whether that packet appears on the IoT-side interface, and whether a response returns. A packet present on the ingress interface but absent on the egress interface strongly implicates gateway policy or routing. A request visible on both sides with no response shifts attention toward the endpoint, service port, or host firewall.</p> <p>A vendor-neutral Linux <code class="language-plaintext highlighter-rouge">nftables</code> policy can express least-privilege inter-VLAN access:</p> <pre><code class="language-nft">table inet filter {
    chain forward {
        type filter hook forward priority 0; policy drop;

        ct state established,related accept

        ip saddr 198.51.100.0/24 ip daddr 192.0.2.50 \
            tcp dport 9000 ct state new accept
    }
}
</code></pre> <p>This permits established return traffic automatically while limiting new trusted-to-IoT sessions to one destination and port. If packet capture proves the IoT device must initiate a separate callback, that direction should receive its own narrowly scoped rule rather than a blanket bidirectional allow.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Discovery reachability is not session reachability.</strong> mDNS reflection can reveal a device while the stateful firewall still blocks its application port.</li> <li><strong>Internet connectivity is not evidence of inter-VLAN permission.</strong> Default routes and NAT can work while east-west policy remains deny-by-default.</li> <li><strong>Broadcast and multicast are not interchangeable.</strong> Broadcast generally terminates at the routed boundary; multicast forwarding depends on group scope, multicast control protocols, and explicit relay or routing behavior.</li> <li><strong>mDNS is intentionally link-local.</strong> Extending it across VLANs requires a reflector, proxy, or gateway service that deliberately republishes selected queries and responses.</li> <li><strong>Direction matters.</strong> A rule allowing trusted clients to initiate sessions toward IoT devices is materially different from allowing IoT devices to initiate arbitrary sessions toward trusted networks.</li> <li><strong>Broad allow rules are diagnostic tools, not final architecture.</strong> They can prove that policy is the blocker, but they should be reduced to specific sources, destinations, protocols, and ports after evidence is collected.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <p>Design IoT segmentation around observable traffic requirements rather than assumptions about how a mobile application behaves. Establish a deny-by-default boundary, then validate discovery and session flows separately with packet captures and state-table evidence.</p> <p>Where service discovery must cross VLANs, prefer narrowly scoped mDNS or DNS-SD reflection over unrestricted multicast forwarding. Avoid attempting to route generic Layer 2 broadcast discovery across security boundaries. For the actual application session, permit only the initiating direction that is required and rely on stateful return handling where appropriate.</p> <p>Finally, treat temporary removal of isolation as a controlled experiment. If communication immediately succeeds, isolation has identified the policy layer responsible, but the correct remediation is not permanent de-segmentation. Re-enable the boundary, capture the successful flow, identify exact endpoints and ports, and implement the smallest policy exception that preserves application functionality without collapsing the trust model.</p>]]></content><author><name></name></author><category term="Networking"/><category term="vlan"/><category term="mdns"/><category term="firewall"/><summary type="html"><![CDATA[A standards-based analysis of why IoT discovery and direct device access fail across VLAN boundaries, and how to design narrowly scoped inter-VLAN firewall policy without defeating segmentation.]]></summary></entry></feed>