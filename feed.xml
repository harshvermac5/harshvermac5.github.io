<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://harshvermac5.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://harshvermac5.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-12T12:36:09+00:00</updated><id>https://harshvermac5.github.io/feed.xml</id><title type="html">Harsh Verma</title><subtitle>Portfolio and learning hub of Harsh Verma, a server and network engineer focused on Linux, data center infrastructure, networking, and automation. </subtitle><entry><title type="html">Why BGP Authentication Fails Before the Session Starts</title><link href="https://harshvermac5.github.io/learn/2026/bgp-tcp-authentication/" rel="alternate" type="text/html" title="Why BGP Authentication Fails Before the Session Starts"/><published>2026-09-11T23:58:00+00:00</published><updated>2026-09-11T23:58:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/bgp-tcp-authentication</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/bgp-tcp-authentication/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>BGP authentication problems are often misdiagnosed as routing-policy or BGP state-machine failures when the real failure occurs one layer lower: during establishment of the underlying TCP connection. BGP-4 uses TCP as its transport, normally with the passive endpoint listening on TCP port <strong>179</strong>. When TCP-level authentication is enabled, protected TCP segments must carry a valid authentication value before BGP can exchange an <strong>OPEN</strong> message.</p> <p>The core principle is that <strong>BGP adjacency establishment depends on successful authenticated TCP establishment first</strong>. A mismatched key, missing authentication option, unsupported mechanism, or failed configuration commit can prevent the TCP three-way handshake from completing. The BGP finite-state machine may then remain in <strong>Connect</strong> or <strong>Active</strong>, with no BGP NOTIFICATION because no BGP protocol exchange ever began.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>BGP Session Authentication and TCP Control-Plane Security</strong></p> <p>The mechanism spans BGP-4 session establishment, TCP MD5 authentication, TCP-AO, and configuration-plane behavior in systems that translate declarative configuration into routing-daemon and kernel socket state.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>BGP-4 is defined by <a href="https://www.rfc-editor.org/rfc/rfc4271"><strong>RFC 4271 — A Border Gateway Protocol 4 (BGP-4), 2006</strong></a>. Its finite-state machine includes <strong>Idle</strong>, <strong>Connect</strong>, <strong>Active</strong>, <strong>OpenSent</strong>, <strong>OpenConfirm</strong>, and <strong>Established</strong>. BGP does not begin exchanging protocol messages until TCP connectivity exists. RFC 4271 requires implementations to establish TCP connectivity to peers and listen for incoming BGP connections on TCP port 179.</p> <p>RFC 4271 also requires support for the TCP MD5 option defined in RFC 2385, reflecting the operational practice of its era.</p> <p><a href="https://www.rfc-editor.org/rfc/rfc2385"><strong>RFC 2385 — Protection of BGP Sessions via the TCP MD5 Signature Option, 1998</strong></a> defines the original TCP-level authentication mechanism for BGP. Its purpose is stated directly: <a href="https://www.rfc-editor.org/rfc/rfc2385">“The primary motivation for this option is to allow BGP to protect itself against the introduction of spoofed TCP segments”</a>. The primary threat was forged TCP traffic, particularly spoofed resets capable of terminating long-lived BGP sessions.</p> <p>TCP MD5 was later obsoleted by <a href="https://www.rfc-editor.org/rfc/rfc5925"><strong>RFC 5925 — The TCP Authentication Option, 2010</strong></a>. TCP-AO adds stronger message authentication, algorithm agility, replay protection, and coordinated key rollover. The standard explicitly says TCP-AO <a href="https://www.rfc-editor.org/rfc/rfc5925">“obsoletes the TCP MD5 Signature option of RFC 2385”</a>.</p> <p><a href="https://www.rfc-editor.org/rfc/rfc7454"><strong>RFC 7454 / BCP 194 — BGP Operations and Security, 2015</strong></a> recommends preferring TCP-AO when both implementations support it, while recognizing the continued deployment of TCP MD5.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A BGP session starts with ordinary TCP establishment. One speaker sends a SYN toward TCP port 179, the peer replies with SYN-ACK, and the initiator completes the handshake with ACK. Only after TCP succeeds can BGP move toward <strong>OpenSent</strong> and exchange OPEN messages containing BGP version, autonomous-system information, hold time, BGP identifier, and optional parameters.</p> <p>With TCP MD5 enabled, authentication applies to the TCP segments themselves. RFC 2385 defines TCP option <strong>Kind 19</strong>, length 18 bytes: two bytes for Kind and Length plus a 16-byte MD5 digest.</p> <p>The digest is calculated over:</p> <ul> <li>the TCP pseudo-header, including source and destination IP addresses, protocol number, and TCP length;</li> <li>the TCP header, excluding TCP options and with the checksum treated as zero;</li> <li>the TCP payload, if present; and</li> <li>a shared secret known to both endpoints.</li> </ul> <p>Because IP addresses participate in the digest, authentication is bound to the expected connection context. Unexpected NAT, an incorrect source interface, or peering from a different address can invalidate signatures even when the configured key appears correct.</p> <p>RFC 2385 defines the critical failure behavior: if the receiver calculates a different digest, the segment is discarded and no response is sent. This intentional silent discard prevents unauthenticated traffic from influencing the protected connection.</p> <p>Operationally, that means a peer using the wrong key can send repeated SYN packets without receiving a SYN-ACK. From the BGP process perspective, the failure looks like inability to establish TCP. No BGP NOTIFICATION is expected because BGP has not reached the message-exchange stage.</p> <p>TCP-AO preserves this layering but improves the security model. It uses TCP option <strong>Kind 29</strong>, introduces key identifiers, supports stronger MAC algorithms, and permits planned key transitions. TCP-AO and TCP MD5 cannot protect the same TCP connection simultaneously.</p> <p>A further complication is the configuration plane. A controller can accept syntactically valid BGP input yet fail while translating it into runtime socket state. Conversely, a routing daemon may store an authentication directive even when applying it to an existing socket requires session recreation. Engineers should therefore distinguish <strong>configuration parsing</strong>, <strong>configuration commit</strong>, <strong>runtime application</strong>, and <strong>protocol establishment</strong>.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <ul> <li><a href="https://www.rfc-editor.org/rfc/rfc4271"><strong>RFC 4271 — BGP-4, 2006</strong></a>: BGP session establishment, TCP transport, and finite-state machine behavior.</li> <li><a href="https://www.rfc-editor.org/rfc/rfc2385"><strong>RFC 2385 — TCP MD5 Signature Option, 1998</strong></a>: MD5-based TCP authentication originally developed for BGP.</li> <li><a href="https://www.rfc-editor.org/rfc/rfc5925"><strong>RFC 5925 — TCP Authentication Option, 2010</strong></a>: successor to TCP MD5 with stronger cryptography, replay protection, and key rollover support.</li> <li><a href="https://www.rfc-editor.org/rfc/rfc7454"><strong>RFC 7454 / BCP 194 — BGP Operations and Security, 2015</strong></a>: operational guidance for protecting BGP speakers and sessions.</li> <li><a href="https://www.rfc-editor.org/rfc/rfc5082"><strong>RFC 5082 — Generalized TTL Security Mechanism, 2007</strong></a>: GTSM, commonly paired with session authentication to reduce exposure to remotely spoofed BGP packets.</li> </ul> <p>These controls are complementary. TCP authentication protects the transport session, GTSM constrains plausible packet origin, and route policy controls what an authenticated peer is allowed to advertise.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>A standards-oriented configuration can be represented abstractly as follows. Exact syntax varies by implementation:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>local-as 65000
router-id 192.0.2.10

neighbor 192.0.2.11 {
    remote-as 65001
    tcp-authentication md5 "shared-secret"
}
</code></pre></div></div> <p>The peer must use the same authentication mechanism and key. A mismatch may produce a trace resembling:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>192.0.2.10:49152 &gt; 192.0.2.11:179  SYN  [TCP MD5 option]
192.0.2.10:49152 &gt; 192.0.2.11:179  SYN retransmission
192.0.2.10:49152 &gt; 192.0.2.11:179  SYN retransmission
</code></pre></div></div> <p>A useful capture filter is:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-ni</span> eth0 <span class="s1">'tcp port 179'</span>
</code></pre></div></div> <p>Repeated authenticated SYNs without SYN-ACK are significant. If IP reachability, ACLs, and TCP/179 are otherwise correct, the receiving endpoint may be silently discarding the segment because authentication failed.</p> <p>The diagnostic order should follow the protocol stack. Confirm that TCP completes before focusing on BGP OPEN parameters. If the handshake succeeds but the session fails afterward, inspect OPEN, KEEPALIVE, and NOTIFICATION messages. If it never completes, investigate transport reachability, filtering, source addresses, TTL policy, and authentication first.</p> <p>RFC 2385 provides the decisive evidence: <a href="https://www.rfc-editor.org/rfc/rfc2385">“A failing comparison must result in the segment being dropped and must not produce any response back to the sender.”</a></p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>BGP authentication failure usually appears as a TCP failure first.</strong> Do not expect a BGP NOTIFICATION when authenticated TCP never establishes.</li> <li><strong>A valid configuration file is not proof of successful runtime application.</strong> Parsing, committing, applying socket options, and protocol establishment are separate checkpoints.</li> <li><strong>Authentication must be symmetric.</strong> Both peers must agree on mechanism and key.</li> <li><strong>TCP MD5 is connection-sensitive.</strong> IP-layer information contributes to the digest, so unexpected addressing or translation can invalidate signatures.</li> <li><strong>Silent packet discard is expected.</strong> SYN retransmissions may indicate authentication rejection rather than ordinary loss.</li> <li><strong>TCP-AO is the stronger architectural choice.</strong> It addresses MD5’s lack of algorithm agility, replay protection, and coordinated key rollover.</li> <li><strong>Session authentication does not replace routing policy.</strong> An authenticated peer can still advertise incorrect routes; prefix filters, maximum-prefix controls, AS-path policy, and control-plane protection remain necessary.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Validate BGP in layers: confirm IP reachability, TCP/179 accessibility, authenticated TCP establishment, BGP OPEN exchange, then routing-policy behavior.</li> <li>Configure authentication consistently on both peers and coordinate key changes to avoid unnecessary session loss.</li> <li>Prefer TCP-AO where both endpoints support it; otherwise use TCP MD5 with strong, non-reused secrets and appropriate operational controls.</li> <li>Combine session authentication with GTSM where appropriate, anti-spoofing filters, control-plane policing, and strict route policy.</li> <li>Capture traffic on both sides when possible. Distinguishing <strong>no SYN received</strong>, <strong>SYN silently discarded</strong>, <strong>successful TCP followed by BGP NOTIFICATION</strong>, and <strong>Established with policy issues</strong> sharply narrows the fault domain.</li> <li>Treat configuration-management errors as evidence about the control plane, not proof that BGP itself is defective. Verify what reached the routing daemon and kernel before drawing protocol-level conclusions.</li> </ul> <p>The central principle is operationally powerful: <strong>BGP authentication protects the TCP transport that BGP depends on, so authentication failures can prevent the BGP protocol from starting at all</strong>. Troubleshooting becomes faster when TCP establishment, authentication, and the BGP finite-state machine are analyzed as separate stages rather than as one undifferentiated “BGP down” condition.</p>]]></content><author><name></name></author><category term="Networking"/><category term="bgp"/><category term="tcp"/><category term="routing-security"/><summary type="html"><![CDATA[A deep technical guide to BGP session authentication, TCP MD5, TCP-AO, and why authentication failures often occur before the BGP finite-state machine can exchange OPEN messages.]]></summary></entry><entry><title type="html">Dynamic DNS Change Detection: Why a Client May Correctly Refuse to Update</title><link href="https://harshvermac5.github.io/learn/2026/dynamic-dns-change-detection/" rel="alternate" type="text/html" title="Dynamic DNS Change Detection: Why a Client May Correctly Refuse to Update"/><published>2026-09-11T23:17:00+00:00</published><updated>2026-09-11T23:17:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/dynamic-dns-change-detection</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/dynamic-dns-change-detection/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>Dynamic DNS failures are often misdiagnosed as authentication or provider problems when the real issue is <strong>change detection</strong>. A Dynamic DNS client normally does not publish an address simply because it was started. It first determines the address that should be advertised, compares that value with remembered state, and updates the remote service only when policy says an update is required.</p> <p>The core principle is <strong>event-driven synchronization between interface state and DNS state</strong>. A healthy client can establish connectivity, authenticate successfully, and still send no update because its state machine concludes that the advertised address has not changed.</p> <p>This distinction matters on gateways, Linux hosts, edge appliances, and automated infrastructure using dynamically assigned public addresses. A forced update can prove that the provider path works, but it does not prove that automatic change detection works.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Dynamic DNS state synchronization and WAN address change detection</strong></p> <p>The subject sits at the intersection of DNS architecture, interface address management, HTTP-based update APIs, persistent client state, and event-driven automation.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>RFC 1034 and RFC 1035, published in 1987 and collectively identified as <strong>STD 13</strong>, define the DNS namespace, resource records, resolvers, authoritative servers, queries, caching, and zone data model.</p> <p>RFC 2136, published in 1997, extended that model with the <strong>DNS UPDATE</strong> opcode. Its motivation is explicit: <a href="https://www.rfc-editor.org/rfc/rfc2136.html">“The Domain Name System was originally designed to support queries of a statically configured database.”</a> DNS UPDATE allows authorized clients to add or delete resource records without offline zone-file edits. The transaction can carry prerequisites and update operations, and <a href="https://www.rfc-editor.org/rfc/rfc2136.html">“UPDATE is atomic”</a>, so failed prerequisites prevent partial application.</p> <p>Public Dynamic DNS services, however, do not necessarily expose RFC 2136. Many provide an <strong>HTTPS update API</strong> in which a client submits a hostname and current address to a service endpoint. The provider ultimately changes authoritative DNS data, but the client may never construct an RFC 2136 packet.</p> <p>This creates two distinct control planes: the client-side mechanism that decides whether an update is needed, and the provider-side mechanism that commits the mapping.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A typical Dynamic DNS client loads configuration containing a hostname, credentials, address-family policy, provider endpoint, and a method for determining the current address. That address source may be a local interface, an external address-discovery service, or a custom command.</p> <p>When an interface is selected, the client enumerates addresses and filters unusable candidates. A link-local IPv6 address such as <code class="language-plaintext highlighter-rouge">fe80::/10</code> is not suitable as a globally reachable DNS destination. A client configured for IPv4-only publication should likewise reject IPv6 candidates.</p> <p>The open-source Inadyn implementation illustrates this model. Its example configuration describes <code class="language-plaintext highlighter-rouge">iface</code> as <a href="https://github.com/troglobit/inadyn/blob/master/examples/inadyn.conf">“Set interface to check for IP”</a>. If no interface is selected, an implementation may query an external check-address service instead.</p> <p>After discovery, the client compares the detected value with cached state from the previous successful invocation:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>current = discover_publishable_address()
previous = load_cached_address(hostname)

if force_update:
    publish(current)
elif current != previous:
    publish(current)
else:
    suppress_update()
</code></pre></div></div> <p>This local comparison avoids unnecessary provider calls and protects against excessive-update policies. It is deliberately different from querying authoritative DNS before every run.</p> <p>When an update is required, an HTTPS-oriented client resolves the update service, establishes transport, negotiates TLS, authenticates, submits the hostname and address, validates the response, and updates its local cache only after provider success.</p> <p>A forced-update option bypasses only the <strong>change-decision gate</strong>. It should not bypass certificate validation, authentication, request validation, or response handling. Therefore, a successful forced update isolates the problem: the publication path works even if automatic triggering remains suspect.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <ul> <li><strong>RFC 1034, Domain Names - Concepts and Facilities, 1987, STD 13</strong> defines the conceptual DNS architecture.</li> <li><strong>RFC 1035, Domain Names - Implementation and Specification, 1987, STD 13</strong> defines DNS message formats and resource-record behavior.</li> <li><strong>RFC 2136, Dynamic Updates in the Domain Name System, 1997</strong> defines the DNS UPDATE opcode, prerequisites, transaction semantics, and response handling.</li> <li><strong>RFC 3007, Secure Domain Name System Dynamic Update, 2000</strong> updates the security model for authenticated dynamic DNS changes.</li> <li><strong>RFC 7617, The Basic HTTP Authentication Scheme, 2015</strong> defines HTTP Basic authentication and requires an external secure channel for confidentiality.</li> <li><strong>RFC 9110, HTTP Semantics, 2022, STD 97</strong> defines the modern HTTP request-response semantics used by many DDNS APIs.</li> <li><strong>RFC 9846, The Transport Layer Security Protocol Version 1.3, 2026</strong> is the current TLS 1.3 specification for securing HTTPS update sessions.</li> </ul> <p>The architectural caveat is that <strong>RFC 2136 and HTTP-based DDNS APIs are not interchangeable protocols</strong>. RFC 2136 standardizes direct DNS update transactions. Provider APIs standardize only as far as HTTP, authentication, and TLS are concerned; URI structure, parameters, response bodies, and account policies remain service-specific.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>Assume a Linux edge host whose publishable IPv4 address is <code class="language-plaintext highlighter-rouge">203.0.113.10</code>. A diagnostic run might show:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Cached IP: 203.0.113.10
Checking interface wan0
Detected IPv4 address: 203.0.113.10
No IP change detected
Update suppressed
</code></pre></div></div> <p>That is not a provider failure. It means the client reached its decision point and concluded that <code class="language-plaintext highlighter-rouge">current == previous</code>.</p> <p>A forced diagnostic run follows a different branch:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Detected IPv4 address: 203.0.113.10
Forced update requested
Connecting to update.ddns.example:443
TLS certificate validation successful
HTTP request sent
HTTP 200 received
Provider response: good 203.0.113.10
Cache updated
</code></pre></div></div> <p>This proves provider name resolution, outbound routing, TCP connectivity, TLS negotiation, certificate validation, authentication acceptance, request formatting, and provider-side processing.</p> <p>A generic Inadyn-style configuration expresses the same architecture without depending on a commercial service:</p> <div class="language-conf highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iface</span> = <span class="n">wan0</span>

<span class="n">custom</span> <span class="n">update</span>.<span class="n">ddns</span>.<span class="n">example</span> {
    <span class="n">username</span> = <span class="s2">"ddns-user"</span>
    <span class="n">password</span> = <span class="s2">"secret-from-protected-store"</span>
    <span class="n">hostname</span> = <span class="s2">"edge.example.net"</span>
    <span class="n">ddns</span>-<span class="n">server</span> = <span class="n">update</span>.<span class="n">ddns</span>.<span class="n">example</span>
    <span class="n">ddns</span>-<span class="n">path</span> = <span class="s2">"/nic/update?hostname=%h&amp;myip=%i"</span>
    <span class="n">ssl</span> = <span class="n">true</span>
}
</code></pre></div></div> <p>Troubleshooting should compare three independent states: the address configured on the WAN interface, the address remembered by the DDNS client, and the address currently published in DNS. If the interface changed but the client cache did not, investigate address discovery or event triggering. If the client detects the new address but the provider rejects it, investigate transport, credentials, policy, or request syntax. If the provider accepts the update but recursive resolvers still return the old record, investigate authoritative state, TTLs, and caching.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>A successful forced update does not validate automatic triggering.</strong> It validates the publication path after the change-detection gate.</li> <li><strong>“No change” is a state decision, not a network error.</strong> Suppressing a redundant request can be correct behavior.</li> <li><strong>The address source is part of the control plane.</strong> A wrong interface, internal address, link-local address, or stale discovery service can feed bad input into otherwise correct logic.</li> <li><strong>Local cache and authoritative DNS can diverge.</strong> Out-of-band provider changes may not be noticed by a client that only compares against its own last successful state.</li> <li><strong>Rate limiting is a design constraint.</strong> Change-based suppression reduces unnecessary updates and avoids provider throttling.</li> <li><strong>Multi-WAN designs increase complexity.</strong> Address discovery and trigger events must follow the interface that owns the advertised egress identity.</li> <li><strong>Debug output can expose secrets.</strong> Authorization headers, tokens, and generated configuration files require redaction.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Bind each DDNS instance to a verifiable address source when multiple WANs, tunnels, or policy-routing domains exist.</li> <li>Persist the last successfully published address and update that cache only after confirmed provider success.</li> <li>Trigger reevaluation on interface address acquisition, DHCP or PPP lease changes, failover transitions, and configuration changes.</li> <li>Use periodic reconciliation as a safety net, but avoid aggressive forced updates that may trigger rate limits.</li> <li>Validate automatic behavior separately from forced behavior. Test the real address-change event rather than merely editing the remote DNS record.</li> <li>Protect credentials in configuration files, process arguments, logs, and support bundles. When HTTP Basic authentication is used, require TLS and avoid logging the <code class="language-plaintext highlighter-rouge">Authorization</code> header.</li> <li>Troubleshoot in layers: verify address discovery, decision logic, provider transport and authentication, authoritative DNS state, then recursive cache behavior.</li> </ul> <p>Dynamic DNS is fundamentally a <strong>state-synchronization system</strong>, not merely an HTTP request or DNS write. Reliable operation depends on accurate address discovery, correct event generation, disciplined comparison with prior state, authenticated publication, and independent verification that authoritative DNS reflects the intended edge address.</p>]]></content><author><name></name></author><category term="Networking"/><category term="ddns"/><category term="dns"/><category term="networking"/><category term="linux"/><category term="automation"/><summary type="html"><![CDATA[A deep technical analysis of how Dynamic DNS clients detect address changes, suppress redundant updates, and securely publish new DNS mappings.]]></summary></entry><entry><title type="html">LACP Link Aggregation, ARP Identity, and Duplicate-IP Detection</title><link href="https://harshvermac5.github.io/learn/2026/lacp-duplicate-ip-detection/" rel="alternate" type="text/html" title="LACP Link Aggregation, ARP Identity, and Duplicate-IP Detection"/><published>2026-09-11T22:58:00+00:00</published><updated>2026-09-11T22:58:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/lacp-duplicate-ip-detection</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/lacp-duplicate-ip-detection/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>A host with multiple Ethernet interfaces can combine them into one logical link for resilience and aggregate throughput. The architectural requirement is that upper layers treat that aggregation as one attachment, even though frames may traverse different physical ports. This creates an important monitoring problem: a device that correlates an IPv4 address with physical ports or source MAC observations can mistake legitimate link aggregation for duplicate address use.</p> <p>The originating example describes port trunking as <a href="https://www.qnap.com/en/how-to/tutorial/article/set-port-trunking-on-your-qnap-nas-to-increase-the-bandwidth-via-802-3ad-protocol">“allows you to combine multiple LAN interfaces for increased bandwidth and load balancing for multiple clients”</a>. That is the practical symptom, but the deeper principle is the boundary between <strong>logical interface identity</strong>, <strong>LACP member-port identity</strong>, <strong>bridge MAC learning</strong>, and <strong>ARP-based IPv4 identity</strong>.</p> <p>The objective is to understand why a correctly formed Link Aggregation Group, or <strong>LAG</strong>, should behave as a single logical link; why a monitor can still report multiple devices using one IP; and which observations prove a benign heuristic mismatch versus an actual LAG configuration failure.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Ethernet Link Aggregation, LACP State, MAC Forwarding Databases, and IPv4 Address Conflict Detection</strong></p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>The historical <strong>IEEE 802.3ad-2000</strong> amendment defined Ethernet link aggregation. Its function was later moved into the MAC-independent <strong>IEEE 802.1AX</strong> family. The current base standard is <a href="https://standards.ieee.org/ieee/802.1AX/6768/">IEEE 802.1AX-2020</a>, which defines a LAG so that a MAC client can treat multiple parallel point-to-point links as a single link. IEEE summarizes the design as allowing parallel links to be <a href="https://standards.ieee.org/ieee/802.1AX/6768/">“used as if they were a single link”</a>.</p> <p>This distinction matters: <strong>link aggregation</strong> is the architecture; <strong>LACP</strong> is the control protocol used to discover compatible partners and maintain operational state. Static aggregation can exist without LACP.</p> <p>At Layer 3, IPv4 uses <strong>ARP</strong> to map an IP address to a link-layer address. <a href="https://www.rfc-editor.org/info/rfc826/">RFC 826</a> defines the base Address Resolution Protocol. <a href="https://www.rfc-editor.org/info/rfc5227/">RFC 5227</a> extends the model with IPv4 Address Conflict Detection, including ARP Probes, Announcements, and ongoing conflict detection. A conflict detector therefore reasons from observations such as “the same sender protocol address appeared with an unexpected sender hardware address.” It does not inherently understand that several physical links may belong to one logical host attachment.</p> <p>At the switching layer, <a href="https://standards.ieee.org/ieee/802.1Q/10323/">IEEE 802.1Q-2022</a> defines bridge and VLAN behavior, including the MAC-service model on which forwarding database learning is based. A correctly configured switch should treat member links of a LAG as one logical forwarding construct rather than unrelated access paths.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>LACP operates between two systems called the <strong>Actor</strong> and <strong>Partner</strong>. Each candidate aggregation port exchanges <strong>LACPDUs</strong> containing system identity, system priority, operational key, port identity, port priority, and state information. The operational key is central: ports can aggregate only when their characteristics and administrative intent are compatible.</p> <p>A receive state machine processes partner information. Periodic and transmit logic sends LACPDUs. Selection logic determines which aggregator a port may join. The multiplexer, or <strong>MUX</strong>, controls whether the port is attached and whether it may <strong>Collect</strong> inbound traffic and <strong>Distribute</strong> outbound traffic.</p> <p>Operational state includes concepts such as <strong>Activity</strong>, <strong>Timeout</strong>, <strong>Aggregation</strong>, <strong>Synchronization</strong>, <strong>Collecting</strong>, <strong>Distributing</strong>, <strong>Defaulted</strong>, and <strong>Expired</strong>. A physical link can be electrically up while still unusable because LACP has not synchronized it into the active aggregator.</p> <p>Common LACP timer behavior uses a fast periodic interval of approximately <strong>1 second</strong> and a slow interval of approximately <strong>30 seconds</strong>. Loss detection typically follows three missed periods, producing roughly <strong>3-second</strong> short and <strong>90-second</strong> long timeout behavior. The Linux bonding implementation documents these values for standards-oriented <code class="language-plaintext highlighter-rouge">802.3ad</code> mode in the <a href="https://docs.kernel.org/networking/bonding.html">kernel bonding documentation</a>.</p> <p>Once synchronized, frame distribution is generally hash-based. A system may hash Layer-2 addresses, Layer-2 plus Layer-3 fields, or other conversation identifiers. The critical property is conversation consistency: frames from one conversation should not be distributed in a way that creates unacceptable reordering. A two-member 1 Gb/s LAG can therefore provide roughly 2 Gb/s across multiple conversations while one flow usually remains bounded by one member.</p> <p>For IPv4, the IP address should normally be assigned to the <strong>logical bonded interface</strong>, not independently configured on each physical member. The bond presents the host’s Layer-3 identity; member interfaces provide transport underneath it.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <ul> <li><a href="https://standards.ieee.org/ieee/802.3ad/1088/">IEEE 802.3ad-2000</a> — the original Ethernet link-aggregation amendment, published in 2000 and now superseded.</li> <li><a href="https://standards.ieee.org/ieee/802.1AX/4176/">IEEE 802.1AX-2008</a> — the first standalone MAC-independent Link Aggregation standard, published in 2008.</li> <li><a href="https://standards.ieee.org/ieee/802.1AX/6768/">IEEE 802.1AX-2020</a> — the active base Link Aggregation standard, published in 2020.</li> <li><a href="https://standards.ieee.org/ieee/802.1Q/10323/">IEEE 802.1Q-2022</a> — Bridges and Bridged Networks, including VLAN and MAC-service behavior, published in 2022.</li> <li><a href="https://www.rfc-editor.org/info/rfc826/">RFC 826</a> — Ethernet Address Resolution Protocol, published in 1982.</li> <li><a href="https://www.rfc-editor.org/info/rfc5227/">RFC 5227</a> — IPv4 Address Conflict Detection, published in 2008 and updating RFC 826 behavior for probing and conflict handling.</li> </ul> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>On a Linux host, a standards-oriented bond can be created with dynamic aggregation and a Layer-2/Layer-3 transmit hash:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip <span class="nb">link </span>add bond0 <span class="nb">type </span>bond mode 802.3ad
ip <span class="nb">link set </span>bond0 <span class="nb">type </span>bond lacp_rate fast xmit_hash_policy layer2+3
ip <span class="nb">link set </span>eth0 master bond0
ip <span class="nb">link set </span>eth1 master bond0
ip addr add 10.35.1.2/24 dev bond0
ip <span class="nb">link set </span>bond0 up
</code></pre></div></div> <p>The IP belongs to <code class="language-plaintext highlighter-rouge">bond0</code>; it should not be separately assigned to <code class="language-plaintext highlighter-rouge">eth0</code> and <code class="language-plaintext highlighter-rouge">eth1</code>.</p> <p>Inspect negotiated state with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> /proc/net/bonding/bond0
</code></pre></div></div> <p>Evidence of a healthy LAG includes the expected bonding mode, one active aggregator, both intended members participating, matching partner system information, and synchronized collecting/distributing state.</p> <p>Check the Layer-3 neighbor relationship from another host:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip neigh show 10.35.1.2
arping <span class="nt">-I</span> eth0 10.35.1.2
</code></pre></div></div> <p>Capture ARP traffic when duplicate-address alarms appear:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-eni</span> any arp and host 10.35.1.2
</code></pre></div></div> <p>A stable source hardware address for the bonded IP strongly supports a single logical host identity. If the same IP is repeatedly advertised by two unrelated source MAC addresses, investigate a real address conflict, an unusual bonding mode, virtualization behavior, clustering, or misconfiguration.</p> <p>On a bridge-capable Linux switch or test system, inspect the forwarding database:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bridge fdb show | <span class="nb">grep</span> <span class="nt">-i</span> <span class="s1">'aa:bb:cc:dd:ee:ff'</span>
</code></pre></div></div> <p>If the same host MAC continually moves between two <strong>independent</strong> physical switch ports, that is MAC flapping. If those ports are legitimate members of one logical LAG, the control plane should interpret the attachment through the aggregate rather than as two competing paths.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>LACP does not create multiple Layer-3 hosts.</strong> Its purpose is to make several physical links usable as one logical link.</li> <li><strong>A duplicate-IP alarm is an inference, not proof.</strong> The detector may correlate IP addresses with MAC addresses, switch ports, ARP observations, DHCP state, or a mixture of those signals.</li> <li><strong>One IP with two unrelated source MAC addresses is materially different from one MAC arriving over two synchronized LAG members.</strong> The former suggests competing Layer-3 identities; the latter may be normal aggregation if the observer lacks LAG awareness.</li> <li><strong>MAC flapping is a strong indicator of a broken aggregation boundary.</strong> Typical causes include configuring only one side as a LAG, placing members in different logical aggregates, mismatched VLAN treatment, or connecting members to independent switches without a multi-chassis aggregation technology.</li> <li><strong>Aggregate bandwidth is not single-flow bandwidth.</strong> Hash-based distribution increases parallel capacity and resilience, not necessarily throughput for one TCP session.</li> <li><strong>Physical link-up is insufficient evidence.</strong> LACP synchronization and Collecting/Distributing state determine whether a member is operationally forwarding as part of the aggregate.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Configure aggregation consistently on both endpoints and verify that all intended members join the same operational aggregator.</li> <li>Assign IPv4 addressing to the logical bond or team interface rather than duplicating the address independently on member NICs.</li> <li>Keep member links compatible in speed, duplex, VLAN handling, MTU, and aggregation policy.</li> <li>When a duplicate-IP warning appears, correlate <strong>ARP captures</strong>, <strong>neighbor-cache entries</strong>, <strong>bond/LACP state</strong>, and the <strong>switch forwarding database</strong> before concluding that a rogue DHCP server or duplicate static address exists.</li> <li>Treat changing IP-to-MAC mappings as higher-risk evidence than the same logical host appearing through multiple synchronized aggregation members.</li> <li>If monitoring software raises alarms on an otherwise healthy LAG, determine whether its detection logic is LAG-aware. Suppressing an alert is appropriate only after proving stable Layer-3 identity and correct aggregation state.</li> </ul> <p>The decisive troubleshooting principle is to identify the abstraction boundary. A healthy LAG hides physical path diversity beneath one logical attachment. If Layer-2 and Layer-3 evidence remains consistent at that boundary, multiple active cables are expected. If identity changes across those paths, the issue is no longer cosmetic: the aggregation, host configuration, or address ownership model requires investigation.</p>]]></content><author><name></name></author><category term="Networking"/><category term="lacp"/><category term="link-aggregation"/><category term="arp"/><summary type="html"><![CDATA[How IEEE link aggregation interacts with MAC learning and ARP, why bonded hosts can trigger duplicate-IP alarms, and how to distinguish benign observations from genuine Layer-2 faults.]]></summary></entry><entry><title type="html">Why LACP Fails Across Independent Chassis: MC-LAG Partner Identity and STP Interaction</title><link href="https://harshvermac5.github.io/learn/2026/mclag-lacp-partner-identity/" rel="alternate" type="text/html" title="Why LACP Fails Across Independent Chassis: MC-LAG Partner Identity and STP Interaction"/><published>2026-09-09T15:58:00+00:00</published><updated>2026-09-09T15:58:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/mclag-lacp-partner-identity</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/mclag-lacp-partner-identity/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>A link aggregation group is only stable when every selected member represents the same logical attachment between two LACP systems. A common design failure appears when one side is a true multi-chassis aggregation pair, while the opposite side consists of two independent switches that are not participating in a shared multi-chassis LAG domain.</p> <p>The physical topology may look symmetrical, but logical symmetry exists only when both switch pairs coordinate aggregation state and present consistent LACP identity. If one side places all links into one aggregator while the opposite side presents different partner identities, LACP excludes or churns mismatched members. STP can then appear guilty because its port states change after the aggregate changes.</p> <p>The objective is to understand <strong>why LACP System ID consistency is fundamental to multi-chassis designs</strong> and how to separate LACP-driven churn from STP-driven convergence.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Ethernet Link Aggregation, Multi-Chassis LAG, LACP Partner Selection, and RSTP Convergence</strong></p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>The current standards foundation for Ethernet link aggregation is <strong>IEEE 802.1AX-2020, Link Aggregation</strong>. IEEE describes the purpose of aggregation as allowing <a href="https://standards.ieee.org/ieee/802.1AX/6768/">“parallel point-to-point links to be used as if they were a single link”</a>. The standard is MAC-independent and defines the control behavior required to combine multiple physical links into one logical service.</p> <p>LACP originated in <strong>IEEE 802.3ad-2000</strong> and later moved into the IEEE 802.1 family. <strong>IEEE/ISO/IEC 8802-1AX-2021</strong> is the international adoption of IEEE 802.1AX-2020.</p> <p>Bridging and spanning tree behavior are governed by <strong>IEEE 802.1Q-2022</strong>. IEEE states that it covers <a href="https://standards.ieee.org/ieee/802.1Q/10323/">“the operation of MAC Bridges and VLAN Bridges, including management, protocols, and algorithms”</a>. RSTP functionality historically associated with IEEE 802.1D is now incorporated into the 802.1Q bridge architecture.</p> <p>The essential design rule is simple: an aggregator is not merely a collection of ports with matching configuration. It is a set of links whose LACP information proves that they connect the same two logical systems with compatible operational keys.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>LACP uses Ethernet Slow Protocol EtherType <strong>0x8809</strong>, subtype <strong>0x01</strong>, and multicast destination <strong>01:80:C2:00:00:02</strong>. Each LACPDU advertises actor and learned partner information.</p> <p>Important actor and partner attributes include:</p> <ul> <li><strong>System Priority</strong> and <strong>System ID</strong>, where the System ID is normally derived from a MAC address and identifies the logical LACP system.</li> <li><strong>Key</strong>, which identifies ports that are eligible to participate in the same aggregator.</li> <li><strong>Port Priority</strong> and <strong>Port Number</strong>, which identify the individual member.</li> <li>State flags such as <strong>Activity</strong>, <strong>Timeout</strong>, <strong>Aggregation</strong>, <strong>Synchronization</strong>, <strong>Collecting</strong>, <strong>Distributing</strong>, <strong>Defaulted</strong>, and <strong>Expired</strong>.</li> </ul> <p>The receive state machine learns partner information on each physical link; selection logic then determines which links can join one aggregator. Matching VLANs or physical proximity is irrelevant if learned partner information is incompatible.</p> <p>A true multi-chassis pair can advertise one coordinated System ID and compatible Key across both chassis, so a peer may legitimately select links terminating on either chassis into one logical bundle. Reverse the topology and the requirement still applies: if four upstream members terminate on two independent downstream switches, some links advertise <strong>System ID A</strong> and others <strong>System ID B</strong>. If one downstream switch is not running LACP, those links may provide no usable partner state at all. One aggregator cannot treat those links as one partner.</p> <p>Typical results are:</p> <ul> <li>Members with an incompatible System ID remain unselected.</li> <li>Ports oscillate between selected and unselected states as partner information changes.</li> <li>The aggregate temporarily loses synchronized members and is declared unavailable until valid members return.</li> </ul> <p>LACP <strong>active</strong> versus <strong>passive</strong> controls who initiates negotiation. At least one side must be active. This does not solve a partner-identity mismatch. Short versus long timeout behavior changes failure-detection speed, not aggregator eligibility.</p> <p>The LACP multiplexer state machine controls whether a member is synchronized, collecting, and distributing. Invalid partner information can stop forwarding while physical carrier remains up: <strong>Layer-1 link-up does not imply active LAG membership at Layer 2</strong>.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The primary authoritative references are:</p> <ul> <li><strong>IEEE 802.1AX-2020</strong>, <em>IEEE Standard for Local and Metropolitan Area Networks—Link Aggregation</em>, published 2020. This is the principal standard for LACP and MAC-independent link aggregation.</li> <li><strong>IEEE/ISO/IEC 8802-1AX-2021</strong>, the international adoption of IEEE 802.1AX-2020, published 2021.</li> <li><strong>IEEE 802.1Q-2022</strong>, <em>Bridges and Bridged Networks</em>, published 2022. This defines modern bridge behavior, VLAN operation, and spanning-tree algorithms including RSTP functionality.</li> <li><strong>IEEE 802.3ad-2000</strong>, the historical Ethernet amendment in which LACP was originally standardized before migration to IEEE 802.1AX.</li> </ul> <p>The architectural principle is that physical redundancy becomes one logical forwarding object only when the control plane presents consistent shared identity and state.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>The following topology is valid only if the two switches on each side participate in a coordinated multi-chassis aggregation domain:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>        Logical system X
       +--------------+
       | chassis X1   |
       | chassis X2   |
       +--------------+
          \\  |  //
           \\ | //
            \\|//
       +--------------+
       | chassis Y1   |
       | chassis Y2   |
       +--------------+
        Logical system Y
</code></pre></div></div> <p>If the lower pair is not a shared logical LACP system, one four-member trunk becomes an invalid design assumption:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Aggregator on system X
  member 1 -&gt; independent system Y1
  member 2 -&gt; independent system Y1
  member 3 -&gt; independent system Y2
  member 4 -&gt; independent system Y2

Observed partner identities:
  members 1-2: System ID = Y1
  members 3-4: System ID = Y2
</code></pre></div></div> <p>A safer architecture is to terminate separate aggregators on each independent chassis and let the bridge control plane provide path redundancy:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>logical-aggregate agg10
  members -&gt; system Y1
  lacp mode active

logical-aggregate agg20
  members -&gt; system Y2
  lacp mode active

RSTP/MSTP controls which redundant Layer-2 path forwards.
</code></pre></div></div> <p>Packet capture can prove the mismatch. On a Linux capture host or mirrored port, inspect Slow Protocol frames:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-ni</span> eth0 <span class="nt">-e</span> <span class="s1">'ether proto 0x8809'</span>
</code></pre></div></div> <p>In Wireshark, use:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lacp
</code></pre></div></div> <p>or inspect the reserved destination directly:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>eth.dst == 01:80:c2:00:00:02
</code></pre></div></div> <p>Compare <strong>Actor System ID</strong>, <strong>Actor Key</strong>, <strong>Partner System ID</strong>, <strong>Partner Key</strong>, and <strong>Synchronization/Collecting/Distributing</strong> flags across all intended members. Different partner System IDs inside one supposed aggregate prove that the logical topology differs from the cabling diagram.</p> <p>A representative event sequence may look like this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LAG member becomes non-active
aggregate loses synchronized members
RSTP forwarding -&gt; discarding
RSTP role designated -&gt; disabled
LACP member becomes synchronized again
RSTP discarding -&gt; learning -&gt; forwarding
</code></pre></div></div> <p>Ordering matters. If aggregate membership changes first and spanning-tree state follows, STP is reacting. If a stable aggregate is blocked first because of BPDU/root-path logic, STP may be the initiating mechanism.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>LACP aggregates logical partners, not merely cables.</strong> Matching speed, VLANs, and port counts are insufficient.</li> <li><strong>System ID consistency is central to MC-LAG.</strong> A multi-chassis pair must coordinate the identity and state exposed to its LACP partner.</li> <li><strong>One-sided MC-LAG does not make both sides multi-chassis capable.</strong> Independent peer switches cannot automatically be treated as one partner.</li> <li><strong>LACP active/passive is a negotiation behavior, not a topology repair mechanism.</strong> It cannot reconcile different partner System IDs or missing aggregation configuration.</li> <li><strong>Physical link state and logical forwarding state are different.</strong> A port may remain electrically up while LACP removes it from Collecting/Distributing state.</li> <li><strong>STP logs can be secondary evidence.</strong> Repeated forwarding, discarding, learning, and role transitions may reflect LAG churn rather than an STP defect.</li> <li><strong>Symmetric configuration matters.</strong> Multi-chassis designs require equivalent aggregation intent, keys, and peer-domain state.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Validate the logical topology before cabling: identify which physical switches are expected to present a single LACP system on each side.</li> <li>Never place links terminating on independent partner systems into one LACP aggregator unless a standards-compatible multi-chassis mechanism makes those systems appear as one logical partner.</li> <li>Confirm <strong>System ID</strong>, <strong>Key</strong>, synchronization, and Collecting/Distributing state on every member rather than relying only on an interface showing <code class="language-plaintext highlighter-rouge">up</code>.</li> <li>Use LACP active on at least one side of every bundle and treat timeout tuning as a convergence choice, not as a fix for identity mismatch.</li> <li>Keep RSTP or MSTP enabled when separate Layer-2 paths provide redundancy; spanning tree still protects the wider bridged topology.</li> <li>During incident analysis, correlate LACP state transitions, aggregator membership, physical carrier events, and STP transitions by timestamp. This establishes which control plane moved first.</li> <li>For active/active connectivity across two chassis on both ends, verify that both switch pairs explicitly support and are configured for multi-chassis aggregation. Redundant cabling alone does not create a multi-chassis LAG.</li> </ul> <p>The general lesson is architectural: <strong>a bundle is valid only when every selected link agrees on who the partner is</strong>. Once that invariant is violated, LACP does exactly what it was designed to do—exclude inconsistent members—and every dependent Layer-2 protocol must reconverge around the resulting topology.</p>]]></content><author><name></name></author><category term="Networking"/><category term="lacp"/><category term="mclag"/><category term="stp"/><summary type="html"><![CDATA[A deep technical analysis of how LACP partner identity, multi-chassis aggregation, and spanning tree interact when one logical bundle spans independent switches.]]></summary></entry><entry><title type="html">WireGuard Multi-WAN Return-Path Failures: Policy Routing, NAT, and Endpoint Roaming</title><link href="https://harshvermac5.github.io/learn/2026/wireguard-multiwan-return-paths/" rel="alternate" type="text/html" title="WireGuard Multi-WAN Return-Path Failures: Policy Routing, NAT, and Endpoint Roaming"/><published>2026-09-09T15:58:00+00:00</published><updated>2026-09-09T15:58:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/wireguard-multiwan-return-paths</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/wireguard-multiwan-return-paths/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>A WireGuard tunnel can fail even when the initiator reaches the correct peer IP and UDP port and the responder receives the handshake. The fault may exist entirely in the <strong>return path</strong>: the responder uses a source address from one WAN, normal routing selects another WAN for the destination, and a policy rule intended to correct that mismatch stops matching because NAT changed the peer’s observed UDP source port.</p> <p>The core principle is <strong>source-aware routing in multihomed systems</strong>. A multi-WAN node must keep source-address selection, egress-interface selection, policy routing, and NAT state consistent. WireGuard adds another variable because peer endpoints can change dynamically as authenticated traffic arrives from new IP:port tuples.</p> <p>An interface being administratively up does not prove that the tunnel is usable. A peer can exist in the running configuration and receive inbound UDP while the cryptographic session still fails because the responder’s packets never return to the initiator.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p>The subject is <strong>multihomed UDP tunnel routing</strong>, combining:</p> <ul> <li>WireGuard handshake and endpoint roaming</li> <li>Linux policy routing and multiple routing tables</li> <li>IPv4 source-address and egress-interface selection</li> <li>UDP NAT address-and-port mapping</li> </ul> <p>The failure is not primarily cryptographic. It is <strong>path inconsistency around the encrypted UDP transport</strong>.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>WireGuard is a connectionless UDP tunnel using the <strong>Noise_IK</strong> handshake. Its protocol documentation describes a handshake initiation from the initiator, a handshake response from the responder, and periodic retries when the response is not received. Only after key exchange can encrypted transport packets flow. See the <a href="https://www.wireguard.com/protocol/">WireGuard protocol and cryptography documentation</a>.</p> <p>Repeated handshake initiations with no successful <code class="language-plaintext highlighter-rouge">latest handshake</code> update therefore point toward a missing responder-to-initiator path, packet filtering, or invalid response handling rather than an inner-tunnel routing problem.</p> <p>Multihoming introduces source-selection constraints. RFC 1122, <strong>Requirements for Internet Hosts — Communication Layers</strong> (1989), covers outbound routing and source-address selection. For responses, it says the source address <a href="https://www.rfc-editor.org/rfc/rfc1122.html">“SHOULD be the specific-destination address of the request”</a>. It also describes strong-versus-weak host behavior, including whether a host may transmit a packet through an interface that does not correspond to its source address.</p> <p>Local permission does not guarantee Internet delivery. RFC 2827 / <strong>BCP 38</strong> (2000) and RFC 3704 / <strong>BCP 84</strong> (2004) describe source-address validation and ingress filtering. RFC 8704 (2020) refines feasible-path uRPF for multihomed networks. A packet sourced from ISP-A’s prefix but transmitted through ISP-B can therefore be discarded upstream even when the local kernel routes it successfully.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>Consider a dual-WAN gateway with <code class="language-plaintext highlighter-rouge">wan0</code> and <code class="language-plaintext highlighter-rouge">wan1</code>. A WireGuard socket is associated with an address on <code class="language-plaintext highlighter-rouge">wan0</code>, but the remote peer’s public address numerically belongs to a prefix directly connected to <code class="language-plaintext highlighter-rouge">wan1</code>.</p> <p>The sequence is:</p> <ul> <li>The peer sends a WireGuard handshake initiation to the gateway’s <code class="language-plaintext highlighter-rouge">wan0</code> address and UDP port.</li> <li>The packet reaches the correct socket and is authenticated.</li> <li>WireGuard prepares a handshake response sourced from the local endpoint associated with <code class="language-plaintext highlighter-rouge">wan0</code>.</li> <li>The kernel performs a route lookup for the peer’s public IP.</li> <li>A more-specific connected route on <code class="language-plaintext highlighter-rouge">wan1</code> wins over the default route on <code class="language-plaintext highlighter-rouge">wan0</code>.</li> <li>The response can leave <code class="language-plaintext highlighter-rouge">wan1</code> while carrying a source address assigned to <code class="language-plaintext highlighter-rouge">wan0</code>.</li> <li>Upstream source-address validation may discard the packet, so the initiator never receives the response.</li> </ul> <p>Policy routing can override this by selecting a routing table whose default route exits <code class="language-plaintext highlighter-rouge">wan0</code>. Linux <code class="language-plaintext highlighter-rouge">ip rule</code> supports selectors such as source, destination, <code class="language-plaintext highlighter-rouge">fwmark</code>, IP protocol, source port, and destination port. The <a href="https://man7.org/linux/man-pages/man8/ip-rule.8.html"><code class="language-plaintext highlighter-rouge">ip-rule(8)</code> manual</a> documents these selectors.</p> <p>The fragile design is matching on a <strong>remote UDP port assumed to be permanent</strong>. WireGuard supports authenticated endpoint roaming; its routing documentation explicitly states that <a href="https://www.wireguard.com/netns/">“WireGuard endpoints can roam”</a>. After receiving valid traffic from a new source IP or port, the implementation may update the peer’s endpoint to that observed tuple.</p> <p>NAT makes port changes normal. RFC 4787, <strong>NAT Behavioral Requirements for Unicast UDP</strong> (2007), defines UDP address-and-port mappings and discusses <a href="https://www.rfc-editor.org/rfc/rfc4787.html">“port preservation”</a>. A NAT is not guaranteed to expose the same source port used by the internal socket. A tunnel listening locally on UDP/51820 might appear remotely as UDP/40231.</p> <p>If a policy rule says “use WAN-A only when the remote port is 51820,” but the live peer endpoint has roamed to UDP/40231, the rule no longer matches. Destination-based routing takes over and may select the wrong uplink.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>Applicable standards and authoritative references include:</p> <ul> <li><strong>RFC 1122, 1989</strong> — host routing, multihoming, UDP source-address behavior, and strong-versus-weak host considerations.</li> <li><strong>RFC 2827 / BCP 38, 2000</strong> — ingress filtering against inappropriate or spoofed source addresses.</li> <li><strong>RFC 3704 / BCP 84, 2004</strong> — source validation and reverse-path filtering in multihomed networks.</li> <li><strong>RFC 4787, 2007</strong> — UDP NAT mappings, filtering behavior, port assignment, and mapping lifetime.</li> <li><strong>RFC 8704 / BCP 84, 2020</strong> — enhanced feasible-path uRPF for multihomed environments.</li> <li><strong>WireGuard protocol documentation</strong> — Noise_IK handshake, retry behavior, key establishment, and UDP encapsulation.</li> <li><strong>WireGuard routing documentation</strong> — endpoint roaming and <code class="language-plaintext highlighter-rouge">fwmark</code>-based routing intended to avoid stale endpoint-specific rules.</li> </ul> <p>These sources converge on one practical requirement: <strong>a multihomed UDP service must use an egress path compatible with its selected source address without assuming the peer’s translated port is stable</strong>.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>Use RFC 5737 documentation prefixes. Assume a gateway owns <code class="language-plaintext highlighter-rouge">198.51.100.10</code> on <code class="language-plaintext highlighter-rouge">wan0</code> and <code class="language-plaintext highlighter-rouge">203.0.113.10/24</code> on <code class="language-plaintext highlighter-rouge">wan1</code>. The peer is <code class="language-plaintext highlighter-rouge">203.0.113.44</code>, directly reachable through <code class="language-plaintext highlighter-rouge">wan1</code>, while WireGuard is intended to use <code class="language-plaintext highlighter-rouge">wan0</code>.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wan0: 198.51.100.10
wan1: 203.0.113.10/24
peer: 203.0.113.44
local WireGuard UDP: 51820
observed peer UDP: 40231
</code></pre></div></div> <p>A route lookup can expose the conflict:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip route get 203.0.113.44 from 198.51.100.10
</code></pre></div></div> <p>A problematic result resembles:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>203.0.113.44 dev wan1 src 198.51.100.10
</code></pre></div></div> <p>The destination exits <code class="language-plaintext highlighter-rouge">wan1</code>, but the source belongs to <code class="language-plaintext highlighter-rouge">wan0</code>.</p> <p>A fragile policy rule could be:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip rule add to 203.0.113.44 ipproto udp dport 51820 lookup 100 priority 100
ip route add default via 198.51.100.1 dev wan0 table 100
</code></pre></div></div> <p>It stops matching if the live peer endpoint becomes UDP/40231.</p> <p>A more resilient pattern is to classify the tunnel socket independently of the peer’s current port. WireGuard’s Linux integration provides <code class="language-plaintext highlighter-rouge">fwmark</code> for this purpose:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wg <span class="nb">set </span>wg0 fwmark 0xca6c
ip rule add fwmark 0xca6c lookup 100 priority 100
ip route add default via 198.51.100.1 dev wan0 table 100
</code></pre></div></div> <p>The exact policy depends on the topology, but the principle is consistent: <strong>route by tunnel identity, source, VRF, or mark rather than a NAT-sensitive remote port</strong>.</p> <p>Useful diagnostics include:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wg show
ip rule show
ip route show table all
ip route get &lt;peer-ip&gt; from &lt;local-wan-ip&gt;
ss <span class="nt">-uapn</span>
conntrack <span class="nt">-L</span> <span class="nt">-p</span> udp
</code></pre></div></div> <p>Capture simultaneously on both uplinks. Seeing the initiation arrive on the intended WAN and the response leave another WAN immediately exposes the return-path split.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Interface up is not tunnel up.</strong> Successful handshakes and receive counters are stronger evidence than link state.</li> <li><strong>Connected routes can defeat WAN affinity.</strong> Longest-prefix match usually beats a default route.</li> <li><strong>Source address matters beyond the host.</strong> Provider source-validation policies can reject packets using the wrong uplink.</li> <li><strong>UDP ports are not stable identities across NAT.</strong> A configured listen port and a remotely observed source port are different things.</li> <li><strong>Endpoint roaming is intentional.</strong> Routing logic that assumes an immutable WireGuard IP:port tuple is brittle.</li> <li><strong>Policy rules must match live traffic.</strong> A correct-looking rule has no effect when one selector no longer matches.</li> <li><strong>One-way handshakes localize the fault.</strong> If the responder sees initiations while the initiator receives zero bytes, prioritize return routing, NAT, firewall state, and upstream filtering.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Keep tunnel <strong>source addresses and egress WANs aligned</strong>.</li> <li>Validate paths with <code class="language-plaintext highlighter-rouge">ip route get &lt;peer&gt; from &lt;source&gt;</code> instead of checking only default routes.</li> <li>Avoid policy rules tied to translated remote UDP ports unless port preservation is explicitly guaranteed.</li> <li>Prefer <strong>packet marks, source-based rules, VRFs, or dedicated routing tables</strong> for WAN affinity.</li> <li>Design routing policy to tolerate WireGuard endpoint roaming.</li> <li>Capture on all candidate WANs during failures; interface counters alone cannot show the response path.</li> <li>Monitor both <code class="language-plaintext highlighter-rouge">transfer</code> counters and <code class="language-plaintext highlighter-rouge">latest handshake</code>; high transmit with zero receive is a strong return-path signal.</li> <li>Account for BCP 38/84 source validation when evaluating locally legal but externally implausible paths.</li> </ul> <p>The broader lesson is architectural: <strong>encrypted tunnels still depend on ordinary IP routing underneath them</strong>. Cryptography can be completely correct while inconsistent routing, NAT state, or source selection makes the tunnel unusable.</p>]]></content><author><name></name></author><category term="Networking"/><category term="wireguard"/><category term="policy-routing"/><category term="multiwan"/><summary type="html"><![CDATA[A deep technical analysis of how multihoming, policy routing, NAT port translation, and WireGuard endpoint roaming can create one-way tunnel failures.]]></summary></entry><entry><title type="html">Why Layer 2 Discovery Fails Across Routed VPNs</title><link href="https://harshvermac5.github.io/learn/2026/routed-vpn-layer2-discovery/" rel="alternate" type="text/html" title="Why Layer 2 Discovery Fails Across Routed VPNs"/><published>2026-09-07T21:28:00+00:00</published><updated>2026-09-07T21:28:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/routed-vpn-layer2-discovery</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/routed-vpn-layer2-discovery/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>Remote-access VPNs can provide complete IP reachability to an internal network while still failing applications that depend on Ethernet adjacency. The architectural distinction is that most remote-access VPNs extend <strong>Layer 3 reachability</strong>, not the <strong>Layer 2 broadcast domain</strong>.</p> <p>This matters for <strong>Link Layer Discovery Protocol (LLDP)</strong>, raw Ethernet discovery, ARP-dependent discovery, and broadcast-oriented management tools. A remote client may successfully ping, SSH to, or open TCP sessions with a device while remaining unable to participate in the same Ethernet segment.</p> <p>The governing principle is straightforward: <strong>routing transports IP packets between subnets; bridging transports Ethernet frames within or between Layer 2 segments</strong>. A firewall rule can permit routed traffic, but it cannot convert an IP tunnel into an Ethernet bridge.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Layer 3 VPN forwarding, Ethernet adjacency, LLDP scope, Proxy ARP, and Layer 2 overlay design.</strong></p> <p>The engineering question is not simply whether a VPN client can use an address from the same IP prefix as a LAN. It is whether the tunnel preserves the forwarding semantics required by the application: unicast IP, broadcast IP, multicast IP, ARP, or arbitrary Ethernet frames.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>IEEE <strong>802.1AB-2016</strong>, <em>Station and Media Access Control Connectivity Discovery</em>, defines LLDP for topology discovery between adjacent IEEE 802 stations. IEEE describes the protocol as supporting <a href="https://standards.ieee.org/ieee/802.1AB/6047/">“discovering the physical topology from adjacent stations in IEEE 802 LANs”</a>. The word <strong>adjacent</strong> is fundamental: LLDP is intended for local-link visibility rather than routed discovery.</p> <p>LLDP commonly uses the nearest-bridge group MAC address <strong>01:80:C2:00:00:0E</strong>. IEEE material describes this destination as constraining propagation to a single physical link. Bridges do not normally relay frames addressed to that reserved group address, and routers do not forward the Ethernet frame at all.</p> <p>RFC <strong>1812</strong>, <em>Requirements for IP Version 4 Routers</em> (1995), reinforces the Layer 3 boundary. Section 5.3.4 states that <a href="https://www.rfc-editor.org/rfc/rfc1812.html#section-5.3.4">“A router MUST NOT forward any packet that the router received as a Link Layer broadcast”</a>, subject to the IP-multicast exception described there. LLDP is even further removed from normal IP forwarding because an LLDPDU is an Ethernet PDU, not an IP packet.</p> <p>ARP is defined by RFC <strong>826</strong> (1982). RFC <strong>1027</strong> (1987) later documented <strong>Proxy ARP</strong>, where a gateway answers ARP on behalf of a host reachable through another interface. Its goal is to let hosts communicate <a href="https://www.rfc-editor.org/rfc/rfc1027.html">“without being aware of the existence of subnets”</a>. Proxy ARP can therefore hide an IP routing boundary, but it does not transport arbitrary Ethernet frames across that boundary.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A routed remote-access VPN introduces a logical Layer 3 interface. The remote host receives or configures a tunnel address, installs routes for protected prefixes, and sends matching IP packets into an encrypted tunnel. The VPN gateway decapsulates those packets and forwards them according to its IP routing table.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Remote host
10.255.0.10/24
    |
virtual VPN interface
    |
encrypted IP tunnel
    |
VPN gateway
10.255.0.1/24
    |
Layer 3 routing
    |
LAN interface
192.0.2.1/24
    |
LAN host
192.0.2.50/24
</code></pre></div></div> <p>The VPN and LAN are separate Layer 3 interfaces. When the gateway forwards an IP packet, it removes the incoming Layer 2 encapsulation, performs an IP route lookup, updates forwarding metadata such as TTL or Hop Limit, resolves the egress next-hop link-layer address, and creates a new Layer 2 frame on the destination interface.</p> <p>LLDP never enters that forwarding path. An LLDP frame uses EtherType <strong>0x88CC</strong> and is processed by an LLDP agent at Layer 2. It contains no source or destination IP address on which a router could perform a route lookup.</p> <p>LLDP itself is stateful. The transmit state machine periodically constructs an LLDPDU containing identification data such as <strong>Chassis ID</strong>, <strong>Port ID</strong>, and <strong>Time To Live</strong>, followed by optional TLVs and an end marker. IEEE 802.1AB configuration models define <strong>msgTxInterval</strong> for periodic transmission and <strong>msgTxHold</strong> as the multiplier used to calculate the advertised lifetime. A commonly recommended interval is <strong>30 seconds</strong> with a hold multiplier of <strong>4</strong>, producing a nominal <strong>120-second TTL</strong>.</p> <p>The receive state machine stores neighbor information until the TTL expires. A TTL of zero instructs the receiving agent to remove the neighbor immediately. These timers govern neighbor database aging; they do not provide any mechanism for routing LLDP across subnets.</p> <p>Proxy ARP changes only the IP reachability model. Consider a LAN using <strong>192.0.2.0/24</strong> and a remote client assigned <strong>192.0.2.200</strong> even though it is not physically attached to that Ethernet segment. When a LAN host broadcasts an ARP request for 192.0.2.200, the gateway can answer with its own MAC address. The LAN host then sends IP packets to the gateway, which routes or tunnels them to the remote client.</p> <p>The client can therefore appear on-subnet from an IP addressing perspective without becoming a true Layer 2 peer. LLDP, spanning-tree BPDUs, unknown EtherTypes, and other link-local frames remain outside the routed data path unless the tunnel explicitly provides Ethernet bridging or pseudowire behavior.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The relevant standards form a clear hierarchy:</p> <ul> <li><strong>IEEE 802.1AB-2016</strong> defines LLDP behavior, LLDPDUs, managed objects, transmission state, reception state, and local-link topology discovery. <strong>802.1ABcu-2021</strong> adds YANG management models without changing the fundamental adjacency model.</li> <li><strong>RFC 826</strong>, <em>An Ethernet Address Resolution Protocol</em> (1982), defines ARP address resolution on a local network.</li> <li><strong>RFC 1027</strong>, <em>Using ARP to Implement Transparent Subnet Gateways</em> (1987), documents Proxy ARP for hiding subnet boundaries from hosts.</li> <li><strong>RFC 1812</strong>, <em>Requirements for IP Version 4 Routers</em> (1995), defines IPv4 router forwarding behavior and link-layer broadcast restrictions.</li> <li><strong>RFC 3931</strong>, <em>Layer Two Tunneling Protocol Version 3</em> (2005), defines a framework for tunneling Layer 2 connections between IP nodes.</li> <li><strong>RFC 4448</strong>, <em>Encapsulation Methods for Transport of Ethernet over MPLS Networks</em> (2006), defines Ethernet pseudowires carrying Ethernet/802.3 PDUs over MPLS.</li> <li><strong>RFC 7348</strong>, <em>Virtual eXtensible Local Area Network</em> (2014), describes VXLAN as a Layer 2 overlay over Layer 3 infrastructure.</li> <li><strong>RFC 7432</strong>, <em>BGP MPLS-Based Ethernet VPN</em> (2015), defines EVPN control-plane procedures for distributed Ethernet services.</li> </ul> <p>These technologies solve different problems. A remote-access VPN focuses on protected IP connectivity. L2TPv3, Ethernet pseudowires, VXLAN, and EVPN explicitly address Layer 2 transport or overlay behavior.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>On a Linux endpoint, the routing boundary is visible through interface and route inspection:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip address show
ip route show
</code></pre></div></div> <p>A routed VPN may produce evidence similar to:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>10.255.0.10/24 dev vpn0
192.0.2.0/24 dev vpn0
</code></pre></div></div> <p>LLDP can be verified independently by capturing EtherType 0x88CC or its destination MAC:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-eni</span> eth0 ether proto 0x88cc
tcpdump <span class="nt">-eni</span> eth0 ether dst 01:80:c2:00:00:0e
</code></pre></div></div> <p>A local segment may show:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>01:80:c2:00:00:0e, ethertype LLDP (0x88cc), length 142
</code></pre></div></div> <p>The same capture on a routed VPN interface should not reveal the LAN’s native LLDP frames unless a deliberate Layer 2 encapsulation mechanism exists.</p> <p>ARP behavior can be contrasted with LLDP using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip neigh show
arping <span class="nt">-I</span> eth0 192.0.2.50
</code></pre></div></div> <p>Proxy ARP may cause a gateway MAC address to appear for a destination that is not physically local. That demonstrates address-resolution mediation, not Ethernet transparency.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Same-subnet addressing is not equivalent to same Layer 2 adjacency.</strong> Address assignment and forwarding behavior are separate design dimensions.</li> <li><strong>Firewall policy cannot create a missing protocol layer.</strong> A rule can allow or deny traffic handled by the forwarding plane; it cannot make a router relay arbitrary link-local Ethernet frames.</li> <li><strong>Proxy ARP is an IP reachability technique, not a generic Layer 2 tunnel.</strong> It changes ARP resolution so traffic reaches a gateway that can forward it elsewhere.</li> <li><strong>Discovery requirements must be classified by protocol.</strong> LLDP, ARP, IPv4 broadcast, mDNS, SSDP, and vendor-defined UDP discovery have different forwarding constraints.</li> <li><strong>Layer 2 extension enlarges the failure domain.</strong> Broadcast, unknown-unicast, control-plane traffic, loops, and MAC learning can all span the overlay.</li> <li><strong>Layer 3 boundaries are usually deliberate.</strong> Routed VPNs provide cleaner segmentation, smaller broadcast domains, and more explicit security-policy enforcement.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <p>Design remote-management workflows around the actual discovery mechanism rather than assuming IP reachability is sufficient.</p> <ul> <li>Prefer management protocols that use <strong>routable unicast IP</strong> when remote administration is required.</li> <li>Verify whether a discovery application depends on <strong>LLDP, ARP, broadcast, multicast, or a vendor-specific EtherType</strong> before changing firewall policy.</li> <li>Capture traffic on both sides of the routing boundary to prove where discovery frames terminate.</li> <li>Treat same-prefix VPN addressing as a routing and address-resolution feature unless the design explicitly states that Ethernet frames are bridged.</li> <li>Deploy Layer 2 overlays only when required, and account for MTU overhead, broadcast propagation, loop prevention, MAC learning, failure-domain expansion, and security exposure.</li> <li>When Layer 2 locality is unavoidable, a controlled management endpoint inside the target segment is often simpler than extending the entire broadcast domain across a remote-access path.</li> </ul> <p>The decisive troubleshooting question is not “Can the VPN reach the LAN?” but <strong>“Which protocol data unit must cross the boundary, and does the tunnel preserve that layer’s forwarding semantics?”</strong></p>]]></content><author><name></name></author><category term="Networking"/><category term="vpn"/><category term="lldp"/><category term="layer2"/><category term="proxy-arp"/><category term="ethernet"/><summary type="html"><![CDATA[A standards-based analysis of why LLDP and other link-local discovery protocols do not traverse routed remote-access VPNs, and where proxy ARP and Layer 2 overlays fit.]]></summary></entry><entry><title type="html">PPPoE MTU, MSS Clamping, and Path MTU Black Holes</title><link href="https://harshvermac5.github.io/learn/2026/pppoe-mtu-mss-clamping/" rel="alternate" type="text/html" title="PPPoE MTU, MSS Clamping, and Path MTU Black Holes"/><published>2026-09-07T20:42:00+00:00</published><updated>2026-09-07T20:42:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/pppoe-mtu-mss-clamping</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/pppoe-mtu-mss-clamping/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>A network can have working DNS, successful TCP handshakes, responsive web pages, and still fail for video, gaming, mobile applications, or other high-throughput traffic. One common cause is an <strong>MTU mismatch</strong> combined with failed <strong>Path MTU Discovery (PMTUD)</strong>.</p> <p>This is especially relevant on <strong>Point-to-Point Protocol over Ethernet (PPPoE)</strong> access links. PPPoE adds eight bytes of overhead inside the Ethernet payload, reducing the conventional IP MTU from 1500 bytes to 1492 bytes unless the network supports larger Ethernet frames.</p> <p>The core principle is simple: <strong>every IP packet must fit the smallest MTU along its path, or the sender must learn to transmit smaller packets</strong>. If that learning mechanism fails, small packets may succeed while larger packets disappear, producing a partial-connectivity failure that looks application-specific.</p> <p>The incident that motivated this analysis was ultimately associated with a PPPoE forwarding-path defect. The original support context described an <a href="https://community.ui.com/releases/UniFi-OS-Dream-Router-7-4-1-20/54761021-50b8-4732-95d1-78a127dd9acc"><code class="language-plaintext highlighter-rouge">"issue ... when it comes to RJ45 ports connected over PPPoE"</code></a>. The transferable lesson, however, is the standards-based interaction between PPPoE overhead, PMTU discovery, TCP MSS negotiation, and packet-processing paths.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>WAN Encapsulation, TCP Path MTU Discovery, and MSS Control</strong></p> <p>The relevant mechanisms span several layers:</p> <ul> <li>PPPoE encapsulation at the access edge</li> <li>IP Path MTU Discovery</li> <li>TCP Maximum Segment Size negotiation</li> <li>ICMP error delivery</li> <li>Packetization-layer probing</li> <li>Router forwarding and queueing paths</li> </ul> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>Classic PPPoE is defined by <a href="https://www.rfc-editor.org/rfc/rfc2516.html">RFC 2516</a>, published in 1999. Ethernet normally carries a maximum payload of 1500 octets. A PPPoE session consumes six octets for the PPPoE header and two octets for the PPP Protocol ID, leaving:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1500 - 6 - 2 = 1492 bytes
</code></pre></div></div> <p>RFC 2516 therefore specifies a maximum PPP MTU of 1492 bytes on a conventional 1500-byte Ethernet path. <a href="https://www.rfc-editor.org/rfc/rfc4638.html">RFC 4638</a>, published in 2006, later defined negotiation for payloads larger than 1492 when the Ethernet infrastructure supports larger frames.</p> <p>For IPv4, <a href="https://www.rfc-editor.org/rfc/rfc1191.html">RFC 1191</a>, published in 1990, defines PMTUD. A host sends packets with the <strong>Don’t Fragment</strong> bit set. If a router cannot forward a packet because the outgoing MTU is smaller, the router returns an ICMP <strong>Destination Unreachable, Fragmentation Needed</strong> message containing information that allows the sender to reduce its packet size.</p> <p>IPv6 does not permit routers to fragment transit packets. <a href="https://www.rfc-editor.org/rfc/rfc8201.html">RFC 8201</a>, published in 2017, defines IPv6 PMTUD using ICMPv6 <strong>Packet Too Big</strong> messages. If required ICMP messages are filtered or lost, the sender can continue transmitting oversized packets; <a href="https://www.rfc-editor.org/rfc/rfc2923.html">RFC 2923</a>, published in 2000, documents this as the classic <strong>PMTU black hole</strong>.</p> <p>TCP adds another control point through the <strong>Maximum Segment Size</strong>, or MSS. <a href="https://www.rfc-editor.org/rfc/rfc6691.html">RFC 6691</a>, published in 2012, clarifies MSS handling. For a simple IPv4 TCP flow with no options:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1492 byte IP MTU
- 20 byte IPv4 header
- 20 byte TCP header
= 1452 byte TCP MSS
</code></pre></div></div> <p>For IPv6 with a 40-byte base header:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1492 byte IP MTU
- 40 byte IPv6 header
- 20 byte TCP header
= 1432 byte TCP MSS
</code></pre></div></div> <p>Tunnels and additional encapsulation can reduce the effective payload further.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A normal PPPoE TCP connection begins with PPPoE establishment and PPP negotiation. The forwarding interface then exposes an effective IP MTU, commonly 1492 bytes.</p> <p>During the TCP three-way handshake, endpoints advertise MSS values in SYN packets. Problems arise when an endpoint behind a router assumes a 1500-byte local Ethernet MTU while the WAN path only supports 1492 bytes.</p> <p>A TCP sender may then construct a 1460-byte IPv4 TCP payload:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1460 TCP data
+ 20 TCP header
+ 20 IPv4 header
= 1500 byte IP packet
</code></pre></div></div> <p>That packet cannot fit into a 1492-byte PPPoE payload.</p> <p>With functioning IPv4 PMTUD, the constrained hop drops the oversized packet, returns ICMP fragmentation-needed, and the sender lowers its PMTU estimate. With broken PMTUD, the packet disappears without useful feedback. SYNs, ACKs, DNS, and small objects may succeed while larger TLS records, media transfers, or application data stall. This explains how a host can have <strong>Internet access</strong> while selected applications report <strong>no Internet</strong>.</p> <p><strong>MSS clamping</strong> changes the TCP MSS value in transit, usually on SYN packets crossing a router. The goal is to ensure that downstream endpoints never generate TCP segments larger than the real path can carry.</p> <p>A conservative fixed value such as <code class="language-plaintext highlighter-rouge">1380</code> may avoid fragmentation across PPPoE plus extra overhead, but it is not universally optimal. An unnecessarily low MSS increases packet count and protocol overhead. Where supported, PMTU-aware clamping is preferable to an arbitrary constant.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <p>The key standards for this failure domain are:</p> <ul> <li><a href="https://www.rfc-editor.org/rfc/rfc2516.html">RFC 2516</a>, <strong>A Method for Transmitting PPP Over Ethernet</strong>, 1999</li> <li><a href="https://www.rfc-editor.org/rfc/rfc4638.html">RFC 4638</a>, <strong>Accommodating an MTU/MRU Greater Than 1492 in PPPoE</strong>, 2006</li> <li><a href="https://www.rfc-editor.org/rfc/rfc1191.html">RFC 1191</a>, <strong>Path MTU Discovery</strong>, 1990</li> <li><a href="https://www.rfc-editor.org/rfc/rfc2923.html">RFC 2923</a>, <strong>TCP Problems with Path MTU Discovery</strong>, 2000</li> <li><a href="https://www.rfc-editor.org/rfc/rfc4821.html">RFC 4821</a>, <strong>Packetization Layer Path MTU Discovery</strong>, 2007</li> <li><a href="https://www.rfc-editor.org/rfc/rfc6691.html">RFC 6691</a>, <strong>TCP Options and Maximum Segment Size</strong>, 2012</li> <li><a href="https://www.rfc-editor.org/rfc/rfc8201.html">RFC 8201</a>, <strong>Path MTU Discovery for IPv6</strong>, 2017</li> <li><a href="https://www.rfc-editor.org/rfc/rfc8899.html">RFC 8899</a>, <strong>Packetization Layer Path MTU Discovery for Datagram Transports</strong>, 2020</li> </ul> <p>RFC 4821 and RFC 8899 are important because <strong>Packetization Layer PMTUD (PLPMTUD)</strong> reduces dependence on ICMP delivery by probing packet sizes and inferring a usable path size from successful delivery.</p> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>On Linux, first inspect the effective interface MTU:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip <span class="nb">link </span>show
ip route get 203.0.113.10
</code></pre></div></div> <p>For IPv4, probe the path while preventing fragmentation:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ping <span class="nt">-M</span> <span class="k">do</span> <span class="nt">-s</span> 1464 203.0.113.10
ping <span class="nt">-M</span> <span class="k">do</span> <span class="nt">-s</span> 1465 203.0.113.10
</code></pre></div></div> <p>The ICMP payload plus the 20-byte IPv4 header and 8-byte ICMP header must fit the path; a 1464-byte payload produces a 1492-byte IP packet.</p> <p><code class="language-plaintext highlighter-rouge">tracepath</code> can also estimate the path MTU:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tracepath 203.0.113.10
</code></pre></div></div> <p>Capture TCP handshakes to inspect negotiated MSS:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcpdump <span class="nt">-ni</span> any <span class="s1">'tcp[tcpflags] &amp; tcp-syn != 0'</span>
</code></pre></div></div> <p>On a Linux router, MSS adjustment can be implemented with netfilter. A path-aware rule is preferable when available:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>iptables <span class="nt">-t</span> mangle <span class="nt">-A</span> FORWARD   <span class="nt">-p</span> tcp <span class="nt">--tcp-flags</span> SYN,RST SYN   <span class="nt">-j</span> TCPMSS <span class="nt">--clamp-mss-to-pmtu</span>
</code></pre></div></div> <p>A fixed clamp is more deterministic but should be justified by the encapsulation budget:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>iptables <span class="nt">-t</span> mangle <span class="nt">-A</span> FORWARD   <span class="nt">-p</span> tcp <span class="nt">--tcp-flags</span> SYN,RST SYN   <span class="nt">-j</span> TCPMSS <span class="nt">--set-mss</span> 1380
</code></pre></div></div> <p>Packet captures should then confirm that the rewritten MSS appears in forwarded SYN packets and that large data segments no longer exceed the effective path.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <p><strong>Partial connectivity is strong evidence.</strong> If DNS, TCP establishment, and small HTTP transactions work while large transfers fail, investigate packet size before blaming application-layer filtering.</p> <p><strong>PPPoE changes the layer-three assumptions of an Ethernet LAN.</strong> A client can correctly use a 1500-byte LAN MTU while the WAN edge must transmit through a 1492-byte PPPoE payload.</p> <p><strong>ICMP is part of normal IP control behavior.</strong> Blanket filtering of ICMP or ICMPv6 can break PMTUD and create failures that are difficult to correlate with firewall policy.</p> <p><strong>MSS clamping is mitigation, not discovery.</strong> It prevents oversized TCP segments but does not repair UDP, QUIC, IPsec, or other protocols that do not use TCP MSS negotiation.</p> <p><strong>Queueing or traffic-shaping features can alter forwarding behavior.</strong> If a queueing discipline unexpectedly fixes an MTU-sensitive problem, traffic may be traversing a different software, hardware-offload, or scheduling path; queueing itself is not necessarily the root cause.</p> <p><strong>Modern applications expose MTU faults differently.</strong> QUIC runs over UDP, so TCP MSS clamping cannot directly fix it. PLPMTUD becomes more important as application stacks move beyond TCP.</p> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Document the real WAN MTU for every encapsulated uplink.</li> <li>Treat PPPoE, IPsec, GRE, VXLAN, WireGuard, and nested tunnels as explicit MTU-budget calculations.</li> <li>Permit the ICMP and ICMPv6 messages required for PMTUD.</li> <li>Validate large packets with DF-aware probes rather than relying only on ordinary ping.</li> <li>Inspect SYN/SYN-ACK MSS values when TCP flows stall after establishment.</li> <li>Prefer dynamic PMTU-aware MSS adjustment over unexplained fixed values.</li> <li>Remember that MSS clamping protects TCP only; test UDP and QUIC separately.</li> <li>Use packet captures on both sides of the constrained hop to distinguish packet generation, forwarding, and return-path failures.</li> <li>When a traffic-management feature changes the symptom, investigate forwarding-path implementation differences instead of concluding that congestion was the cause.</li> <li>Prefer PLPMTUD-capable transports and operating systems where classical ICMP-based PMTUD cannot be trusted.</li> </ul> <p>The engineering objective is not merely to make a failing application work. It is to ensure that every layer agrees on the largest packet that can cross the path, and that endpoints have a reliable mechanism to adapt when that path changes.</p>]]></content><author><name></name></author><category term="Networking"/><category term="pppoe"/><category term="mtu"/><category term="tcp"/><category term="mss"/><category term="pmtud"/><summary type="html"><![CDATA[A deep technical guide to how PPPoE overhead, Path MTU Discovery failures, and TCP MSS handling can cause selective application connectivity problems.]]></summary></entry><entry><title type="html">Why SFP Compatibility Problems Are Usually Not Voltage Problems</title><link href="https://harshvermac5.github.io/learn/2026/sfp-compatibility-not-voltage/" rel="alternate" type="text/html" title="Why SFP Compatibility Problems Are Usually Not Voltage Problems"/><published>2026-09-07T20:11:00+00:00</published><updated>2026-09-07T20:11:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/sfp-compatibility-not-voltage</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/sfp-compatibility-not-voltage/"><![CDATA[<p>SFP compatibility problems are often blamed on voltage differences between transceivers. In practice, that is usually the wrong place to start.</p> <p>For SFP+ modules, the host provides nominal <strong>3.3 V</strong> power on the transmitter and receiver supply rails. The SFP+ electrical specifications also define tight voltage, current, hot-plug, and power-consumption limits. A module that is actually compliant with the form-factor specification should therefore not require a completely different operating voltage simply because it comes from another vendor.</p> <p>The more common causes are <strong>host firmware policy</strong>, <strong>EEPROM identity checks</strong>, <strong>unsupported power levels</strong>, <strong>SerDes or PHY incompatibility</strong>, and <strong>optical-layer mismatches</strong>.</p> <h2 id="the-33-v-supply-does-not-guarantee-compatibility">The 3.3 V Supply Does Not Guarantee Compatibility</h2> <p>The legacy <strong>SFF-8431</strong> SFP+ specification defines separate <strong>VccT</strong> and <strong>VccR</strong> 3.3 V supply contacts. Its low-speed electrical section specifies a nominal 3.3 V module supply, while the power requirements define limits that include ripple, droop, and noise.</p> <p>This means two modules can use the same nominal supply voltage and still behave very differently in the same switch.</p> <p>Power consumption is a separate issue from voltage. The SFF-8431 addendum defines SFP+ power levels of approximately:</p> <ul> <li><strong>Power Level I:</strong> up to 1.0 W</li> <li><strong>Power Level II:</strong> up to 1.5 W</li> <li><strong>Power Level III:</strong> up to 2.0 W</li> </ul> <p>Higher-power modules are not simply allowed to draw their full rated power immediately. The host and module coordinate higher-power operation through the management interface. A host designed only for lower-power optics may therefore detect the module but refuse to enable its requested power state.</p> <p>Hot insertion also creates transient current requirements. If the host power rail, filtering, connector path, or protection circuitry cannot tolerate the module’s startup behavior, the result may be module resets, port instability, or hardware protection events. That is a <strong>power-budget or transient-current problem</strong>, not a different-voltage problem.</p> <h2 id="eeprom-identification-and-vendor-enforcement">EEPROM Identification and Vendor Enforcement</h2> <p>SFP and SFP+ modules expose identification data through a two-wire management interface. The EEPROM contains fields such as:</p> <ul> <li><strong>Vendor name</strong></li> <li><strong>Vendor OUI</strong></li> <li><strong>Part number and revision</strong></li> <li><strong>Transceiver compliance codes</strong></li> <li><strong>Nominal signaling rate</strong></li> <li><strong>Supported wavelength</strong></li> <li><strong>Diagnostic-monitoring capabilities</strong></li> </ul> <p>A network device can read this information before enabling normal operation.</p> <p>Some vendors use these fields as part of a compatibility policy. The switch may compare the module against an approved optics database, validate vendor-specific data, or mark an unknown module as unsupported. Depending on the platform, the module may generate only a warning, remain disabled, or place the interface into an error state.</p> <p>This explains why an electrically valid third-party optic can work in one switch but be rejected by another.</p> <h2 id="the-host-phy-must-support-the-modules-signaling">The Host PHY Must Support the Module’s Signaling</h2> <p>Mechanical fit does not imply signaling compatibility.</p> <p>A <strong>1 GbE SFP</strong> commonly uses a 1.25 GBd serial interface for 1000BASE-X, while a <strong>10 GbE SFP+</strong> Ethernet interface operates around 10.3125 GBd for 10GBASE-R. The host SerDes must support the required signaling rate and the surrounding PHY/PCS logic must support the Ethernet mode being requested.</p> <p>This is why inserting a 1G SFP into a 10G SFP+ cage does not automatically guarantee a 1G link. Some hosts support rate adaptation or explicitly support both 1G and 10G operation. Others are fixed to 10G signaling.</p> <p>When troubleshooting, verify both the module and the port support the same:</p> <ul> <li>Ethernet standard and data rate</li> <li>SerDes signaling mode</li> <li>Auto-negotiation behavior where applicable</li> <li>Fiber or copper media type</li> <li>FEC requirements on newer interfaces</li> </ul> <p>A module can be perfectly healthy and still fail because the host cannot generate or interpret the electrical signaling it requires.</p> <h2 id="optical-compatibility-still-matters">Optical Compatibility Still Matters</h2> <p>Once the module initializes, the optical link must still satisfy the relevant physical-medium specification.</p> <p>Check <strong>wavelength, fiber type, link budget, and optic pairing</strong>. Common failures include connecting a multimode optic to an unsuitable fiber plant, using mismatched BiDi wavelengths, exceeding receiver input limits, or having insufficient receive power because of attenuation and dirty connectors.</p> <p>Optical parameters such as transmitter power, receiver sensitivity, extinction ratio, and jitter tolerance matter, but two properly compliant modules implementing the same Ethernet PMD should normally interoperate within their specified link budget. If they do not, suspect excessive loss, contamination, marginal hardware, or a module that does not actually meet the claimed specification.</p> <p>Digital diagnostics can make this visible. On supported Linux interfaces, inspect module EEPROM and DOM data with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ethtool eth0
ethtool <span class="nt">-m</span> eth0
</code></pre></div></div> <p>On network switches, inspect the vendor-specific equivalents for transceiver inventory, DOM values, interface errors, and system logs. Useful evidence includes <strong>Tx power, Rx power, temperature, voltage, bias current, LOS state, CRC counters, and unsupported-transceiver messages</strong>.</p> <h2 id="a-better-troubleshooting-order">A Better Troubleshooting Order</h2> <p>When an SFP or SFP+ module does not work, avoid starting with the assumption that the voltage is wrong.</p> <ul> <li>Confirm the exact <strong>port type, supported speeds, and supported transceiver families</strong>.</li> <li>Read the module EEPROM and verify the <strong>identifier, vendor fields, compliance codes, and nominal signaling rate</strong>.</li> <li>Check system logs for <strong>unsupported-module, EEPROM, I2C, power, or PHY initialization errors</strong>.</li> <li>Verify the host can supply the module’s <strong>required power level</strong> and thermal budget.</li> <li>Confirm both ends use compatible <strong>speed, wavelength, fiber type, and optical standard</strong>.</li> <li>Check DOM readings and physical-layer counters before replacing hardware.</li> </ul> <p>The key distinction is simple: <strong>voltage compatibility is only one prerequisite</strong>. Successful operation also depends on power limits, management-plane acceptance, electrical signaling, PHY support, and optical interoperability. Treating those layers separately usually makes SFP troubleshooting much faster and prevents a healthy module from being misdiagnosed as electrically incompatible.</p>]]></content><author><name></name></author><category term="Networking"/><category term="sfp"/><category term="optics"/><category term="ethernet"/><category term="troubleshooting"/><summary type="html"><![CDATA[A practical look at why SFP and SFP+ modules fail to initialize or link despite sharing the same nominal supply voltage.]]></summary></entry><entry><title type="html">Troubleshooting IKEv2 Authentication and Traffic Selector Failures</title><link href="https://harshvermac5.github.io/learn/2026/ikev2-auth-traffic-selectors/" rel="alternate" type="text/html" title="Troubleshooting IKEv2 Authentication and Traffic Selector Failures"/><published>2026-09-07T19:47:00+00:00</published><updated>2026-09-07T19:47:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/ikev2-auth-traffic-selectors</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/ikev2-auth-traffic-selectors/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>A site-to-site IPsec tunnel can appear partially established while remaining unusable. The correct troubleshooting model is to treat <strong>IKEv2 as a sequence of distinct protocol stages</strong>, not a single up-or-down event.</p> <p><strong>IKE_SA_INIT, IKE_AUTH, and CHILD_SA creation prove different things</strong>. Proposal negotiation does not prove that the peer identity or pre-shared key is correct. Authentication does not prove that the protected networks are acceptable. A working CHILD_SA does not prove routing or forwarding. Troubleshooting should therefore follow the protocol state machine and stop at the first transition that fails.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>IPsec site-to-site VPN negotiation: IKEv2 authentication, Security Associations, Traffic Selectors, and IPsec policy architecture.</strong></p> <p>The key architectural boundary is between the <strong>IKE SA</strong>, which protects IKE control traffic and authenticates peers, and <strong>Child SAs</strong>, which protect user traffic, usually with ESP.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>IKEv2 is standardized in <strong>RFC 7296 / STD 79, Internet Key Exchange Protocol Version 2, October 2014</strong>. Its role is to authenticate peers, negotiate cryptographic parameters, derive keying material, and create IPsec SAs. The specification states: <a href="https://www.rfc-editor.org/rfc/rfc7296.html#section-1">“IKE is a component of IPsec used for performing mutual authentication and establishing and maintaining Security Associations.”</a></p> <p>The broader IPsec processing model is defined by <strong>RFC 4301, Security Architecture for the Internet Protocol, December 2005</strong>. That architecture separates policy, established SA state, and peer authorization. RFC 4301 describes the model directly: <a href="https://www.rfc-editor.org/rfc/rfc4301.html#section-4.4">“There are three nominal databases in this model: the Security Policy Database (SPD), the Security Association Database (SAD), and the Peer Authorization Database (PAD).”</a></p> <p>The <strong>SPD</strong> determines whether traffic is discarded, bypassed, or protected. The <strong>SAD</strong> contains instantiated SA state. The <strong>PAD</strong> ties authenticated identities to policy. Failures can therefore occur during peer-policy selection, Child SA creation, or packet-to-policy matching.</p> <p><strong>Policy-based VPN</strong> and <strong>route-based VPN</strong> are implementation descriptions, not core IETF protocol modes. Policy-based systems expose SPD-like selectors directly; route-based systems commonly bind IPsec to a logical interface and let routing choose traffic. Both still rely on standards-compliant IKEv2 and IPsec SAs.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>The first exchange is <strong>IKE_SA_INIT</strong>. The initiator sends an SA proposal, Diffie-Hellman key exchange material, and a nonce. The responder selects an acceptable proposal and returns its own key exchange material and nonce. NAT-detection notifications may also appear.</p> <p>Conceptually, the exchange is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Initiator                         Responder
SAi1, KEi, Ni        --&gt;
                     &lt;--  SAr1, KEr, Nr
</code></pre></div></div> <p>Completion proves bidirectional IKE reachability, an acceptable IKE suite, a compatible Diffie-Hellman group, and enough shared ephemeral material to derive IKE protection keys. It does <strong>not</strong> prove that the peers trust each other; the pre-shared key, certificate, or other authentication credential has not yet been validated.</p> <p>The second exchange is <strong>IKE_AUTH</strong>. It is encrypted and integrity protected with keys derived after IKE_SA_INIT. The peers exchange identities through <code class="language-plaintext highlighter-rouge">IDi</code> and <code class="language-plaintext highlighter-rouge">IDr</code>, prove possession of the corresponding authentication credential with the <code class="language-plaintext highlighter-rouge">AUTH</code> payload, and normally negotiate the first Child SA using <code class="language-plaintext highlighter-rouge">SAi2</code>, <code class="language-plaintext highlighter-rouge">SAr2</code>, <code class="language-plaintext highlighter-rouge">TSi</code>, and <code class="language-plaintext highlighter-rouge">TSr</code>.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Initiator                                      Responder
SK { IDi, AUTH, SAi2, TSi, TSr }   --&gt;
                                   &lt;--  SK { IDr, AUTH, SAr2, TSi, TSr }
</code></pre></div></div> <p>With pre-shared-key authentication, the <code class="language-plaintext highlighter-rouge">AUTH</code> value is derived from the configured shared secret plus transcript-dependent values. A wrong PSK can therefore allow <strong>IKE_SA_INIT to succeed and IKE_AUTH to fail</strong>. The same is true when the PSK is correct but the asserted identity selects a different credential or policy entry on the peer.</p> <p>Authentication-stage failures should focus on:</p> <ul> <li>Pre-shared-key equality and encoding.</li> <li><code class="language-plaintext highlighter-rouge">IDi</code> and <code class="language-plaintext highlighter-rouge">IDr</code> values.</li> <li>Identity-to-credential mapping.</li> <li>Authentication method selection.</li> <li>Integrity verification of the protected IKE_AUTH message.</li> </ul> <p>An optional <code class="language-plaintext highlighter-rouge">CERTREQ</code> payload does not, by itself, prove that certificate authentication is required. RFC 7296 permits certificate-request payloads in exchanges where a peer is indicating acceptable trust anchors. The actual configured authentication method and <code class="language-plaintext highlighter-rouge">AUTH</code> processing are more authoritative evidence.</p> <p>IKEv2 is request/response based. The requester retransmits after timeout, with exponentially increasing intervals, while Message IDs correlate requests and responses. Repeated identical IKE_AUTH requests with no response therefore differ materially from an explicit protected <code class="language-plaintext highlighter-rouge">AUTHENTICATION_FAILED</code> notification.</p> <p>After authentication, the first <strong>Child SA</strong> is created. It carries protected traffic and has its own transforms, SPIs, selectors, and lifetimes. Peer authentication can therefore succeed while Child SA negotiation fails.</p> <p><strong>Traffic Selectors</strong> are central to this stage. <code class="language-plaintext highlighter-rouge">TSi</code> describes traffic associated with the initiator side and <code class="language-plaintext highlighter-rouge">TSr</code> describes traffic associated with the responder side. Selectors can include IP ranges, protocol numbers, and port ranges. The responder may accept them, narrow them to an allowed subset, or reject them with <code class="language-plaintext highlighter-rouge">TS_UNACCEPTABLE</code> when policy permits none of the proposed traffic.</p> <p>The boundary is critical: <strong>authentication failure prevents the IKE SA from being established; selector failure can occur after authentication while blocking the desired Child SA.</strong></p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <ul> <li><strong>RFC 7296 / STD 79, 2014</strong> defines IKEv2 exchanges, identities, authentication, Traffic Selectors, retransmissions, Child SAs, and error notifications.</li> <li><strong>RFC 4301, 2005</strong> defines the IPsec architecture, including SPD, SAD, PAD, packet selectors, and the protect/bypass/discard processing model.</li> <li><strong>RFC 4303, 2005</strong> defines Encapsulating Security Payload, which provides confidentiality, integrity, authentication, and anti-replay services for protected IP traffic.</li> <li><strong>RFC 8247, 2017</strong> defines IKEv2 cryptographic algorithm implementation requirements and usage guidance. It should be read together with later updates rather than treated as a permanent list of preferred algorithms.</li> <li><strong>RFC 8221, 2017</strong> provides corresponding algorithm implementation guidance for ESP and AH.</li> <li><strong>RFC 9395, 2023</strong> formally deprecates IKEv1 and updates the status of obsolete cryptographic algorithms. New deployments should use IKEv2 and current cryptographic guidance.</li> </ul> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>Consider a generic diagnostic state:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Security Associations: 0 established, 1 connecting
IKEv2 state: CONNECTING
Selected IKE proposal: AES-CBC-256 / HMAC-SHA2-256 / PRF-SHA2-256 / MODP-2048
Active tasks: IKE_AUTH, IKE_ESTABLISH, CHILD_CREATE
</code></pre></div></div> <p>A selected proposal means IKE_SA_INIT progressed. If the session remains connecting while <code class="language-plaintext highlighter-rouge">IKE_AUTH</code> is active, investigate authentication and identity before changing Phase 1 transforms.</p> <p>A generic integrity-related diagnostic during IKE_AUTH might look like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>IKE_AUTH processing failed
protected-message integrity verification failed
</code></pre></div></div> <p>That evidence is consistent with incorrect derived authentication state, a credential or identity mismatch, or malformed protected input. It does not by itself prove an encryption-transform mismatch.</p> <p>Once authentication succeeds, inspect selector negotiation. Compare the intended protected domains in both directions; do not assume that <code class="language-plaintext highlighter-rouge">10.10.0.0/16 -&gt; 10.20.0.0/16</code> is represented identically on each peer.</p> <p>On a Linux IPsec implementation, useful data-plane and policy checks include:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip xfrm state
ip xfrm policy
ip route get 10.20.0.10
tcpdump <span class="nt">-ni</span> any <span class="s1">'udp port 500 or udp port 4500 or proto 50'</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">ip xfrm state</code> shows SAs, <code class="language-plaintext highlighter-rouge">ip xfrm policy</code> shows selector policy, <code class="language-plaintext highlighter-rouge">ip route get</code> confirms forwarding selection, and packet capture verifies whether IKE, NAT-T, and ESP packets are transmitted and returned.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>A selected IKE proposal is not proof of successful authentication.</strong> IKE_SA_INIT and IKE_AUTH solve different problems.</li> <li><strong>A wrong PSK often fails late enough to confuse operators.</strong> Diffie-Hellman negotiation can succeed even when the configured shared secrets differ.</li> <li><strong>Identity matters independently of the PSK string.</strong> A correct shared secret associated with the wrong <code class="language-plaintext highlighter-rouge">IDi</code> or <code class="language-plaintext highlighter-rouge">IDr</code> can still fail authentication or policy lookup.</li> <li><strong><code class="language-plaintext highlighter-rouge">CERTREQ</code> is not proof of certificate-based authentication.</strong> Diagnose the actual authentication method and <code class="language-plaintext highlighter-rouge">AUTH</code> payload behavior.</li> <li><strong>Traffic Selector failures are Child SA policy failures, not necessarily IKE authentication failures.</strong> Treat <code class="language-plaintext highlighter-rouge">AUTHENTICATION_FAILED</code> and <code class="language-plaintext highlighter-rouge">TS_UNACCEPTABLE</code> as different branches of the state machine.</li> <li><strong>Route-based and policy-based administration can interoperate only when their resulting selector expectations are compatible.</strong> A logical tunnel interface does not eliminate IKEv2 Traffic Selector negotiation.</li> <li><strong>Dashboard state is secondary evidence.</strong> The authoritative questions are whether an IKE SA exists, whether a Child SA exists, which selectors were installed, and whether packets match them.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Troubleshoot in strict protocol order: <strong>IKE_SA_INIT -&gt; IKE_AUTH -&gt; CHILD_SA -&gt; routing -&gt; security policy -&gt; packet forwarding</strong>.</li> <li>Record both peers’ IKE transforms, authentication method, identities, Child SA transforms, and protected prefixes before making changes.</li> <li>Treat PSKs as binary secrets with exact encoding requirements; avoid visually similar strings, trailing whitespace, and inconsistent secret handling.</li> <li>Define peer identities explicitly when multiple identities, FQDNs, addresses, or credential mappings are possible.</li> <li>Compare <code class="language-plaintext highlighter-rouge">TSi</code> and <code class="language-plaintext highlighter-rouge">TSr</code> from packet captures or detailed logs when Child SA creation fails.</li> <li>In route-oriented deployments, validate both the logical tunnel routing decision and the underlying IPsec selector policy.</li> <li>Use current IETF algorithm guidance and avoid preserving legacy transforms solely for convenience.</li> <li>Prefer evidence that maps directly to a protocol stage. A troubleshooting change is useful only when it tests a specific hypothesis about that stage.</li> </ul> <p>The transferable rule is: <strong>an IPsec tunnel is a chain of independently verifiable state transitions</strong>. Diagnose the first failed transition; later symptoms are downstream effects.</p>]]></content><author><name></name></author><category term="Networking"/><category term="ipsec"/><category term="ikev2"/><category term="troubleshooting"/><summary type="html"><![CDATA[A standards-based guide to separating IKE_SA_INIT, IKE_AUTH, CHILD_SA, and traffic-selector failures when diagnosing site-to-site IPsec tunnels.]]></summary></entry><entry><title type="html">IGMP Snooping, Querier Election, and Multicast Flooding</title><link href="https://harshvermac5.github.io/learn/2026/igmp-snooping-multicast-flooding/" rel="alternate" type="text/html" title="IGMP Snooping, Querier Election, and Multicast Flooding"/><published>2026-09-07T19:38:00+00:00</published><updated>2026-09-07T19:38:00+00:00</updated><id>https://harshvermac5.github.io/learn/2026/igmp-snooping-multicast-flooding</id><content type="html" xml:base="https://harshvermac5.github.io/learn/2026/igmp-snooping-multicast-flooding/"><![CDATA[<h2 id="problem-statement-or-learning-objective">Problem Statement or Learning Objective</h2> <p>Ethernet switches normally have no Layer 3 knowledge of which hosts want a particular IPv4 multicast stream. Without multicast-aware filtering, a bridge can replicate multicast frames across forwarding ports much like unknown traffic. <strong>IGMP snooping</strong> changes this behavior by observing Internet Group Management Protocol control traffic and building Layer 2 forwarding state for multicast groups.</p> <p>The core engineering problem is understanding why a network can have IGMP snooping enabled yet still flood multicast traffic. The answer usually lies in the separation between <strong>snooping</strong>, <strong>querier operation</strong>, <strong>membership state</strong>, and <strong>unknown multicast forwarding policy</strong>. A switch reporting itself as a non-querier is not inherently unhealthy; it may simply be observing the elected querier elsewhere on the subnet.</p> <h2 id="co-technical-subject">Co-Technical Subject</h2> <p><strong>Layer 2 multicast forwarding with IGMPv3 group-management state and querier election.</strong></p> <p>IGMP operates between IPv4 hosts and multicast routers, while snooping bridges inspect those messages to derive per-port forwarding decisions.</p> <h2 id="theoretical-foundation">Theoretical Foundation</h2> <p>The active bridging baseline is <strong>IEEE Std 802.1Q-2022, Bridges and Bridged Networks</strong>, which defines MAC bridging and VLAN operation. IEEE identifies it as the active standard governing how the MAC service is supported by bridged networks. At the IP multicast layer, the current IGMPv3 specification is <strong>RFC 9776, Internet Standard STD 100, March 2025</strong>. RFC 9776 obsoletes RFC 3376 and updates IGMPv2 behavior defined by RFC 2236.</p> <p>IGMP snooping itself is documented by <strong>RFC 4541, May 2006</strong>, an Informational RFC containing implementation recommendations rather than an Internet Standard. Its motivation is explicit: <a href="https://www.rfc-editor.org/rfc/rfc4541.html">“significant bandwidth can be wasted by flooding”</a>. Snooping therefore optimizes Layer 2 delivery by mapping observed receiver interest to bridge ports.</p> <p>RFC 9776 defines the control-plane behavior that snooping devices observe. It states that <a href="https://www.rfc-editor.org/rfc/rfc9776.html#section-6.6.2">“IGMPv3 elects a single querier per subnet”</a>. Election is based on source IPv4 address: when a multicast router receives a General Query from a lower address, it stops acting as querier and starts an Other-Querier-Present timer.</p> <p>This distinction is critical. <strong>The querier maintains membership freshness; the snooping bridge consumes that membership signaling to build forwarding state.</strong> A device can snoop successfully while remaining a non-querier.</p> <h2 id="mechanism-breakdown">Mechanism Breakdown</h2> <p>A healthy IGMP-snooped VLAN converges through a repeating control-plane cycle.</p> <ul> <li>The elected querier periodically sends <strong>General Query</strong> messages. RFC 9776 defines a default <strong>Query Interval</strong> of 125 seconds and a default <strong>Robustness Variable</strong> of 2.</li> <li>Hosts that want multicast reception maintain interface-level group state. When queried, they send membership reports within the advertised response window rather than all replying simultaneously.</li> <li>IGMPv3 membership reports are sent to <code class="language-plaintext highlighter-rouge">224.0.0.22</code>. Reports can express <strong>INCLUDE</strong> or <strong>EXCLUDE</strong> source-filtering state, allowing Source-Specific Multicast behavior as well as traditional Any-Source Multicast.</li> <li>A snooping bridge inspects these reports and associates the multicast group with the ingress port on which receiver interest was observed.</li> <li>The bridge separately identifies <strong>multicast-router ports</strong>, commonly by observing IGMP Queries arriving from those ports. RFC 4541 recommends maintaining this router-port knowledge because reports and multicast data must be delivered appropriately toward multicast-routing functions.</li> <li>Data for a registered multicast group is forwarded only toward interested receiver ports plus required multicast-router ports, rather than indiscriminately across the VLAN.</li> </ul> <p>Querier election is timer driven. When a router hears a General Query from a lower IPv4 address, it yields. The <strong>Other Querier Present Interval</strong> is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Other Querier Present Interval =
(Robustness Variable x Query Interval) +
(0.5 x Query Response Interval)
</code></pre></div></div> <p>With the RFC 9776 defaults of Robustness Variable <code class="language-plaintext highlighter-rouge">2</code>, Query Interval <code class="language-plaintext highlighter-rouge">125 s</code>, and Query Response Interval <code class="language-plaintext highlighter-rouge">10 s</code>, the interval is <code class="language-plaintext highlighter-rouge">255 s</code>. A non-querier therefore does not immediately take over if a few Queries are missed. It waits for the timer to expire before beginning General Queries itself.</p> <p>Receiver departure follows a different path. When group state may have changed, the querier can send <strong>Group Specific Queries</strong> or IGMPv3 <strong>Group-and-Source Specific Queries</strong>. The default <strong>Last Member Query Interval</strong> is 1 second, while the <strong>Last Member Query Count</strong> defaults to the Robustness Variable. This creates a short verification window before forwarding state is pruned.</p> <p>Snooping state must also age independently. RFC 4541 warns that a switch must not rely exclusively on Leave messages because receivers can disappear without transmitting a clean leave. Periodic queries and membership timers are therefore fundamental to correct pruning.</p> <h2 id="industry-standards-reference">Industry Standards Reference</h2> <ul> <li><strong>IEEE Std 802.1Q-2022</strong> — active standard for Bridges and Bridged Networks. It defines the underlying VLAN-aware bridging architecture on which multicast filtering operates. The IEEE description states that it <a href="https://standards.ieee.org/ieee/802.1Q/10323/">“specifies how the Media Access Control (MAC) Service is supported by Bridged Networks”</a>.</li> <li><strong>RFC 9776, March 2025, STD 100</strong> — current Internet Standard for IGMPv3. It defines membership reports, source filtering, querier election, protocol timers, compatibility behavior, and multicast-router state.</li> <li><strong>RFC 4541, May 2006</strong> — Informational recommendations for IGMP and MLD snooping switches. It separates snooping behavior into IGMP control forwarding and multicast data forwarding considerations.</li> <li><strong>RFC 4604, August 2006</strong> — IGMPv3 and MLDv2 requirements for Source-Specific Multicast.</li> </ul> <h2 id="practical-examples-and-evidence">Practical Examples and Evidence</h2> <p>The most useful diagnostic evidence is packet-level proof that Queries are present, Reports are returning, and multicast data forwarding corresponds to receiver state.</p> <p>On a Linux observation point or mirrored switch port, capture IGMP and multicast IPv4 traffic:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>tcpdump <span class="nt">-ni</span> eth0 <span class="s1">'igmp or (ip multicast)'</span>
</code></pre></div></div> <p>A healthy exchange may resemble:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>10.30.0.1 &gt; 224.0.0.1: igmp query v3
10.30.0.44 &gt; 224.0.0.22: igmp v3 report, group 239.20.30.40
10.30.0.50 &gt; 239.20.30.40: UDP, length 1316
</code></pre></div></div> <p>The evidence should be interpreted causally:</p> <ul> <li>General Queries prove that an active querier is refreshing group membership on the VLAN.</li> <li>Membership Reports prove that receivers are expressing interest and that a snooping bridge has control traffic available to learn from.</li> <li>Multicast data arriving only on receiver-facing ports demonstrates that forwarding state is being enforced.</li> <li>Multicast data appearing on unrelated access ports indicates either missing group state, unknown-multicast flooding policy, incorrect router-port classification, implementation limitations, or stale state after topology change.</li> </ul> <p>Compare a working and affected segment using the same group. If both see Queries but only one sees receiver Reports, the failure is downstream of the querier. If neither is visible, investigate VLAN continuity, filtering, topology, or control-plane forwarding first.</p> <h2 id="key-technical-insights">Key Technical Insights</h2> <ul> <li><strong>Non-querier status is normally expected.</strong> Only one querier should win per subnet. Multiple snooping switches can legitimately report non-querier while learning from the same router or Layer 3 interface.</li> <li><strong>Snooping configuration is not proof of snooping state.</strong> The operational question is whether group-to-port entries exist and are refreshed by observed Reports.</li> <li><strong>Unknown multicast flooding can be standards-consistent.</strong> RFC 4541 requires unregistered multicast to reach multicast-router ports and permits implementations to flood unregistered traffic more broadly. Flooding does not automatically prove that IGMP processing is disabled.</li> <li><strong>Missing Queries eventually destroys useful state.</strong> Without periodic General Queries, receiver memberships age out and streams can revert to unknown-multicast behavior.</li> <li><strong>Topology changes matter.</strong> RFC 4541 recommends that snooping switches account for spanning-tree changes because a forwarding-path change can invalidate learned multicast-port state and delay convergence.</li> <li><strong>Version compatibility matters.</strong> IGMPv3 networks may fall back when older-version Queries are observed. A device that only partially understands IGMPv3 reports can mis-handle source-filtered membership and either over-forward or incorrectly prune traffic.</li> </ul> <h2 id="prevention-strategies-and-takeaways">Prevention Strategies and Takeaways</h2> <ul> <li>Ensure every multicast-enabled VLAN has a stable, reachable IGMP querier, even when no multicast routing protocol is otherwise required on that segment.</li> <li>Treat the querier and the snooping function as separate control-plane roles. Do not diagnose a switch as faulty merely because it is a non-querier.</li> <li>Validate operation with packet captures and forwarding-state inspection rather than configuration screenshots alone.</li> <li>Confirm that General Queries reach every relevant Layer 2 segment and that Membership Reports return from receiver ports.</li> <li>Compare unknown-multicast policy with registered-group forwarding behavior before concluding that all flooding is a snooping failure.</li> <li>Keep IGMP versions and source-filtering capabilities consistent across the switching path, especially for IGMPv3 and SSM deployments.</li> <li>After spanning-tree, uplink, or VLAN topology changes, verify that multicast-router ports and membership entries reconverge correctly.</li> <li>Use timer tuning only after control-plane correctness is proven. Shorter timers may improve convergence but increase protocol traffic and sensitivity to loss; larger robustness values tolerate loss but increase leave latency.</li> </ul> <p>The decisive troubleshooting principle is to <strong>prove the entire membership lifecycle</strong>: identify the querier, observe Queries, confirm Reports, verify group-to-port state, and validate data-plane pruning. Multicast flooding usually reflects missing or untrusted forwarding state, not merely a disabled feature toggle.</p>]]></content><author><name></name></author><category term="Networking"/><category term="multicast"/><category term="igmp"/><category term="switching"/><summary type="html"><![CDATA[A standards-based analysis of how IGMP snooping builds multicast forwarding state, how querier election sustains that state, and why failures cause Layer 2 multicast flooding.]]></summary></entry></feed>